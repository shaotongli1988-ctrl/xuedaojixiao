---
description: cool-admin-midway 标准模块模板（参考 modules/demo）
globs: *.ts, *.json
---

# cool-admin-midway 标准模块模板

本规则用于在 `cool-admin-midway` 中创建**标准模块、标准 CRUD 接口、标准 service/entity/config 结构**。

优先参考：

1. 当前项目同类模块
2. `src/modules/demo`
3. `https://node.cool-admin.com`

如果是普通后台模块，默认按下面模板组织。

## 标准模块目录模板

```txt
src/modules/<module-name>/
  ├── config.ts
  ├── controller/
  │   ├── admin/
  │   │   └── <biz>.ts
  │   └── open/
  │       └── <biz>.ts
  ├── entity/
  │   └── <biz>.ts
  ├── service/
  │   └── <biz>.ts
  ├── event/
  ├── schedule/
  ├── dto/
  ├── db.json
  └── menu.json
```

最小可用模块至少应有：

- `config.ts`
- `entity/<biz>.ts`
- `controller/admin/<biz>.ts` 或 `controller/open/<biz>.ts`
- `service/<biz>.ts`

## config.ts 模板

适用于模块定义与模块级配置。

```ts
import { ModuleConfig } from '@cool-midway/core';

/**
 * 模块配置
 */
export default () => {
  return {
    name: '模块名称',
    description: '模块描述',
    middlewares: [],
    globalMiddlewares: [],
    order: 0,
  } as ModuleConfig;
};
```

要求：

- `config.ts` 必须存在
- `name` 与 `description` 写清楚模块职责
- 不要省略模块配置文件

## entity 模板

适用于标准业务实体。

```ts
import { BaseEntity } from '../../base/entity/base';
import { Column, Entity, Index } from 'typeorm';

/**
 * 示例实体
 */
@Entity('demo_goods')
export class DemoGoodsEntity extends BaseEntity {
  @Index()
  @Column({ comment: '标题', length: 50 })
  title: string;

  @Column({ comment: '状态', default: 1 })
  status: number;
}
```

要求：

- 实体类继承 `BaseEntity`
- 表名使用 `snake_case`
- 字段命名使用 `camelCase`
- 字段写清楚 `comment`
- 普通实体不要默认写复杂关联关系

## controller 模板

适用于标准后台 CRUD。

```ts
import { CoolController, BaseController } from '@cool-midway/core';
import { DemoGoodsEntity } from '../../entity/goods';
import { DemoGoodsService } from '../../service/goods';

/**
 * 商品模块-商品信息
 */
@CoolController({
  api: ['add', 'delete', 'update', 'info', 'list', 'page'],
  entity: DemoGoodsEntity,
  service: DemoGoodsService,
})
export class AdminDemoGoodsController extends BaseController {}
```

如果有标准筛选/联表分页，优先继续在 `@CoolController` 中补：

```ts
pageQueryOp: {
  keyWordLikeFields: ['a.description'],
  fieldEq: ['a.status'],
  fieldLike: ['a.title'],
}
```

要求：

- 标准 CRUD 默认优先使用 `@CoolController`
- Controller 保持轻量
- 不要把业务逻辑堆进 Controller
- 普通联表与筛选优先 `pageQueryOp` / `listQueryOp`

## service 模板

适用于模块业务逻辑承载。

```ts
import { Provide } from '@midwayjs/core';
import { BaseService } from '@cool-midway/core';
import { InjectEntityModel } from '@midwayjs/typeorm';
import { Repository } from 'typeorm';
import { DemoGoodsEntity } from '../entity/goods';

/**
 * 商品示例
 */
@Provide()
export class DemoGoodsService extends BaseService {
  @InjectEntityModel(DemoGoodsEntity)
  demoGoodsEntity: Repository<DemoGoodsEntity>;
}
```

如果需要自定义方法，优先写在 Service：

```ts
async entityPage(query) {
  const find = this.demoGoodsEntity.createQueryBuilder();
  return this.entityRenderPage(find, query);
}
```

要求：

- 业务逻辑优先放 Service
- 优先复用 `BaseService`
- 普通查询优先 TypeORM / QueryBuilder
- SQL 仅在必要时使用

## admin / open controller 选择

### admin

适用于后台管理接口，例如：

- 管理端 CRUD
- 运营配置
- 后台筛选列表

通常路径：

- `controller/admin/<biz>.ts`

### open

适用于公开接口或前台调用接口，例如：

- 开放查询
- 小程序 / App / H5 使用接口
- 无后台权限体系的公开能力

通常路径：

- `controller/open/<biz>.ts`

## 标准开发步骤

开发普通模块时，默认顺序：

1. 先参考 `src/modules/demo` 找最近的示例
2. 先建 `config.ts`
3. 再建 `entity`
4. 再建 `service`
5. 最后建 `controller`
6. 若有筛选/联表，再补 `pageQueryOp` / `listQueryOp`
7. 若有事件/任务，再补 `event` / `schedule`

## 不要这样写

以下情况优先判定为跑偏：

- 没有 `config.ts`
- 模块目录不完整
- 标准 CRUD 却不用 `@CoolController`
- Controller 中写大量业务逻辑
- 查询逻辑到处散落
- 能用 `pageQueryOp` 的筛选硬写成重复分页方法
- 普通模块默认写大量原始 SQL

## 最终目标

让 AI 或开发者在写模块时，默认产出：

- 结构清晰
- 接近 `src/modules/demo`
- 接近官方文档
- Controller 轻、Service 稳、Entity 清楚
- 不把普通模块写复杂
