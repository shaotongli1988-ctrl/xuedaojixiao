---
description: cool-admin-midway 反模式约束（防止把普通模块写复杂）
globs: *.ts, *.js, *.json
---

# cool-admin-midway 反模式规则

本规则专门用于限制模块开发时的“跑偏写法”。

## 核心原则

**如果这是一个普通 cool-admin 模块，就不要把它写成一套自定义框架实验。**

cool-admin-midway 已经提供了：

- 模块组织方式
- 标准 CRUD 模式
- 控制器组织方式
- 服务承载方式
- 实体映射方式
- 模块配置方式
- 常见事件、缓存、任务扩展方式

因此默认应该：

- 优先复用
- 优先贴近 `src/modules/demo`
- 优先贴近官方文档
- 优先降低复杂度

## 常见错误写法

### 错误 1：Controller 直接承载业务

反例特征：

- Controller 方法里写大量业务流程
- Controller 直接做复杂数据库操作
- Controller 直接承担跨模块编排

正确方式：

- Controller 只做入口与接口组织
- 业务逻辑进入 Service

### 错误 2：普通模块绕开 `@CoolController`

反例特征：

- 标准 CRUD 场景却完全不用 `@CoolController`
- 手写重复的 add/delete/update/info/list/page
- 一个普通模块写出大量重复样板代码

正确方式：

- 标准 CRUD 优先 `@CoolController`
- 只把真正特殊的接口单独自定义

### 错误 3：普通查询默认写成复杂 SQL 工程

反例特征：

- 能用 TypeORM / QueryBuilder 的查询全部写成原始 SQL
- 能用 `pageQueryOp` / `listQueryOp` 的筛选全部手写分页逻辑
- 查询能力分散在多个层级里重复实现

正确方式：

- 普通查询优先 TypeORM
- 联表分页优先 `pageQueryOp` / `listQueryOp`
- 只有必要时才写 SQL，并保持集中管理

### 错误 4：模块结构被打散

反例特征：

- controller/entity/service 不按模块归档
- 新增功能不补 `config.ts`
- 相关文件落在无关目录

正确方式：

- 优先维护模块边界
- 代码按模块收敛
- 模块配置保持完整

### 错误 5：乱设 prefix 与接口风格不统一

反例特征：

- 模块内 controller 到处手写 prefix
- 路由风格与 cool-admin 模块自动路由规则明显冲突
- 接口路径缺少统一模式

正确方式：

- 优先遵循自动路由
- 必须手写 prefix 时说明原因

## 标准模块默认选型

### 标准后台模块

默认优先：

- `config.ts`
- `entity`
- `controller`
- `service`
- `@CoolController`
- `BaseService`
- TypeORM

### 需要筛选或联表分页的模块

在标准模块基础上补：

- `pageQueryOp`
- `listQueryOp`
- QueryBuilder

### 需要事件/任务/缓存的模块

在标准模块基础上补：

- `event`
- `schedule`
- 对应缓存规则

## 什么时候可以不用这些默认模式

只有下面情况才允许明显偏离：

- 接口不是标准后台 CRUD 接口
- 业务流程远超模块 demo 范围
- cool 现有能力无法覆盖目标
- 当前项目已有更高优先级的既有实现方式

即便如此，也要先说明：

1. 为什么不能直接用 cool-admin 官方模块范式
2. 哪些部分仍然沿用模块结构与服务分层
3. 为什么新的复杂度是必要的

## 编码前快速判断

开始写之前，先问自己：

- 这是不是一个普通模块/普通后台接口？
- demo 里有没有相似例子？
- 官方文档里有没有现成模式？
- 我是不是准备手写一整套 cool 已经做过的模块组织和 CRUD 逻辑？

只要答案偏向“是”，就应该回到 cool-admin-midway 官方模块范式。

## 最终目标

让基于这个 base 开发出来的 midway 代码具备这些特征：

- 模块边界清晰
- 结构统一
- 接近官方范式
- Service 承担业务逻辑
- Controller 保持轻量
- 不因 AI 或个人习惯而无序膨胀

如果一个普通模块写完后让人感觉“怎么这么散、这么重、这么绕”，优先怀疑实现方式偏离了本规则。
