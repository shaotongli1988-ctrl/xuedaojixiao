---
description: cool-admin-midway 开发总控规则（优先模块 demo 与官方范式）
globs: *.ts, *.js, *.json
---

# cool-admin-midway 开发总控规则

本规则用于约束基于 `cool-admin-midway` 的模块、控制器、服务、实体与配套配置开发方式。

目标只有一个：**优先走 cool-admin-midway 官方文档与 `src/modules/demo` 已经提供的模块范式，不要把普通模块、普通接口写成一套过度自定义、过度分散、过度复杂的 Midway 项目。**

## 最高优先级参考顺序

处理模块、实体、接口、服务、事件、任务时，按下面顺序参考：

1. 当前项目中同类模块实现
2. `src/modules/demo` 中最接近的示例写法
3. `https://node.cool-admin.com`
4. 当前目录下其他 `.cursor/rules/*.mdc`
5. 通用 Midway / Node 最佳实践

如果前 1～3 项已经能覆盖，不要退回到“自己设计一套 controller / service / 路由 / 查询组织方式”。

## 开发默认路径

### 先判断是不是标准 cool-admin 模块

只要需求属于以下任意一种，默认按 cool-admin 官方模块方式开发：

- 标准后台 CRUD 接口
- 普通模块扩展
- 标准实体 + 服务 + 控制器开发
- 模块配置补充
- 常规联表分页与列表筛选
- 标准事件、缓存、任务能力接入

这类需求默认优先：

- `src/modules/<module-name>`
- `config.ts`
- `entity`
- `controller`
- `service`
- `@CoolController`
- `BaseService`
- `pageQueryOp` / `listQueryOp`
- TypeORM 实体与 QueryBuilder
- 模块化配置与模块边界

### 再判断是不是复杂例外场景

如果是复杂异步编排、第三方系统深度集成、特殊协议、非标准交互接口，才考虑在官方模块范式上局部扩展。

## 强制要求

### 1. 优先参考模块 demo

写新模块、新接口、新服务前，先找 `src/modules/demo` 中最接近的示例。

尤其优先参考：

- `controller/admin`
- `controller/open`
- `service`
- `entity`
- `config.ts`
- `event`
- `queue`

如果 demo 已经有现成模式，就以 demo 的模块组织方式与职责划分为主，而不是重造。

### 2. 标准模块优先使用官方组织方式

普通模块默认按下面结构落地：

- `src/modules/<module-name>/config.ts`
- `src/modules/<module-name>/entity/*`
- `src/modules/<module-name>/controller/*`
- `src/modules/<module-name>/service/*`

需要时再补：

- `event`
- `schedule`
- `dto`
- `middleware`
- `db.json`
- `menu.json`

### 3. 让 Controller、Service、Entity 各司其职

产出的代码应该满足：

- Controller 负责接口暴露、参数接收、调用 service、配置 CRUD
- Service 负责业务逻辑
- Entity 负责字段定义与表结构映射
- config.ts 负责模块说明与模块级配置

### 4. 常规模块复杂度必须受控

对于普通模块与普通后台接口：

- 能通过 `@CoolController` 标准 CRUD 表达的，不要手写一整套重复接口
- 能通过 `pageQueryOp` / `listQueryOp` 表达的，不要先写成大量重复查询逻辑
- 能通过 `BaseService` 与 TypeORM 组织的，不要先退回到散乱 SQL 与分散逻辑
- 能复用现有模块组织方式的，不要发明第二套目录结构

目标是：**标准问题走标准模块路径。**

## 明确禁止的反模式

以下写法默认视为反模式，除非任务明确要求或现有项目已经这样做：

### 1. Controller 里堆业务逻辑

反例特征：

- Controller 中写大量业务判断
- Controller 中直接组织复杂数据库查询
- Controller 中处理跨模块业务编排

正确方式：

- Controller 负责路由与入口
- 业务逻辑下沉到 Service

### 2. 绕开 Service 直接把逻辑散在各处

反例特征：

- Controller、event、task 中各自复制业务逻辑
- 同一业务规则没有统一 service 承载

正确方式：

- 业务规则优先集中在 Service
- 其他入口复用 Service，而不是各写一份

### 3. 乱设路由前缀或脱离模块自动路由

反例特征：

- 明明是模块内 controller，却大量手动指定 prefix
- 不理解 cool-admin 自动路由规则，导致接口风格混乱

正确方式：

- 优先遵循模块内自动路由规则
- 只有明确需要时才设置 prefix，并说明原因

### 4. 脱离模块结构自由落文件

反例特征：

- entity / service / controller 不按模块归档
- 业务代码散落到无关目录
- config.ts 缺失或模块边界不清

正确方式：

- 先按模块结构组织
- 再按功能补充 event/schedule/dto 等目录

### 5. 重复造通用 CRUD / 查询模式

反例特征：

- 明明标准 CRUD 足够，却手写 add/delete/update/info/list/page 全套接口
- 明明 `pageQueryOp` 能表达筛选，却写重复分页查询
- 明明 TypeORM 足够，却默认回退到原始 SQL 堆砌

正确方式：

- 标准 CRUD 优先 `@CoolController`
- 普通查询优先 TypeORM / QueryBuilder
- 联表分页优先 `pageQueryOp` / `listQueryOp`

## 允许例外的情况

以下情况可以不完全按标准模块范式：

- 高度定制的第三方协议接入
- 特殊实时通信或复杂异步编排
- 标准 CRUD 完全无法覆盖的复杂业务流程
- demo 与官方文档都无法覆盖的场景

但即使是例外，也要：

- 先说明为什么不能沿用官方模块模式
- 尽量局部突破，不要全局推翻
- 仍优先复用模块结构、Service、Entity、配置方式

## 输出检查清单

写完模块前自检：

1. 我是否先参考了 `src/modules/demo`？
2. 这个需求是否本来可以用标准模块结构与 `@CoolController` 表达？
3. 我是否把业务逻辑堆到了 Controller？
4. 我是否重复造了查询、CRUD、路由组织方式？
5. 这份代码看起来是否像 cool-admin-midway 项目，而不是普通自由发挥的 Midway 项目？

如果 2、3、4、5 任一答案不理想，就优先回退到 cool-admin 官方模块范式。
