# 权限矩阵

## 文档职责

这份文档是“权限规则”的唯一事实源。

它只负责定义：

1. 角色
2. 菜单权限
3. 页面按钮权限
4. 接口权限
5. 数据范围权限

## 不负责内容

这份文档不负责定义：

- 页面字段布局
- 接口字段格式
- 脱敏算法细节

脱敏细节以 `12-数据权限与脱敏规则.md` 为准。

## Batch 4 治理回写

2026-04-24 的第四批 SSOT 治理触达了 `performance` contract producer registry 与前端 decoder 共享运行时枚举，但没有新增、删除或重命名任何权限键，也没有改变页面/按钮显隐口径。

因此本页现有权限矩阵在本轮后继续有效，无需调整角色、菜单、按钮或接口授权判断。

## 权限命名规则

沿用当前 Cool 项目既有风格，统一使用：

`performance:<resource>:<action>`

示例：

- `performance:assessment:page`
- `performance:assessment:approve`
- `performance:goal:progressUpdate`
- `performance:salary:info`

## 角色定义

| 角色 | 说明 |
| --- | --- |
| HR 管理员 | 全局绩效配置、归档和敏感数据管理角色 |
| 部门经理 | 团队绩效审批和跟进角色 |
| 员工 | 个人绩效查看和自评角色 |

## 菜单权限

| 菜单 | 权限键 | HR 管理员 | 部门经理 | 员工 |
| --- | --- | --- | --- | --- |
| 绩效驾驶舱 | `performance:dashboard:summary` | 是 | 是 | 否 |
| 我的考核 | `performance:assessment:myPage` | 是 | 是 | 是 |
| 已发起考核 | `performance:assessment:page` | 是 | 是 | 否 |
| 待我审批 | `performance:assessment:pendingPage` | 是 | 是 | 否 |
| 指标库 | `performance:indicator:page` | 是 | 否 | 否 |
| 360 环评 | `performance:feedback:page` | 是 | 是 | 是 |
| 目标地图 | `performance:goal:page` | 是 | 是 | 是 |
| PIP 追踪 | `performance:pip:page` | 是 | 是 | 否 |
| 晋升管理 | `performance:promotion:page` | 是 | 是 | 否 |
| 薪资管理 | `performance:salary:page` | 是 | 否 | 否 |

补充说明：

1. `HR` 可以进入“我的考核”“已发起考核”“待我审批”三个页面查看数据。
2. `HR` 在“待我审批”页面首期只拥有查看权限，不拥有审批动作权限。
3. 部门经理在“待我审批”页面拥有审批动作权限。

## 角色工作台入口规则（2026-04-19）

本节只负责冻结桌面端 `角色工作台` 的入口显隐和权限承载关系；它不新增后端聚合接口，也不新增独立权限键。

### 入口承载

| 入口 | 路由 | 权限键 | HR 管理员 | 部门经理 | 员工 | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
| 角色工作台 | `/performance/workbench` | 无新增权限键 | 是 | 是 | 是 | 当前由菜单可见性控制入口，本页本身不新增按钮权限；进入后各卡片仍受下游页面既有权限控制 |

### 冻结规则

1. `角色工作台` 不新增 `performance:workbench:*` 权限键。
2. 工作台首页只做角色化分发，不放大任何下游页面权限。
3. 工作台里每张任务卡或快捷入口，必须继续复用目标页面既有权限判断。
4. 若当前账号无权访问下游页面，工作台不得展示对应入口卡片。
5. `角色工作台` 当前角色识别只用于首页占位内容切换，不作为后端鉴权依据。

## 页面按钮权限

### 评估单

| 按钮 | 权限键 | HR 管理员 | 部门经理 | 员工 |
| --- | --- | --- | --- | --- |
| 查看详情 | `performance:assessment:info` | 是 | 是 | 仅本人 |
| 新建 | `performance:assessment:add` | 是 | 是 | 否 |
| 编辑 | `performance:assessment:update` | 是 | 是 | 仅本人自评草稿 |
| 删除 | `performance:assessment:delete` | 是 | 是 | 否 |
| 提交 | `performance:assessment:submit` | 否 | 否 | 是 |
| 审批通过 | `performance:assessment:approve` | 否 | 是 | 否 |
| 审批驳回 | `performance:assessment:reject` | 否 | 是 | 否 |
| 导出 | `performance:assessment:export` | 是 | 否 | 否 |

补充说明：
`assessment` 当前已进入仓库级 API 契约迁移范围；权限键、接口路径与前端消费显隐必须与 `contracts/openapi/xuedao.openapi.json` 和生成类型同步收敛。

### 目标

| 按钮 | 权限键 | HR 管理员 | 部门经理 | 员工 |
| --- | --- | --- | --- | --- |
| 查看目标详情 | `performance:goal:info` | 是 | 是 | 仅本人 |
| 新建目标 | `performance:goal:add` | 是 | 是 | 否 |
| 编辑目标 | `performance:goal:update` | 是 | 是 | 仅本人 |
| 删除目标 | `performance:goal:delete` | 是 | 是 | 否 |
| 更新进度 | `performance:goal:progressUpdate` | 是 | 是 | 仅本人 |
| 运营台管理 | `performance:goal:opsManage` | 是 | 是 | 否 |
| 运营台全局范围 | `performance:goal:opsGlobalScope` | 是 | 否 | 否 |
| 导出目标 | `performance:goal:export` | 是 | 是 | 否 |

补充说明（目标运营台 ops 子接口沿用口径）：

1. `performance:goal:opsManage` 是目标运营台部门级管理专用权限，覆盖部门配置、公共计划维护、自动补零、日报生成与日报状态更新。
2. `performance:goal:opsGlobalScope` 是目标运营台 `HR` 全局范围专用权限；经理保留 `opsManage` 时仍然只能操作本人部门树范围。
3. 员工新增或维护个人补充目标，沿用 `performance:goal:update` 与 `performance:goal:progressUpdate`，但只能作用于本人 `sourceType = personal` 的计划项。
4. `performance:goal:add` 不再被视为部门运营台管理权限的代替信号，避免把“个人补充目标新建”误放大为部门管理权限。
5. 员工无权维护部门配置、公共计划和日报状态。
6. `goal` 当前已进入仓库级 API 契约迁移范围；主链与目标运营台子接口的权限键、接口路径与前端消费显隐必须与 `contracts/openapi/xuedao.openapi.json` 和生成类型同步收敛。

| 目标运营台子接口 | 权限键 | HR 管理员 | 部门经理 | 员工 | 说明 |
| --- | --- | --- | --- | --- | --- |
| 权限画像 | `performance:goal:opsAccessProfile` | 是 | 是 | 否 | 只读，用于前端裁剪当前部门与视角 |
| 部门配置详情 | `performance:goal:opsDepartmentConfig` | 是 | 是 | 否 | 只读，经理仅本人部门树 |
| 保存部门配置 | `performance:goal:opsDepartmentConfigSave` | 是 | 是 | 否 | 需要 `opsManage` 语义 |
| 计划分页 | `performance:goal:opsPlanPage` | 是 | 是 | 否 | 只读计划池分页 |
| 计划详情 | `performance:goal:opsPlanInfo` | 是 | 是 | 否 | 只读计划详情 |
| 保存计划 | `performance:goal:opsPlanSave` | 是 | 是 | 否 | 公共计划需 `opsManage`；个人补充目标可复用本人维护能力 |
| 删除计划 | `performance:goal:opsPlanDelete` | 是 | 是 | 否 | 公共计划需 `opsManage`；个人目标自删兼容本人维护能力 |
| 日结果填报 | `performance:goal:opsDailySubmit` | 是 | 是 | 否 | 员工在 Web 端不开放该子权限键，移动端仍走既有 `update/progressUpdate` 主链 |
| 总览 | `performance:goal:opsOverview` | 是 | 是 | 否 | 只读总览与贡献拆分 |
| 自动补零 | `performance:goal:opsDailyFinalize` | 是 | 是 | 否 | 需要部门级管理能力 |
| 日报详情 | `performance:goal:opsReportInfo` | 是 | 是 | 否 | 只读日报 |
| 生成日报 | `performance:goal:opsReportGenerate` | 是 | 是 | 否 | 需要部门级管理能力 |
| 更新日报状态 | `performance:goal:opsReportStatusUpdate` | 是 | 是 | 否 | 需要部门级管理能力 |

### 指标、环评、PIP、晋升、薪资

| 按钮 | 权限键 | HR 管理员 | 部门经理 | 员工 |
| --- | --- | --- | --- | --- |
| 查看指标详情 | `performance:indicator:info` | 是 | 否 | 否 |
| 新建指标 | `performance:indicator:add` | 是 | 否 | 否 |
| 编辑指标 | `performance:indicator:update` | 是 | 否 | 否 |
| 删除指标 | `performance:indicator:delete` | 是 | 否 | 否 |

补充约束：

1. `performance:indicator:*` 当前已进入仓库级 API 契约迁移范围；权限键、接口路径与前端消费显隐必须与 `contracts/openapi/xuedao.openapi.json` 和生成类型同步收敛。
| 查看环评详情 | `performance:feedback:info` | 是 | 是 | 仅任务相关人 |
| 发起环评 | `performance:feedback:add` | 是 | 是 | 否 |
| 提交环评反馈 | `performance:feedback:submit` | 是 | 是 | 是 |
| 查看环评汇总 | `performance:feedback:summary` | 是 | 是 | 是 |
| 查看 PIP 详情 | `performance:pip:info` | 是 | 是 | 否 |
| 新建 PIP | `performance:pip:add` | 是 | 是 | 否 |
| 编辑 PIP | `performance:pip:update` | 是 | 是 | 否 |
| 启动 PIP | `performance:pip:start` | 是 | 是 | 否 |
| 提交 PIP 跟进 | `performance:pip:track` | 是 | 是 | 否 |
| 完成 PIP | `performance:pip:complete` | 是 | 是 | 否 |
| 关闭 PIP | `performance:pip:close` | 是 | 是 | 否 |
| 查看晋升详情 | `performance:promotion:info` | 是 | 是 | 否 |
| 发起晋升 | `performance:promotion:add` | 是 | 是 | 否 |
| 编辑晋升 | `performance:promotion:update` | 是 | 是 | 否 |
| 提交晋升 | `performance:promotion:submit` | 是 | 是 | 否 |
| 晋升评审 | `performance:promotion:review` | 是 | 是 | 否 |
| 查看薪资 | `performance:salary:info` | 是 | 否 | 仅个人结果页二期考虑 |
| 新增薪资 | `performance:salary:add` | 是 | 否 | 否 |
| 修改薪资 | `performance:salary:update` | 是 | 否 | 否 |
| 确认薪资 | `performance:salary:confirm` | 是 | 否 | 否 |
| 归档薪资 | `performance:salary:archive` | 是 | 否 | 否 |
| 调整薪资 | `performance:salary:changeAdd` | 是 | 否 | 否 |

补充说明：

1. 首期仅开放 `assessment` 和 `goal` 导出权限。
2. 首期不开放 `feedback`、`pip`、`promotion`、`salary` 导出权限。

## 二期主题 2 导出增强冻结权限

以下内容仅用于二期开发前冻结，不代表首期当前已开放这些下载入口。

### 阶段 1 首批权限同步边界

1. 当前阶段 1 首批仅允许进入主实现同步的下载权限为：
   - `performance:feedback:export`
   - `performance:pip:export`
2. `performance:promotion:export` 和 `performance:salary:export` 仍属于冻结权限，不得回写为“已开放”。
3. 在仓库出现主实现和验证证据前，本节只能作为权限冻结口径，不能写成“权限已交付完成”。

| 导出模块 | 权限键 | HR 管理员 | 部门经理 | 员工 | 数据范围 |
| --- | --- | --- | --- | --- | --- |
| 360 环评汇总导出 | `performance:feedback:export` | 是 | 是 | 否 | `HR` 全部；经理仅所辖部门树内摘要 |
| PIP 摘要导出 | `performance:pip:export` | 是 | 是 | 否 | `HR` 全部；经理仅所辖部门树内摘要 |
| 晋升摘要导出 | `performance:promotion:export` | 是 | 是 | 否 | `HR` 全部；经理仅所辖部门树内摘要 |
| 薪资摘要导出 | `performance:salary:export` | 是 | 否 | 否 | `HR` 全部；不向经理和员工开放 |

补充规则：

1. 导出权限是独立下载权限，不能因为已有 `page` 或 `info` 权限就默认继承。
2. 前端必须同时控制菜单 / 按钮显隐，后端必须再次校验下载权限和数据范围。
3. 即使拥有导出权限，也只能导出 [12-数据权限与脱敏规则.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/12-数据权限与脱敏规则.md) 明确允许的摘要字段。

## 接口权限

### HR 管理员

- 拥有绩效模块全部查看类接口权限。
- 拥有薪资和指标配置接口权限。
- 拥有评估单新增、编辑、删除和归档管理权限。
- 首期不拥有评估审批动作权限。
- 首期独占薪资管理入口和金额维护权限。

### 部门经理

- 可访问：
  - 团队评估分页、详情、审批
  - 团队目标分页、更新进度
  - PIP 启动、跟进、完成、关闭
  - 晋升提交、评审
  - 环评相关接口
- 不可访问：
  - 指标库配置
  - 全量薪资分页
  - 薪资详情和薪资调整接口

### 员工

- 可访问：
  - 我的考核列表和详情
  - 本人自评提交
  - 本人目标查看和进度更新
  - 指派给自己的环评反馈提交
- 不可访问：
  - 他人评估单
  - 审批接口
  - 薪资管理后台
- 不可创建评估单
- 不可访问任何薪资管理接口

## 数据范围权限

| 角色 | 评估单 | 目标 | PIP | 晋升 | 薪资 |
| --- | --- | --- | --- | --- | --- |
| HR 管理员 | 全部 | 全部 | 全部 | 全部 | 全部 |
| 部门经理 | 直接管理部门及其下级部门 | 直接管理部门及其下级部门 | 直接管理部门及其下级部门 | 直接管理部门及其下级部门 | 无 |
| 员工 | 本人 | 本人 | 本人只读 | 本人只读 | 首期后台无入口 |

## 二期主题 6：跨模块驾驶舱权限正式冻结（2026-04-18）

本节负责冻结主题 6 的正式查看权限和拒绝语义；它不开放任何明细、动作按钮或源域后台跳转。

### 正式权限键

| 能力 | 权限键 | 说明 |
| --- | --- | --- |
| 跨模块驾驶舱汇总查看 | `performance:dashboard:crossSummary` | 仅用于主题 6 的只读聚合接口与页面 |

### 角色权限矩阵

| 能力 | `HR` | 部门经理 | 员工 | 说明 |
| --- | --- | --- | --- | --- |
| 查看跨模块驾驶舱入口 | 是 | 是 | 否 | 员工无入口 |
| 请求 `/admin/performance/dashboard/crossSummary` | 是 | 是 | 否 | 经理仅限本人部门树范围 |
| 查看全局汇总 | 是 | 否 | 否 | 经理不得越权看全局 |
| 查看部门树范围汇总 | 是 | 是 | 否 | 经理仅在来源支持部门树映射时可见真实值 |
| 查看招聘 / 培训 / 会议原始明细 | 否 | 否 | 否 | 全角色禁止下钻 |

### 权限正文

1. `员工` 不可见跨模块驾驶舱入口、页面和接口。
2. `HR` 拥有主题 6 三类指标族的全量聚合查看权限，但仍只可见聚合值、状态提示和更新时间。
3. `部门经理` 仅可在本人部门树范围内查看聚合结果，不得请求或缓存跨部门结果。
4. 若来源域当前不支持部门树映射、组织归属或部门裁剪，`部门经理` 不得回退查看全局值；对应卡片必须显示 `unavailable`。
5. 不允许因为已有 `performance:dashboard:summary` 权限，就自动继承主题 6 的跨模块驾驶舱查看权限。
6. 任何角色都不得通过主题 6 接口下钻查看招聘、培训、会议域原始明细。

## 二期主题 3：`cool-uni` 登录与权限设计（2026-04-20）

本节只负责固定 `cool-uni` 当前已实现移动主链和下一优先级候选能力的登录态复用、页面显隐、动作白名单和拒绝策略；不新增移动端专用权限键。未进入“当前已实现页面显隐矩阵”的页面，不得按已开放处理。

### 登录态复用方案

1. 移动端登录必须复用现有后台验证码登录链路：
   - `/admin/base/open/captcha`
   - `/admin/base/open/login`
   - `/admin/base/open/refreshToken`
   - `/admin/base/comm/permmenu`
   - `/admin/base/comm/logout`
2. 不新增移动端专用账号体系、手机号快捷登录、短信登录或独立 token 协议。
3. 移动端页面可见性和动作权限继续以现有 `permmenu` 和权限键为准，不新增 `mobile:*` 或 `uni:*` 权限键。
4. 移动端工作台入口卡片必须按“两层裁剪”生成：
   - 第一层：只保留当前已实现移动页面白名单
   - 第二层：再与当前登录用户的 `permmenu` 权限交集裁剪
5. 即使当前账号在 PC 端还拥有其他权限，如“已发起考核”“指标库”“PIP”“晋升”“薪资”，当前移动端也不得显示这些页面入口。

### 当前已实现页面显隐矩阵

| 页面 | 复用权限 / 前提 | 员工 | 部门经理 | 显示规则 |
| --- | --- | --- | --- | --- |
| 登录 | 无，需验证码链路可用 | 是 | 是 | 未登录或登录态失效时唯一入口 |
| 工作台 | token 有效且 `permmenu` 拉取成功 | 是 | 是 | 只展示当前白名单内且当前账号有权访问的卡片 |
| 我的考核 | `performance:assessment:myPage` | 是 | 否 | 仅员工当前页面；经理即使 PC 端具备 `myPage`，当前移动端也不显示 |
| 待我审批 | `performance:assessment:pendingPage` | 否 | 是 | 仅经理当前页面 |
| 目标 | `performance:goal:page` | 是 | 是 | 员工看本人；经理看部门树范围 |
| 360 环评 | `performance:feedback:page` | 是 | 是 | 员工看本人相关任务和汇总；经理看部门树范围 |

### 当前已实现动作白名单

当前移动端只允许开放下表动作；即使 PC 端有更高权限，未列入本表的动作也不得在移动端显示。

| 页面 | 动作 | 复用权限键 | 员工 | 部门经理 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 我的考核 | 查看详情 | `performance:assessment:info` | 是 | 否 | 仅本人 |
| 我的考核 | 保存草稿 | `performance:assessment:update` | 是 | 否 | 仅本人草稿 |
| 我的考核 | 提交自评 | `performance:assessment:submit` | 是 | 否 | 仅本人草稿 |
| 待我审批 | 查看详情 | `performance:assessment:info` | 否 | 是 | 仅待审批且在部门树范围内 |
| 待我审批 | 审批通过 | `performance:assessment:approve` | 否 | 是 | 仅 `submitted` 且在部门树范围内 |
| 待我审批 | 审批驳回 | `performance:assessment:reject` | 否 | 是 | 仅 `submitted` 且在部门树范围内 |
| 目标 | 查看详情 | `performance:goal:info` | 是 | 是 | 员工仅本人；经理仅部门树范围 |
| 目标 | 编辑 | `performance:goal:update` | 是 | 是 | 当前不开放 `add / delete` |
| 目标 | 更新进度 | `performance:goal:progressUpdate` | 是 | 是 | 员工仅本人；经理仅部门树范围 |
| 360 环评 | 查看任务详情 | `performance:feedback:info` | 是 | 是 | 员工仅本人相关任务；经理仅部门树范围 |
| 360 环评 | 提交反馈 | `performance:feedback:submit` | 是 | 是 | 仅本人被分配任务且未提交时可用 |
| 360 环评 | 查看汇总 | `performance:feedback:summary` | 是 | 是 | 员工只看本人被评价汇总；经理看部门树汇总 |

### 下一优先级候选页面权限对齐规则

| 候选能力 | 复用权限键 | 当前结论 | 当前移动端规则 |
| --- | --- | --- | --- |
| 培训学习闭环 `course-learning` | `performance:courseRecite:page|info|submit`、`performance:coursePractice:page|info|submit`、`performance:courseExam:summary` | 下一优先级候选 | 仅员工个人学习子链可进入候选，经理与 `HR` 不因后台课程权限自动获得移动入口 |
| 工作计划个人子集 `work-plan` | 复用 `performance:workPlan:*`，共享事实源已在本文件补齐 | 下一优先级候选 | 移动端若开放，只允许继承当前桌面端已冻结的 `page / info / start / complete` 个人动作边界 |
| 班主任待办轻量子集 `teacher-todo` | `performance:teacherTodo:page` 及必要的 `teacherInfo / teacherFollow` 下游键 | 下一优先级候选 | 只允许按主题19既有角色边界做 `today / overdue` 轻量待办，不得扩成班主任后台全量入口 |
| 会议个人子集 / 知识库阅读 / 文件管理阅读 | 继续沿用各主题桌面端权限口径 | 后续观察 | 当前无移动入口、无按钮、无卡片；正式开放前必须先回写本文件 |

### 当前明确不开放的移动端动作

1. 评估单发起、删除、导出
2. 目标新增、删除、导出
3. 环评任务发起、导出
4. `course-learning / work-plan / teacher-todo` 候选能力在正式进入实现前的任意预开按钮
5. 指标库、驾驶舱、PIP、晋升、薪资相关全部动作
6. 任何后台配置动作

### 无权限与越权拒绝策略

1. 页面级：
   - 若未登录或 token 失效，进入登录页，不得展示业务页旧缓存。
   - 若页面不在当前移动白名单，或当前账号缺少对应权限键，则工作台不显示入口。
   - 若用户通过深链或本地路由强行进入无权限页面，显示“无权限访问该页面”空态，不放大权限、不切换为其他业务页。
2. 动作级：
   - 若动作不在当前动作白名单，即使 PC 端有对应权限，移动端也不得展示按钮。
   - 若按钮已展示但后端返回 `403` 或等效拒绝结果，前端只提示“无权限执行该操作”，不做乐观更新。
3. 数据级：
   - 员工越权查询一律按“本人口径”重新裁剪，不因前端携带 `employeeId`、`departmentId` 放宽。
   - 经理越权查询一律按“部门树口径”重新裁剪，不因前端携带跨部门 `employeeId` 放宽。
4. 列表为空与无权限要分开：
   - 合法范围内无数据：显示空数据态
   - 页面或动作无权限：显示无权限态
5. 不允许通过“前端隐藏、后端放行”的方式交付移动端权限；后端仍是最终鉴权裁判。
6. 路线图候选能力若尚未进入“当前已实现页面显隐矩阵”，即使后端已有接口和权限键，也视为未开放。

## 落地规则

1. 前端用 `v-permission` 和 `_permission` 控制按钮。
2. 后端必须再次校验角色和数据范围。
3. 菜单可见不等于数据可见，列表查询必须附带服务端数据范围条件。
4. 部门经理的数据范围统一按组织架构部门树计算，不单独支持跨部门兼任授权。

## 工作计划主链权限基线（2026-04-21）

### 当前冻结结论

- 当前唯一结论：`workPlan` 已进入仓库级 API 契约和菜单权限统一治理，桌面端主链按既有 seed 角色范围冻结。

### 权限键清单

| 能力 | 权限键 |
| --- | --- |
| 工作计划列表 | `performance:workPlan:page` |
| 工作计划详情 | `performance:workPlan:info` |
| 新建工作计划 | `performance:workPlan:add` |
| 编辑工作计划 | `performance:workPlan:update` |
| 删除工作计划 | `performance:workPlan:delete` |
| 开始执行工作计划 | `performance:workPlan:start` |
| 完成工作计划 | `performance:workPlan:complete` |
| 取消工作计划 | `performance:workPlan:cancel` |
| 同步钉钉审批来源 | `performance:workPlan:sync` |

### 角色权限矩阵

| 权限键 | HR 管理员 | 部门经理 | 员工 | 说明 |
| --- | --- | --- | --- | --- |
| `performance:workPlan:page` | 是 | 是 | 是 | 员工仅本人 / 协同人范围；经理按部门树范围；`HR` 全量 |
| `performance:workPlan:info` | 是 | 是 | 是 | 同上 |
| `performance:workPlan:add` | 是 | 是 | 否 | 首期只开放管理侧手工建单 |
| `performance:workPlan:update` | 是 | 是 | 否 | 员工不开放编辑既有计划 |
| `performance:workPlan:delete` | 是 | 是 | 否 | 仅草稿或可删除状态允许执行 |
| `performance:workPlan:start` | 是 | 是 | 是 | 员工只可启动本人 / 协同且来源审批已通过的计划 |
| `performance:workPlan:complete` | 是 | 是 | 是 | 员工只可完成本人 / 协同中的计划 |
| `performance:workPlan:cancel` | 是 | 是 | 否 | 取消属于管理动作，不向员工开放 |
| `performance:workPlan:sync` | 是 | 是 | 否 | 仅管理侧或系统回调链路可触发 |

### 权限规则

1. `workPlan` 的唯一契约资源名固定为 `workPlan`，菜单、OpenAPI、前端 service 和权限文档不得再拆出第二套命名。
2. 钉钉来源计划即使拥有 `performance:workPlan:start`，在 `sourceStatus != approved` 时也必须被后端拒绝。
3. `performance:workPlan:sync` 是来源同步权限，不等于编辑权限；同步回调仍需额外校验签名或专用 token。
4. 移动端若开放 `work-plan` 子集，必须继续复用本节权限键，不得另起 `mobile` 或 `workbench` 专属动作键。

## 二期主题 4：自动建议引擎权限冻结基线（2026-04-17）

本节记录自动建议引擎进入开发前已冻结的权限键和角色边界。若后续前端落为独立页面、工作台卡片或嵌入式入口，统一复用同一组权限键。

### 当前冻结结论

- 当前唯一结论：`自动建议引擎权限已冻结，可进入开发`

### 权限键定义

| 能力 | 权限键 |
| --- | --- |
| 建议分页 | `performance:suggestion:page` |
| 建议详情 | `performance:suggestion:info` |
| 采用建议 | `performance:suggestion:accept` |
| 忽略建议 | `performance:suggestion:ignore` |
| 驳回建议 | `performance:suggestion:reject` |
| 撤销建议 | `performance:suggestion:revoke` |

### 角色权限矩阵

| 角色 | 建议列表 / 详情 | 采用建议 | 忽略建议 | 驳回建议 | 撤销建议 |
| --- | --- | --- | --- | --- | --- |
| HR 管理员 | 是，全量范围 | 是 | 是 | 是 | 是 |
| 部门经理 | 是，仅部门树范围 | 是，仅本范围且具备对应创建权限 | 是，仅部门树范围 | 是，仅部门树范围 | 否 |
| 员工 | 否 | 否 | 否 | 否 | 否 |

### 权限规则

1. 自动建议权限不复用现有 `performance:pip:*` 或 `performance:promotion:*` 权限键，统一使用 `performance:suggestion:*`。
2. `HR` 拥有建议查看和全部处理权限，但这不等于放宽 `PIP / 晋升`、薪资或其他模块的既有权限边界。
3. 部门经理只允许处理自己部门树范围内的建议；超范围查询和动作必须被后端拒绝。
4. 部门经理执行 `accept` 前，除具备 `performance:suggestion:accept` 外，还必须具备对应正式单据的创建权限：
   - `pip` 建议要求 `performance:pip:add`
   - `promotion` 建议要求 `performance:promotion:add`
5. 员工既没有建议入口，也没有建议接口和按钮权限；前端隐藏不构成放行依据，后端必须继续拒绝。
6. 若后续实现采用独立页面或菜单入口，统一以 `performance:suggestion:page` 作为入口权限键，不另起新键名。

## 二期主题 5：自动审批流权限冻结基线（2026-04-17）

本节记录自动审批流进入开发前已冻结的权限键和角色边界。自动审批流不新增独立菜单页权限，统一通过对象详情、待办入口和动作权限进入。

### 当前冻结结论

- 当前唯一结论：`自动审批流权限已冻结，可进入开发`

### 权限键定义

| 能力 | 权限键 |
| --- | --- |
| 配置详情 | `performance:approvalFlow:configInfo` |
| 配置保存 | `performance:approvalFlow:configSave` |
| 实例详情 | `performance:approvalFlow:info` |
| 审批通过 | `performance:approvalFlow:approve` |
| 审批驳回 | `performance:approvalFlow:reject` |
| 当前节点转办 | `performance:approvalFlow:transfer` |
| 发起人撤回 | `performance:approvalFlow:withdraw` |
| 催办 | `performance:approvalFlow:remind` |
| `HR` 人工指定 / 恢复 | `performance:approvalFlow:resolve` |
| 回退到手工审批主链 | `performance:approvalFlow:fallback` |
| `HR` 强制终止 | `performance:approvalFlow:terminate` |

### 角色权限矩阵

| 权限键 | HR 管理员 | 部门经理 | 员工 | 说明 |
| --- | --- | --- | --- | --- |
| `performance:approvalFlow:configInfo` | 是 | 否 | 否 | 仅 `HR` 可查看配置 |
| `performance:approvalFlow:configSave` | 是 | 否 | 否 | 仅 `HR` 可保存配置 |
| `performance:approvalFlow:info` | 是 | 是 | 是 | `HR` 全量；经理仅部门树范围 / 本人参与；员工仅本人发起实例 |
| `performance:approvalFlow:approve` | 是 | 是 | 否 | 还必须命中“当前节点当前审批人” |
| `performance:approvalFlow:reject` | 是 | 是 | 否 | 还必须命中“当前节点当前审批人” |
| `performance:approvalFlow:transfer` | 是 | 是 | 否 | 普通转办仅当前审批人；`HR` 可强制转办 |
| `performance:approvalFlow:withdraw` | 是 | 是 | 是 | 还必须命中“当前实例发起人” |
| `performance:approvalFlow:remind` | 是 | 是 | 是 | 还必须命中“当前实例发起人”或 `HR` |
| `performance:approvalFlow:resolve` | 是 | 否 | 否 | 仅 `HR` 处理 `pending_resolution / manual_pending` |
| `performance:approvalFlow:fallback` | 是 | 否 | 否 | 仅 `HR` 可回退到手工审批主链 |
| `performance:approvalFlow:terminate` | 是 | 否 | 否 | 仅 `HR` 可强制终止 |

### 仓库级 SSOT 别名回写

OpenAPI 主源、菜单生成物和前端生成权限常量已经收敛到 kebab-case 资源名，因此以下键名在仓库级 SSOT 中同样成立，并与上表 camelCase 资源别名保持一一映射：

| 能力 | 仓库级权限键 |
| --- | --- |
| 配置详情 | `performance:approval-flow:configInfo` |
| 配置保存 | `performance:approval-flow:configSave` |
| 实例详情 | `performance:approval-flow:info` |
| 审批通过 | `performance:approval-flow:approve` |
| 审批驳回 | `performance:approval-flow:reject` |
| 当前节点转办 | `performance:approval-flow:transfer` |
| 发起人撤回 | `performance:approval-flow:withdraw` |
| 催办 | `performance:approval-flow:remind` |
| `HR` 人工指定 / 恢复 | `performance:approval-flow:resolve` |
| 回退到手工审批主链 | `performance:approval-flow:fallback` |
| `HR` 强制终止 | `performance:approval-flow:terminate` |

### 权限规则

1. 自动审批流配置权限不继承现有 `assessment / promotion` 的 `page / info` 查看权限，必须使用独立配置权限键。
2. `approve / reject / transfer` 同时要求：
   - 调用方拥有对应动作权限键
   - 调用方是当前节点当前审批人
   - 实例状态为 `in_review`
3. `withdraw / remind` 同时要求：
   - 调用方拥有对应动作权限键
   - 调用方是当前实例发起人，或调用方是 `HR`
4. `resolve / fallback / terminate` 只允许 `HR`；部门经理和员工即使能看到实例，也不能执行这三类人工介入动作。
5. 员工没有配置、审批、驳回、转办权限，但可以在本人发起且规则允许的实例上执行 `withdraw / remind`。
6. 自动审批流权限键不替代源业务对象的 `submit`、`approve`、`reject`、`review` 权限；源业务对象的提交仍按既有权限校验，自动审批流只接管“提交后的审批层”。
7. 若自动审批流实例已回退到手工审批主链，后续动作重新回到 `performance:assessment:approve`、`performance:assessment:reject`、`performance:promotion:review` 的既有权限链路。

## 二期主题 7：培训课程管理权限冻结基线（2026-04-18）

### 当前冻结结论

- 当前唯一结论：`培训课程管理权限已冻结，且主题7已完成阶段0冻结`

### 权限键定义

| 能力 | 权限键 |
| --- | --- |
| 课程列表 | `performance:course:page` |
| 课程详情 | `performance:course:info` |
| 新增课程 | `performance:course:add` |
| 编辑课程 | `performance:course:update` |
| 删除课程 | `performance:course:delete` |
| 课程报名列表 | `performance:course:enrollmentPage` |

### 角色权限矩阵

| 权限键 | HR 管理员 | 部门经理 | 员工 | 说明 |
| --- | --- | --- | --- | --- |
| `performance:course:page` | 是 | 是 | 否 | 经理只读 |
| `performance:course:info` | 是 | 是 | 否 | 经理只读 |
| `performance:course:add` | 是 | 否 | 否 | 仅 `HR` |
| `performance:course:update` | 是 | 否 | 否 | 仅 `HR` |
| `performance:course:delete` | 是 | 否 | 否 | 仅 `HR` |
| `performance:course:enrollmentPage` | 是 | 否 | 否 | 仅 `HR` 查看报名摘要 |

### 权限规则

1. 首批课程管理固定为后台管理能力，不向员工开放。
2. 部门经理首批只有课程列表 / 详情只读权限，不拥有课程维护和报名列表查看权限。
3. 课程报名列表不等于员工报名入口；首批不新增 `performance:course:enroll` 权限键。
4. `published` 状态下的编辑权限仍只属于 `HR`，且只允许修改已冻结的非关键字段。

## 二期主题 8：招聘面试管理权限冻结基线（2026-04-18）

### 当前冻结结论

- 当前唯一结论：`招聘面试管理权限已冻结，且主题8已完成阶段0冻结`
- `interview` 已接入仓库级 OpenAPI 主源；接口路径、请求/响应结构与来源快照字段以 `contracts/openapi/xuedao.openapi.json` 为准，权限判定仍以本节为准。

### 权限键定义

| 能力 | 权限键 |
| --- | --- |
| 面试列表 | `performance:interview:page` |
| 面试详情 | `performance:interview:info` |
| 新增面试 | `performance:interview:add` |
| 编辑面试 | `performance:interview:update` |
| 删除面试 | `performance:interview:delete` |

### 角色权限矩阵

| 权限键 | HR 管理员 | 部门经理 | 员工 | 说明 |
| --- | --- | --- | --- | --- |
| `performance:interview:page` | 是 | 是 | 否 | 经理仅部门树范围 |
| `performance:interview:info` | 是 | 是 | 否 | 经理仅部门树范围 |
| `performance:interview:add` | 是 | 是 | 否 | 经理仅部门树范围 |
| `performance:interview:update` | 是 | 是 | 否 | 经理仅部门树范围 |
| `performance:interview:delete` | 是 | 否 | 否 | 删除只保留 `HR` |

### 权限规则

1. 员工首批无面试管理入口和动作权限。
2. 部门经理只能操作本人部门树范围内的面试记录，范围按 `departmentId` 判定。
3. 首批不新增招聘计划、简历池、录用管理相关权限键。

## 二期主题 9：会议管理权限冻结基线（2026-04-18）

### 当前冻结结论

- 当前唯一结论：`会议管理权限已冻结，且主题9已完成阶段0冻结`

### 权限键定义

| 能力 | 权限键 |
| --- | --- |
| 会议列表 | `performance:meeting:page` |
| 会议详情 | `performance:meeting:info` |
| 新增会议 | `performance:meeting:add` |
| 编辑会议 | `performance:meeting:update` |
| 删除会议 | `performance:meeting:delete` |
| 会议签到 | `performance:meeting:checkIn` |

### 角色权限矩阵

| 权限键 | HR 管理员 | 部门经理 | 员工 | 说明 |
| --- | --- | --- | --- | --- |
| `performance:meeting:page` | 是 | 是 | 否 | 经理仅部门树范围 |
| `performance:meeting:info` | 是 | 是 | 否 | 经理仅部门树范围 |
| `performance:meeting:add` | 是 | 是 | 否 | 经理仅部门树范围 |
| `performance:meeting:update` | 是 | 是 | 否 | 经理仅部门树范围 |
| `performance:meeting:delete` | 是 | 是 | 否 | 删除只允许 `scheduled` |
| `performance:meeting:checkIn` | 是 | 是 | 否 | 只允许 `in_progress` 状态 |

补充约束：

1. `performance:meeting:*` 当前已进入仓库级 API 契约迁移范围；权限键、接口路径与前端消费显隐必须与 `contracts/openapi/xuedao.openapi.json` 和生成类型同步收敛。

### 权限规则

1. 员工首批无会议管理入口和动作权限。
2. 部门经理只能操作本人部门树范围内的会议记录，且 `organizerId / participantIds` 也必须落在本人部门树范围内。
3. 首批不新增会议驾驶舱或外部会议系统接入权限键。

## 二期主题 10：合同管理权限冻结基线（2026-04-18）

### 当前冻结结论

- 当前唯一结论：`合同管理权限已冻结`
- API 契约载体已开始迁移到仓库级 OpenAPI；合同资源的接口路径与动作名以 `contracts/openapi/xuedao.openapi.json` 为准，权限判定仍以本节为准。

### 权限键定义

| 能力 | 权限键 |
| --- | --- |
| 合同列表 | `performance:contract:page` |
| 合同详情 | `performance:contract:info` |
| 新增合同 | `performance:contract:add` |
| 编辑合同 | `performance:contract:update` |
| 删除合同 | `performance:contract:delete` |

### 角色权限矩阵

| 权限键 | HR 管理员 | 部门经理 | 员工 | 说明 |
| --- | --- | --- | --- | --- |
| `performance:contract:page` | 是 | 否 | 否 | 首批只保留 `HR` 合同台账入口 |
| `performance:contract:info` | 是 | 否 | 否 | 详情包含敏感字段，首批不开放给非 `HR` |
| `performance:contract:add` | 是 | 否 | 否 | 首批只允许 `HR` 建档 |
| `performance:contract:update` | 是 | 否 | 否 | 首批只允许 `HR` 编辑 |
| `performance:contract:delete` | 是 | 否 | 否 | 删除只允许 `draft` |

### 权限规则

1. 员工首批无合同管理入口和动作权限，不开放员工侧合同签收或查看入口。
2. 部门经理首批无合同管理入口和动作权限，避免在未冻结脱敏和审批边界前扩散敏感信息。
3. 首批不新增电子签、签名板、审批流、归档下载相关权限键。
4. 首批不与主题 5 自动审批流耦合，后续若需要审批链路，必须重新打开阶段 0 冻结。

## 二期主题 11：采购与供应商管理权限冻结基线（2026-04-19）

### 当前冻结结论

- 当前唯一结论：`采购与供应商管理扩容权限已冻结`

### 权限键定义

| 能力 | 权限键 |
| --- | --- |
| 采购订单列表 | `performance:purchaseOrder:page` |
| 采购订单详情 | `performance:purchaseOrder:info` |
| 新增采购订单 | `performance:purchaseOrder:add` |
| 编辑采购订单 | `performance:purchaseOrder:update` |
| 删除采购订单 | `performance:purchaseOrder:delete` |
| 提交询价 | `performance:purchaseOrder:submitInquiry` |
| 提交审批 | `performance:purchaseOrder:submitApproval` |
| 审批通过 | `performance:purchaseOrder:approve` |
| 审批驳回 | `performance:purchaseOrder:reject` |
| 收货 | `performance:purchaseOrder:receive` |
| 关闭采购单 | `performance:purchaseOrder:close` |
| 供应商列表 | `performance:supplier:page` |
| 供应商详情 | `performance:supplier:info` |
| 新增供应商 | `performance:supplier:add` |
| 编辑供应商 | `performance:supplier:update` |
| 删除供应商 | `performance:supplier:delete` |
| 采购报表汇总 | `performance:purchaseReport:summary` |
| 采购报表趋势 | `performance:purchaseReport:trend` |
| 供应商统计 | `performance:purchaseReport:supplierStats` |

### 角色权限矩阵

| 权限键 | HR 管理员 | 部门经理 | 员工 | 说明 |
| --- | --- | --- | --- | --- |
| `performance:purchaseOrder:page` | 是 | 是 | 否 | 经理仅部门树范围 |
| `performance:purchaseOrder:info` | 是 | 是 | 否 | 经理仅部门树范围 |
| `performance:purchaseOrder:add` | 是 | 是 | 否 | 经理仅可创建本人部门树范围订单 |
| `performance:purchaseOrder:update` | 是 | 是 | 否 | 经理仅可维护本人部门树范围订单 |
| `performance:purchaseOrder:delete` | 是 | 否 | 否 | 删除只保留 `HR` |
| `performance:purchaseOrder:submitInquiry` | 是 | 是 | 否 | 经理仅部门树范围 |
| `performance:purchaseOrder:submitApproval` | 是 | 是 | 否 | 经理仅部门树范围 |
| `performance:purchaseOrder:approve` | 是 | 是 | 否 | 经理仅部门树范围 |
| `performance:purchaseOrder:reject` | 是 | 是 | 否 | 经理仅部门树范围 |
| `performance:purchaseOrder:receive` | 是 | 是 | 否 | 经理仅部门树范围 |
| `performance:purchaseOrder:close` | 是 | 是 | 否 | 经理仅部门树范围，需关闭原因 |
| `performance:supplier:page` | 是 | 是 | 否 | 经理只读，且字段脱敏 |
| `performance:supplier:info` | 是 | 是 | 否 | 经理只读，且字段脱敏 |
| `performance:supplier:add` | 是 | 否 | 否 | 仅 `HR` |
| `performance:supplier:update` | 是 | 否 | 否 | 仅 `HR` |
| `performance:supplier:delete` | 是 | 否 | 否 | 仅 `HR` |
| `performance:purchaseReport:summary` | 是 | 是 | 否 | 经理仅部门树范围统计 |
| `performance:purchaseReport:trend` | 是 | 是 | 否 | 经理仅部门树范围统计 |
| `performance:purchaseReport:supplierStats` | 是 | 是 | 否 | 经理仅部门树范围统计 |

### 权限规则

1. 员工首批对采购订单和供应商都完全无入口、无页面权限、无动作权限。
2. 部门经理只允许在本人部门树范围内查看和维护采购订单；范围按 `departmentId` 判定。
3. 部门经理对供应商只保留只读查询权限，用于选择和查看可用供应商摘要，不拥有供应商主数据维护权。
4. 当前已新增询价、审批、收货和报表权限键，但仍不新增付款、对账和库存总账联动权限键。
5. 供应商敏感字段的最终展示仍以 [12-数据权限与脱敏规则.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/12-数据权限与脱敏规则.md) 的角色裁剪结果为准。

## 二期主题 14：培训学习与考试增强权限冻结基线（2026-04-18）

### 当前冻结结论

- 当前唯一结论：`培训学习与考试增强已完成阶段0冻结，可进入开发前评审`

### 权限键定义

| 能力 | 权限键 |
| --- | --- |
| 背诵任务列表 | `performance:courseRecite:page` |
| 背诵任务详情 | `performance:courseRecite:info` |
| 提交背诵任务 | `performance:courseRecite:submit` |
| 练习任务列表 | `performance:coursePractice:page` |
| 练习任务详情 | `performance:coursePractice:info` |
| 提交练习任务 | `performance:coursePractice:submit` |
| 考试 / 结果摘要 | `performance:courseExam:summary` |

### 角色权限矩阵

| 权限键 | HR 管理员 | 部门经理 | 员工 | 说明 |
| --- | --- | --- | --- | --- |
| `performance:courseRecite:page` | 否 | 否 | 是 | 仅当前登录员工本人任务 |
| `performance:courseRecite:info` | 否 | 否 | 是 | 仅当前登录员工本人任务 |
| `performance:courseRecite:submit` | 否 | 否 | 是 | 仅当前登录员工本人任务 |
| `performance:coursePractice:page` | 否 | 否 | 是 | 仅当前登录员工本人任务 |
| `performance:coursePractice:info` | 否 | 否 | 是 | 仅当前登录员工本人任务 |
| `performance:coursePractice:submit` | 否 | 否 | 是 | 仅当前登录员工本人任务 |
| `performance:courseExam:summary` | 否 | 否 | 是 | 仅当前登录员工本人课程结果摘要 |

### 权限规则

1. 主题14 固定为员工自助学习能力；不新增 `HR` 或部门经理的背诵 / 练习 / 考试写入动作权限。
2. `HR` 与部门经理若需要查看课程报名与分数摘要，继续沿用主题7课程管理侧已冻结能力，不通过主题14新增权限绕行。
3. 员工拥有主题14权限，不代表员工获得主题7课程管理页面权限；员工仍无课程 CRUD 和课程报名列表后台入口。
4. 首批不新增模型管理、Prompt 配置、题库配置和结果人工改分相关权限键。

## 二期主题 12：招聘人才资产增强权限冻结基线（2026-04-18）

### 当前冻结结论

- 当前唯一结论：`招聘人才资产增强权限已冻结，且主题12已完成阶段0冻结`
- `talentAsset` 已接入仓库级 OpenAPI 主源；接口路径与动作名以 `contracts/openapi/xuedao.openapi.json` 为准，权限判定仍以本节为准。

### 权限键定义

| 能力 | 权限键 |
| --- | --- |
| 人才资产列表 | `performance:talentAsset:page` |
| 人才资产详情 | `performance:talentAsset:info` |
| 新增人才资产 | `performance:talentAsset:add` |
| 编辑人才资产 | `performance:talentAsset:update` |
| 删除人才资产 | `performance:talentAsset:delete` |

### 角色权限矩阵

| 权限键 | HR 管理员 | 部门经理 | 员工 | 说明 |
| --- | --- | --- | --- | --- |
| `performance:talentAsset:page` | 是 | 是 | 否 | 经理仅部门树范围 |
| `performance:talentAsset:info` | 是 | 是 | 否 | 经理仅部门树范围 |
| `performance:talentAsset:add` | 是 | 是 | 否 | 经理仅可创建本人部门树范围人才资产 |
| `performance:talentAsset:update` | 是 | 是 | 否 | 经理仅可维护本人部门树范围人才资产 |
| `performance:talentAsset:delete` | 是 | 否 | 否 | 删除只保留 `HR`，且仅 `new` 状态 |

### 权限规则

1. 员工首批对人才资产完全无入口、无页面权限、无动作权限。
2. 部门经理只能操作 `targetDepartmentId` 落在本人部门树范围内的人才资产记录。
3. “人才库”不新增第二套权限键，统一复用 `talentAsset` 权限模型。
4. 首批不新增简历下载、联系方式导出、面试自动转化等扩展权限键。

## 二期主题 13：人才发展与认证增强权限冻结基线（2026-04-18）

### 当前冻结结论

- 当前唯一结论：`人才发展与认证增强权限已冻结，且主题13已完成阶段0冻结`
- 主题13资源的 API 契约载体已开始迁移到仓库级 OpenAPI；能力地图与证书台账的接口动作仍以本节权限矩阵为准。

### 权限键定义

| 能力 | 权限键 |
| --- | --- |
| 能力模型列表 | `performance:capabilityModel:page` |
| 能力模型详情 | `performance:capabilityModel:info` |
| 新增能力模型 | `performance:capabilityModel:add` |
| 编辑能力模型 | `performance:capabilityModel:update` |
| 能力项详情 | `performance:capabilityItem:info` |
| 能力画像摘要 | `performance:capabilityPortrait:info` |
| 证书列表 | `performance:certificate:page` |
| 证书详情 | `performance:certificate:info` |
| 新增证书 | `performance:certificate:add` |
| 编辑证书 | `performance:certificate:update` |
| 发放证书 | `performance:certificate:issue` |
| 证书记录列表 | `performance:certificate:recordPage` |

### 角色权限矩阵

| 权限键 | HR 管理员 | 部门经理 | 员工 | 说明 |
| --- | --- | --- | --- | --- |
| `performance:capabilityModel:page` | 是 | 是 | 否 | 模型定义为全局只读基线 |
| `performance:capabilityModel:info` | 是 | 是 | 否 | 模型定义为全局只读基线 |
| `performance:capabilityModel:add` | 是 | 否 | 否 | 仅 `HR` |
| `performance:capabilityModel:update` | 是 | 否 | 否 | 仅 `HR` |
| `performance:capabilityItem:info` | 是 | 是 | 否 | 经理只读 |
| `performance:capabilityPortrait:info` | 是 | 是 | 否 | 经理仅部门树范围 |
| `performance:certificate:page` | 是 | 是 | 否 | 证书台账定义只读开放给经理 |
| `performance:certificate:info` | 是 | 是 | 否 | 经理只读 |
| `performance:certificate:add` | 是 | 否 | 否 | 仅 `HR` |
| `performance:certificate:update` | 是 | 否 | 否 | 仅 `HR` |
| `performance:certificate:issue` | 是 | 否 | 否 | 仅 `HR` 可发放 |
| `performance:certificate:recordPage` | 是 | 是 | 否 | 经理仅部门树范围只读 |

### 权限规则

1. 主题 13 首批只开放后台管理入口，不开放员工入口和移动端权限键。
2. 部门经理只读能力模型、能力项和证书定义；能力画像摘要与证书记录只允许查询本人部门树范围内员工。
3. `HR` 负责能力模型维护、证书定义维护和证书发放。
4. 首批不新增课程结业发证、面试流程联动或人才主数据维护类权限键。

## 二期主题 15：招聘简历池管理权限冻结基线（2026-04-18）

### 当前冻结结论

- 当前唯一结论：`招聘简历池管理权限已冻结，且主题15已完成阶段0冻结`
- `resumePool` 已接入仓库级 OpenAPI 主源；接口路径与动作名以 `contracts/openapi/xuedao.openapi.json` 为准，权限判定仍以本节为准。

### 权限键定义

| 能力 | 权限键 |
| --- | --- |
| 简历列表 | `performance:resumePool:page` |
| 简历详情 | `performance:resumePool:info` |
| 新增简历 | `performance:resumePool:add` |
| 编辑简历 | `performance:resumePool:update` |
| 导入简历 | `performance:resumePool:import` |
| 导出简历 | `performance:resumePool:export` |
| 上传附件 | `performance:resumePool:uploadAttachment` |
| 下载附件 | `performance:resumePool:downloadAttachment` |
| 转人才资产 | `performance:resumePool:convertToTalentAsset` |
| 发起面试 | `performance:resumePool:createInterview` |

### 角色权限矩阵

| 权限键 | HR 管理员 | 部门经理 | 员工 | 说明 |
| --- | --- | --- | --- | --- |
| `performance:resumePool:page` | 是 | 是 | 否 | 经理仅部门树范围 |
| `performance:resumePool:info` | 是 | 是 | 否 | 经理仅部门树范围 |
| `performance:resumePool:add` | 是 | 是 | 否 | 经理仅可创建本人部门树范围简历 |
| `performance:resumePool:update` | 是 | 是 | 否 | 经理仅可维护本人部门树范围简历 |
| `performance:resumePool:import` | 是 | 是 | 否 | 经理仅可导入本人部门树范围简历 |
| `performance:resumePool:export` | 是 | 否 | 否 | 导出只保留 `HR` |
| `performance:resumePool:uploadAttachment` | 是 | 是 | 否 | 经理仅部门树范围 |
| `performance:resumePool:downloadAttachment` | 是 | 否 | 否 | 下载只保留 `HR` |
| `performance:resumePool:convertToTalentAsset` | 是 | 是 | 否 | 经理仅部门树范围 |
| `performance:resumePool:createInterview` | 是 | 是 | 否 | 经理仅部门树范围 |

### 权限规则

1. 员工首批对简历池完全无入口、无页面权限、无动作权限。
2. 部门经理只能操作 `targetDepartmentId` 落在本人部门树范围内的简历记录。
3. 首批不新增 `delete` 权限键，删除动作不开放。
4. 导出和附件下载都属于下载类动作，首批只保留 `HR`。
5. `createInterview` 与 `convertToTalentAsset` 的调用权限不代表获得主题8 / 主题12的全量管理权限，后续主题仍按各自权限模型独立校验。

## 二期主题 16：招聘计划管理权限冻结基线（2026-04-19）

### 当前冻结结论

- 当前唯一结论：`招聘计划管理权限已按备案制扩展版重新冻结，且主题16已完成新一轮阶段0冻结`

### 权限键定义

| 能力 | 权限键 |
| --- | --- |
| 招聘计划列表 | `performance:recruitPlan:page` |
| 招聘计划详情 | `performance:recruitPlan:info` |
| 新增招聘计划 | `performance:recruitPlan:add` |
| 编辑招聘计划 | `performance:recruitPlan:update` |
| 删除招聘计划 | `performance:recruitPlan:delete` |
| 导入招聘计划 | `performance:recruitPlan:import` |
| 导出招聘计划摘要 | `performance:recruitPlan:export` |
| 提交招聘计划 | `performance:recruitPlan:submit` |
| 关闭招聘计划 | `performance:recruitPlan:close` |
| 作废招聘计划 | `performance:recruitPlan:void` |
| 重新开启招聘计划 | `performance:recruitPlan:reopen` |

### 角色权限矩阵

| 权限键 | HR 管理员 | 部门经理 | 员工 | 说明 |
| --- | --- | --- | --- | --- |
| `performance:recruitPlan:page` | 是 | 是 | 否 | 经理仅部门树范围 |
| `performance:recruitPlan:info` | 是 | 是 | 否 | 经理仅部门树范围 |
| `performance:recruitPlan:add` | 是 | 是 | 否 | 经理仅可创建本人部门树范围计划 |
| `performance:recruitPlan:update` | 是 | 是 | 否 | 经理仅可维护本人部门树范围计划 |
| `performance:recruitPlan:delete` | 是 | 是 | 否 | 删除只允许 `draft` 且经理仅本人部门树范围 |
| `performance:recruitPlan:import` | 是 | 是 | 否 | 导入只允许本人部门树范围模板 |
| `performance:recruitPlan:export` | 是 | 否 | 否 | 导出只保留 `HR` 摘要下载 |
| `performance:recruitPlan:submit` | 是 | 是 | 否 | 经理仅部门树范围 |
| `performance:recruitPlan:close` | 是 | 是 | 否 | 经理仅部门树范围 |
| `performance:recruitPlan:void` | 是 | 是 | 否 | 作废只允许 `active` 且经理仅部门树范围 |
| `performance:recruitPlan:reopen` | 是 | 是 | 否 | 重开只允许 `closed / voided` 且经理仅部门树范围 |

### 权限规则

1. 员工首批无招聘计划入口、无页面权限、无动作权限。
2. 部门经理只能操作 `targetDepartmentId` 落在本人部门树范围内的招聘计划。
3. `export` 属于批量数据提取动作，首批只保留 `HR`。
4. 招聘计划权限不自动赋予职位标准、简历池、面试或录用主题权限。
5. `performance:recruitPlan:*` 当前已进入仓库级 API 契约迁移范围；权限键、接口路径与前端消费显隐必须与 `contracts/openapi/xuedao.openapi.json` 和生成类型同步收敛。

## 二期主题 17：职位标准管理权限冻结基线（2026-04-19）

### 当前冻结结论

- 当前唯一结论：`职位标准管理权限已冻结，且主题17已完成阶段0冻结`

### 权限键定义

| 能力 | 权限键 |
| --- | --- |
| 职位标准列表 | `performance:jobStandard:page` |
| 职位标准详情 | `performance:jobStandard:info` |
| 新增职位标准 | `performance:jobStandard:add` |
| 编辑职位标准 | `performance:jobStandard:update` |
| 更新职位标准状态 | `performance:jobStandard:setStatus` |

### 角色权限矩阵

| 权限键 | HR 管理员 | 部门经理 | 员工 | 说明 |
| --- | --- | --- | --- | --- |
| `performance:jobStandard:page` | 是 | 是 | 否 | 经理仅部门树范围，只读 |
| `performance:jobStandard:info` | 是 | 是 | 否 | 经理仅部门树范围，只读 |
| `performance:jobStandard:add` | 是 | 否 | 否 | 仅 `HR` 可维护 |
| `performance:jobStandard:update` | 是 | 否 | 否 | 仅 `HR` 可维护 |
| `performance:jobStandard:setStatus` | 是 | 否 | 否 | 仅 `HR` 可启停用 |

### 权限规则

1. 部门经理首批只保留职位标准列表 / 详情只读权限，不拥有维护和启停用权限。
2. 员工首批无职位标准入口、无页面权限、无动作权限。
3. 首批不新增删除、导出、评价模板设计器或独立招聘角色模型权限键。
4. 职位标准查看权限不自动赋予招聘计划、面试或录用主题的管理权限。
5. `performance:jobStandard:*` 当前已进入仓库级 API 契约迁移范围；权限键、接口路径与前端消费显隐必须与 `contracts/openapi/xuedao.openapi.json` 和生成类型同步收敛。

## 二期主题 18：录用管理权限冻结基线（2026-04-19）

### 当前冻结结论

- 当前唯一结论：`录用管理权限已冻结，且主题18已完成阶段0冻结`

### 权限键定义

| 能力 | 权限键 |
| --- | --- |
| 录用列表 | `performance:hiring:page` |
| 录用详情 | `performance:hiring:info` |
| 新增录用 | `performance:hiring:add` |
| 更新录用状态 | `performance:hiring:updateStatus` |
| 撤回 / 关闭录用 | `performance:hiring:close` |
| 录用全局范围 | `performance:hiring:all` |

### 角色权限矩阵

| 权限键 | HR 管理员 | 部门经理 | 员工 | 说明 |
| --- | --- | --- | --- | --- |
| `performance:hiring:page` | 是 | 是 | 否 | 经理仅部门树范围 |
| `performance:hiring:info` | 是 | 是 | 否 | 经理仅部门树范围 |
| `performance:hiring:add` | 是 | 是 | 否 | 经理仅可创建本人部门树范围录用单 |
| `performance:hiring:updateStatus` | 是 | 是 | 否 | 经理仅可更新本人部门树范围录用单状态 |
| `performance:hiring:close` | 是 | 是 | 否 | 经理仅可关闭本人部门树范围录用单 |
| `performance:hiring:all` | 是 | 否 | 否 | 仅 `HR` 具备全局范围，不再借用薪资权限或角色名推断 |

### 权限规则

1. 员工首批无录用管理入口、无页面权限、无动作权限。
2. 部门经理只能操作 `targetDepartmentId` 落在本人部门树范围内的录用记录。
3. `performance:hiring:all` 是录用模块 `HR` 全局范围的唯一事实源；运行时不得再通过 `salary` 权限或角色名关键词推断 `HR`。
4. 首批不新增 `delete`、`export`、`downloadAttachment`、`createEmployee`、`syncTalentAssetStatus` 权限键。
5. 录用权限只约束主题18主链，不自动授予主题8 / 15 / 16 / 17全量权限。
6. `hiring` 当前已进入仓库级 API 契约迁移范围；权限键、接口路径与前端消费显隐必须与 `contracts/openapi/xuedao.openapi.json` 和生成类型同步收敛。

## 招聘中心跨模块整合权限协同基线（2026-04-19）

### 当前冻结结论

- 当前唯一结论：`招聘中心跨模块整合权限协同已冻结，但当前不新增独立超级权限或统一写权限键`

### 协同规则

1. `HR` 在招聘中心子主题范围内继续保持全量可见、可维护。
2. 部门经理在招聘中心子主题范围内继续只拥有本人部门树内可见、可写或只读权限；具体动作权限仍以各子主题冻结结论为准。
3. 员工在招聘中心统一入口和各子主题继续无入口、无页面权限、无动作权限。
4. 当前阶段不单独冻结 `performance:recruitmentCenter:*` 权限键；统一入口是否显隐，继续由子主题权限与后续菜单实现卡决定。
5. 拥有某主题的引用写入权限，不代表自动获得被引用主题的写权限。
6. 跨模块跳转或查看来源对象时，仍必须同时满足目标主题自己的 `page / info` 权限和数据范围规则。
7. 任何跨模块引用都不允许绕过目标主题既有的部门树裁剪、只读限制和字段脱敏规则。

## 二期主题 19：班主任化 V0.1 权限冻结基线（2026-04-19）

### 当前冻结结论

- 当前唯一结论：`班主任化 V0.1 权限已冻结，且主题19已完成阶段0冻结`

### 权限键清单

| 能力 | 权限键 |
| --- | --- |
| 班主任资源列表 | `performance:teacherInfo:page` |
| 班主任资源详情 | `performance:teacherInfo:info` |
| 班主任当前归因信息 | `performance:teacherInfo:attributionInfo` |
| 班主任归因历史 | `performance:teacherInfo:attributionHistory` |
| 新增班主任资源 | `performance:teacherInfo:add` |
| 编辑班主任资源 | `performance:teacherInfo:update` |
| 分配资源归属 | `performance:teacherInfo:assign` |
| 更新合作状态 | `performance:teacherInfo:updateStatus` |
| 跟进记录列表 | `performance:teacherFollow:page` |
| 新增跟进记录 | `performance:teacherFollow:add` |
| 标记合作 | `performance:teacherCooperation:mark` |
| 班级列表 | `performance:teacherClass:page` |
| 班级详情 | `performance:teacherClass:info` |
| 新增班级 | `performance:teacherClass:add` |
| 编辑班级 | `performance:teacherClass:update` |
| 删除班级 | `performance:teacherClass:delete` |
| 首页看板 | `performance:teacherDashboard:summary` |
| 我的待跟进 | `performance:teacherTodo:page` |
| 代理列表 | `performance:teacherAgent:page` |
| 代理详情 | `performance:teacherAgent:info` |
| 新增代理 | `performance:teacherAgent:add` |
| 编辑代理 | `performance:teacherAgent:update` |
| 代理状态更新 | `performance:teacherAgent:updateStatus` |
| 拉黑代理 | `performance:teacherAgent:blacklist` |
| 移出代理黑名单 | `performance:teacherAgent:unblacklist` |
| 代理关系列表 | `performance:teacherAgentRelation:page` |
| 新增代理关系 | `performance:teacherAgentRelation:add` |
| 编辑代理关系 | `performance:teacherAgentRelation:update` |
| 删除代理关系 | `performance:teacherAgentRelation:delete` |
| 归因列表 | `performance:teacherAttribution:page` |
| 归因详情 | `performance:teacherAttribution:info` |
| 建立归因 | `performance:teacherAttribution:assign` |
| 调整归因 | `performance:teacherAttribution:change` |
| 移除归因 | `performance:teacherAttribution:remove` |
| 归因冲突列表 | `performance:teacherAttributionConflict:page` |
| 归因冲突详情 | `performance:teacherAttributionConflict:info` |
| 创建归因冲突 | `performance:teacherAttributionConflict:create` |
| 处理归因冲突 | `performance:teacherAttributionConflict:resolve` |
| 代理审计列表 | `performance:teacherAgentAudit:page` |
| 代理审计详情 | `performance:teacherAgentAudit:info` |

### 角色权限矩阵

| 权限键 | 管理层 / 超级管理员 | 部门负责人 | 部门员工 | 只读账号 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `performance:teacherInfo:page` | 是 | 是 | 是 | 是 | 负责人部门树范围；员工本人范围；只读按授权范围只读 |
| `performance:teacherInfo:info` | 是 | 是 | 是 | 是 | 同上 |
| `performance:teacherInfo:attributionInfo` | 是 | 是 | 是 | 是 | 与 `page / info` 保持同口径，只读账号仍返回脱敏后的归因信息 |
| `performance:teacherInfo:attributionHistory` | 是 | 是 | 是 | 是 | 与 `page / info` 保持同口径，只读账号只读 |
| `performance:teacherInfo:add` | 是 | 是 | 是 | 否 | 只读账号无写权限 |
| `performance:teacherInfo:update` | 是 | 是 | 是 | 否 | 负责人部门树范围；员工本人范围 |
| `performance:teacherInfo:assign` | 是 | 是 | 否 | 否 | 仅管理层与部门负责人可分配归属 |
| `performance:teacherInfo:updateStatus` | 是 | 是 | 是 | 否 | 负责人部门树范围；员工本人范围 |
| `performance:teacherFollow:page` | 是 | 是 | 是 | 是 | 只读账号可查看时间线，不可新增 |
| `performance:teacherFollow:add` | 是 | 是 | 是 | 否 | 负责人部门树范围；员工本人范围 |
| `performance:teacherCooperation:mark` | 是 | 是 | 是 | 否 | 只读账号无合作动作 |
| `performance:teacherClass:page` | 是 | 是 | 是 | 是 | 负责人部门树范围；员工本人范围 |
| `performance:teacherClass:info` | 是 | 是 | 是 | 是 | 同上 |
| `performance:teacherClass:add` | 是 | 是 | 是 | 否 | 仅已合作资源可建班 |
| `performance:teacherClass:update` | 是 | 是 | 是 | 否 | 负责人部门树范围；员工本人范围 |
| `performance:teacherClass:delete` | 是 | 是 | 是 | 否 | 仅 `draft` 班级可删 |
| `performance:teacherDashboard:summary` | 是 | 是 | 是 | 是 | 管理层全量；负责人部门树范围；员工 / 只读为本人或授权范围 |
| `performance:teacherTodo:page` | 是 | 是 | 是 | 是 | 管理层全量；负责人部门树范围；员工 / 只读为本人或授权范围 |
| `performance:teacherAgent:page` | 是 | 否 | 否 | 否 | 代理主数据当前仅管理层可见 |
| `performance:teacherAgent:info` | 是 | 否 | 否 | 否 | 同上 |
| `performance:teacherAgent:add` | 是 | 否 | 否 | 否 | 当前仅管理层可新增代理 |
| `performance:teacherAgent:update` | 是 | 否 | 否 | 否 | 当前仅管理层可编辑代理 |
| `performance:teacherAgent:updateStatus` | 是 | 否 | 否 | 否 | 当前仅管理层可更新代理状态 |
| `performance:teacherAgent:blacklist` | 是 | 否 | 否 | 否 | 当前仅管理层可拉黑代理 |
| `performance:teacherAgent:unblacklist` | 是 | 否 | 否 | 否 | 当前仅管理层可移出黑名单 |
| `performance:teacherAgentRelation:page` | 是 | 否 | 否 | 否 | 代理关系当前仅管理层可见 |
| `performance:teacherAgentRelation:add` | 是 | 否 | 否 | 否 | 当前仅管理层可新增代理关系 |
| `performance:teacherAgentRelation:update` | 是 | 否 | 否 | 否 | 当前仅管理层可编辑代理关系 |
| `performance:teacherAgentRelation:delete` | 是 | 否 | 否 | 否 | 当前仅管理层可删除代理关系 |
| `performance:teacherAttribution:page` | 是 | 否 | 否 | 否 | 归因台账当前仅管理层可见 |
| `performance:teacherAttribution:info` | 是 | 否 | 否 | 否 | 同上 |
| `performance:teacherAttribution:assign` | 是 | 否 | 否 | 否 | 当前仅管理层可建立归因 |
| `performance:teacherAttribution:change` | 是 | 否 | 否 | 否 | 当前仅管理层可调整归因 |
| `performance:teacherAttribution:remove` | 是 | 否 | 否 | 否 | 当前仅管理层可移除归因 |
| `performance:teacherAttributionConflict:page` | 是 | 否 | 否 | 否 | 冲突台账当前仅管理层可见 |
| `performance:teacherAttributionConflict:info` | 是 | 否 | 否 | 否 | 同上 |
| `performance:teacherAttributionConflict:create` | 是 | 否 | 否 | 否 | 当前仅管理层可创建冲突 |
| `performance:teacherAttributionConflict:resolve` | 是 | 否 | 否 | 否 | 当前仅管理层可处理冲突 |
| `performance:teacherAgentAudit:page` | 是 | 否 | 否 | 否 | 审计记录当前仅管理层可见 |
| `performance:teacherAgentAudit:info` | 是 | 否 | 否 | 否 | 同上 |

### 权限规则

1. 数据范围固定为四档：
   - 管理层 / 超级管理员：全量
   - 部门负责人：本人部门树范围
   - 部门员工：本人归属范围
   - 只读账号：授权范围只读
2. 只读账号不拥有新增、编辑、分配、跟进、合作和建班动作权限。
3. 部门员工无资源分配权限。
4. 代理、归因、冲突和审计能力当前只冻结管理层口径，不向部门负责人、员工或只读账号开放。
5. 主题19权限不自动赋予代理、绩效、结算或导入导出能力。
6. 当前前端一级导航已按业务域拆分为 `目标&计划 -> 班主任化` 二级目录；`首页看板 / 班主任列表 / 我的待跟进 / 班级列表` 不再与“绩效中心”共用同名一级入口。
7. `班级列表` 页面当前仍只消费 `performance:teacherClass:page / info / add / update / delete` 既有权限键；本轮前端只修正表单默认值工厂，不新增权限键、不扩大按钮显隐范围。
## 采购&资产 Phase 1：物资管理权限冻结基线（2026-04-19）

### 当前冻结结论

- 当前唯一结论：`物资管理 Phase 1 当前只冻结资源权限键和最小菜单授权口径；stage2 seed 暂按 HR 可用、经理和员工无入口收口`

### 权限键清单

| 能力 | 权限键 |
| --- | --- |
| 物资台账列表 | `performance:materialCatalog:page` |
| 物资台账详情 | `performance:materialCatalog:info` |
| 新增物资 | `performance:materialCatalog:add` |
| 编辑物资 | `performance:materialCatalog:update` |
| 更新物资状态 | `performance:materialCatalog:updateStatus` |
| 删除物资 | `performance:materialCatalog:delete` |
| 物资库存列表 | `performance:materialStock:page` |
| 物资库存详情 | `performance:materialStock:info` |
| 物资库存汇总 | `performance:materialStock:summary` |
| 物资库存流水分页 | `performance:materialStockLog:page` |
| 物资入库列表 | `performance:materialInbound:page` |
| 物资入库详情 | `performance:materialInbound:info` |
| 新增物资入库单 | `performance:materialInbound:add` |
| 编辑物资入库单 | `performance:materialInbound:update` |
| 提交物资入库单 | `performance:materialInbound:submit` |
| 确认物资入库 | `performance:materialInbound:receive` |
| 取消物资入库单 | `performance:materialInbound:cancel` |
| 物资领用列表 | `performance:materialIssue:page` |
| 物资领用详情 | `performance:materialIssue:info` |
| 新增物资领用单 | `performance:materialIssue:add` |
| 编辑物资领用单 | `performance:materialIssue:update` |
| 提交物资领用单 | `performance:materialIssue:submit` |
| 确认物资出库 | `performance:materialIssue:issue` |
| 取消物资领用单 | `performance:materialIssue:cancel` |

### 角色权限矩阵

| 权限键 | HR 管理员 | 部门经理 | 普通员工 | 说明 |
| --- | --- | --- | --- | --- |
| `performance:materialCatalog:*` | 是 | 否 | 否 | Phase 1 目录主数据暂不向经理/员工开放 |
| `performance:materialStock:*` | 是 | 否 | 否 | Phase 1 库存只读入口仍按 HR 菜单下发 |
| `performance:materialStockLog:page` | 是 | 否 | 否 | 库存流水当前作为库存只读子页，仅 `HR` 可查 |
| `performance:materialInbound:*` | 是 | 否 | 否 | Phase 1 入库闭环暂由 HR 管理入口承接 |
| `performance:materialIssue:*` | 是 | 否 | 否 | Phase 1 领用单管理暂不下发经理/员工入口 |

### 权限规则

1. 当前只冻结 `performance:materialCatalog / materialStock / materialStockLog / materialInbound / materialIssue` 五组资源键，不新增独立 `workbench`、审批或报表权限键。
2. `materialStock` Phase 1 保持只读，因此本节只冻结 `page / info / summary`，库存流水分页继续作为只读附属能力。
3. `materialInbound` 只冻结 `page / info / add / update / submit / receive / cancel`，不提前虚写 `delete / approve`。
4. `materialIssue` 只冻结 `page / info / add / update / submit / issue / cancel`，不提前虚写 `return`。
5. 若后续要开放经理或员工入口，必须重新冻结角色范围、部门树口径和按钮显隐，不在本轮 seed 里默认放开。

## 二期主题 20：资产管理全生命周期权限冻结基线（2026-04-19）

### 当前冻结结论

- 当前唯一结论：`资产管理全生命周期权限已冻结，且主题20已完成阶段0冻结`

### 权限键清单

| 能力 | 权限键 |
| --- | --- |
| 资产首页 | `performance:assetDashboard:summary` |
| 资产台账列表 | `performance:assetInfo:page` |
| 资产详情 | `performance:assetInfo:info` |
| 新增资产 | `performance:assetInfo:add` |
| 编辑资产 | `performance:assetInfo:update` |
| 删除资产 | `performance:assetInfo:delete` |
| 更新资产状态 | `performance:assetInfo:updateStatus` |
| 领用记录列表 | `performance:assetAssignment:page` |
| 新增领用记录 | `performance:assetAssignment:add` |
| 编辑领用记录 | `performance:assetAssignment:update` |
| 归还资产 | `performance:assetAssignment:return` |
| 标记丢失 | `performance:assetAssignment:markLost` |
| 删除领用记录 | `performance:assetAssignment:delete` |
| 维护记录列表 | `performance:assetMaintenance:page` |
| 新增维护记录 | `performance:assetMaintenance:add` |
| 编辑维护记录 | `performance:assetMaintenance:update` |
| 完成维护 | `performance:assetMaintenance:complete` |
| 取消维护 | `performance:assetMaintenance:cancel` |
| 删除维护记录 | `performance:assetMaintenance:delete` |
| 采购入库列表 | `performance:assetProcurement:page` |
| 采购入库详情 | `performance:assetProcurement:info` |
| 新增采购入库 | `performance:assetProcurement:add` |
| 编辑采购入库 | `performance:assetProcurement:update` |
| 提交采购入库 | `performance:assetProcurement:submit` |
| 确认入库 | `performance:assetProcurement:receive` |
| 取消采购入库 | `performance:assetProcurement:cancel` |
| 调拨单列表 | `performance:assetTransfer:page` |
| 调拨单详情 | `performance:assetTransfer:info` |
| 新增调拨单 | `performance:assetTransfer:add` |
| 编辑调拨单 | `performance:assetTransfer:update` |
| 提交调拨单 | `performance:assetTransfer:submit` |
| 完成调拨 | `performance:assetTransfer:complete` |
| 取消调拨单 | `performance:assetTransfer:cancel` |
| 盘点单列表 | `performance:assetInventory:page` |
| 盘点单详情 | `performance:assetInventory:info` |
| 新增盘点单 | `performance:assetInventory:add` |
| 编辑盘点单 | `performance:assetInventory:update` |
| 开始盘点 | `performance:assetInventory:start` |
| 完成盘点 | `performance:assetInventory:complete` |
| 关闭盘点 | `performance:assetInventory:close` |
| 折旧列表 | `performance:assetDepreciation:page` |
| 折旧汇总 | `performance:assetDepreciation:summary` |
| 折旧重算 | `performance:assetDepreciation:recalculate` |
| 报废单列表 | `performance:assetDisposal:page` |
| 报废单详情 | `performance:assetDisposal:info` |
| 新增报废单 | `performance:assetDisposal:add` |
| 编辑报废单 | `performance:assetDisposal:update` |
| 提交报废单 | `performance:assetDisposal:submit` |
| 审批报废单 | `performance:assetDisposal:approve` |
| 执行报废 | `performance:assetDisposal:execute` |
| 取消报废单 | `performance:assetDisposal:cancel` |
| 资产报表汇总 | `performance:assetReport:summary` |
| 资产报表分页 | `performance:assetReport:page` |
| 导出资产报表 | `performance:assetReport:export` |

### 角色权限矩阵

| 权限键 | 管理层 / 超级管理员 | 资产管理员 | 部门负责人 | 财务只读 | 普通员工 | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
| `performance:assetDashboard:summary` | 是 | 是 | 是 | 是 | 否 | 员工无入口 |
| `performance:assetInfo:page` | 是 | 是 | 是 | 是 | 否 | 部门负责人部门树范围；财务只读只读 |
| `performance:assetInfo:info` | 是 | 是 | 是 | 是 | 否 | 同上 |
| `performance:assetInfo:add` | 是 | 是 | 否 | 否 | 否 | 新建资产由管理层 / 资产管理员执行 |
| `performance:assetInfo:update` | 是 | 是 | 否 | 否 | 否 | 部门负责人无台账主数据维护权 |
| `performance:assetInfo:delete` | 是 | 是 | 否 | 否 | 否 | 删除受终态与引用限制 |
| `performance:assetInfo:updateStatus` | 是 | 是 | 否 | 否 | 否 | 主状态切换由资产侧角色执行 |
| `performance:assetAssignment:page` | 是 | 是 | 是 | 是 | 否 | 财务只读只看摘要 |
| `performance:assetAssignment:add` | 是 | 是 | 是 | 否 | 否 | 部门负责人仅部门树范围 |
| `performance:assetAssignment:update` | 是 | 是 | 是 | 否 | 否 | 同上 |
| `performance:assetAssignment:return` | 是 | 是 | 是 | 否 | 否 | 同上 |
| `performance:assetAssignment:markLost` | 是 | 是 | 否 | 否 | 否 | 丢失终态仅资产侧角色可改 |
| `performance:assetAssignment:delete` | 是 | 是 | 否 | 否 | 否 | 删除只限资产侧角色 |
| `performance:assetMaintenance:page` | 是 | 是 | 是 | 是 | 否 | 财务只读只读 |
| `performance:assetMaintenance:add` | 是 | 是 | 是 | 否 | 否 | 部门负责人仅部门树范围 |
| `performance:assetMaintenance:update` | 是 | 是 | 是 | 否 | 否 | 同上 |
| `performance:assetMaintenance:complete` | 是 | 是 | 是 | 否 | 否 | 同上 |
| `performance:assetMaintenance:cancel` | 是 | 是 | 是 | 否 | 否 | 同上 |
| `performance:assetMaintenance:delete` | 是 | 是 | 否 | 否 | 否 | 删除仅资产侧角色 |
| `performance:assetProcurement:*` | 是 | 是 | 否 | 是 | 否 | 财务只读可看 `page / info`，无写权限 |
| `performance:assetTransfer:*` | 是 | 是 | 是 | 否 | 否 | 部门负责人仅部门树范围 |
| `performance:assetInventory:*` | 是 | 是 | 是 | 否 | 否 | 部门负责人仅部门树范围 |
| `performance:assetDepreciation:page` | 是 | 是 | 否 | 是 | 否 | 折旧仅管理 / 财务查看 |
| `performance:assetDepreciation:summary` | 是 | 是 | 否 | 是 | 否 | 同上 |
| `performance:assetDepreciation:recalculate` | 是 | 是 | 否 | 否 | 否 | 重算仅资产侧角色 |
| `performance:assetDisposal:*` | 是 | 是 | 是 | 否 | 否 | 部门负责人仅可提交本人部门树范围报废单，不可执行跨范围 |
| `performance:assetReport:summary` | 是 | 是 | 是 | 是 | 否 | 员工无入口 |
| `performance:assetReport:page` | 是 | 是 | 是 | 是 | 否 | 同上 |
| `performance:assetReport:export` | 是 | 是 | 否 | 是 | 否 | 报表导出不授予部门负责人 |

### 权限规则

1. 数据范围固定为五档：
   - 管理层 / 超级管理员：全量
   - 资产管理员：全量
   - 部门负责人：本人部门树范围
   - 财务只读：金额与折旧只读范围
   - 普通员工：无入口
2. 财务只读不拥有新增、编辑、审批、执行和重算动作权限。
3. 部门负责人不拥有采购入库确认、折旧重算和资产主数据维护权限。
4. 当前阶段0权限键保持不变，仍以现有 `performance:assetAssignment:*` 作为系统内领用 / 归还 / 丢失处理权限。
5. `L0` 免审批直领继续沿用现有 `performance:assetAssignment:add` 权限链，不新增审批权限键。
6. `L1 / L2` 领用审批规则当前为正式治理口径，但尚未在系统内新增申请层资源和审批权限键，因此本节不提前虚写新的 `submit / approve / reject` 权限。
7. 若后续把 `L1 / L2` 场景系统审批化，必须重新打开主题20与统一审批流边界，并补充申请提交、审批通过 / 驳回、发放执行权限；不能直接复用 `performance:assetAssignment:add` 作为审批动作权限。
8. 命中敏感资产、跨部门借用、遗失后补领、异常补发和报废替换发放的领用场景，不得以“当前已有 `assetAssignment:add` 写权限”为由绕过正式审批口径。
4. 主题20权限不自动赋予采购审批、供应商维护、财务凭证和车辆管理能力。

### Theme20 L1/L2 领用申请权限补记（2026-04-20）

以下权限键已在 2026-04-20 本地 stage2 runtime 的 `menu.json + seed-stage2-performance.mjs` 中真实落地。

| 能力 | 权限键 |
| --- | --- |
| 领用申请列表 | `performance:assetAssignmentRequest:page` |
| 领用申请详情 | `performance:assetAssignmentRequest:info` |
| 新增领用申请草稿 | `performance:assetAssignmentRequest:add` |
| 编辑领用申请草稿 | `performance:assetAssignmentRequest:update` |
| 提交领用申请 | `performance:assetAssignmentRequest:submit` |
| 撤回领用申请 | `performance:assetAssignmentRequest:withdraw` |
| 配发资产 | `performance:assetAssignmentRequest:assign` |
| 取消领用申请 | `performance:assetAssignmentRequest:cancel` |

| 权限键 | `HR` | 资产管理员 | 部门负责人 | 普通员工 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `performance:assetAssignmentRequest:page` | 是 | 是 | 是 | 是 | 员工仅本人申请；经理仅本人部门树范围；`HR / 资产管理员` 全量 |
| `performance:assetAssignmentRequest:info` | 是 | 是 | 是 | 是 | 同上 |
| `performance:assetAssignmentRequest:add` | 否 | 否 | 可选 | 是 | 首批员工本人发起；seed 中部门负责人保留同类入口便于联调 |
| `performance:assetAssignmentRequest:update` | 否 | 否 | 可选 | 是 | 仅 `draft` 且仅申请人本人可编辑 |
| `performance:assetAssignmentRequest:submit` | 否 | 否 | 否 | 是 | 仅申请人本人 |
| `performance:assetAssignmentRequest:withdraw` | 否 | 否 | 否 | 是 | 仅申请人本人且审批未终态 |
| `performance:assetAssignmentRequest:assign` | 否 | 是 | 否 | 否 | 仅 `approvedPendingAssignment` 允许执行 |
| `performance:assetAssignmentRequest:cancel` | 是 | 是 | 否 | 否 | 用于 `HR / 资产侧` 收口异常申请 |

补充规则：

1. `approval-flow:approve / reject / transfer` 仍只授予当前审批角色，不随 `assetAssignmentRequest:*` 自动放开。
2. `assetAssignmentRequest:assign` 不等于审批权，它只承接“审批通过后的实物配发”动作。

## 行政协同主链权限基线（2026-04-19）

### 当前冻结结论

- 当前唯一结论：`行政协同域 9 条 HR-only 台账主链均已冻结；其中主题21覆盖文件管理 / 知识库，主题22覆盖 5 个行政协同记录页，主模块补充覆盖车辆管理 / 知识产权管理`

### 权限键清单

| 能力 | 权限键 |
| --- | --- |
| 年检材料入口 | `performance:annualInspection:page` |
| 荣誉管理入口 | `performance:honor:page` |
| 宣传资料入口 | `performance:publicityMaterial:page` |
| 美工协同入口 | `performance:designCollab:page` |
| 快递协同入口 | `performance:expressCollab:page` |
| 文件管理入口 | `performance:documentCenter:page` |
| 知识库入口 | `performance:knowledgeBase:page` |
| 车辆管理入口 | `performance:vehicle:page` |
| 车辆管理详情 | `performance:vehicle:info` |
| 车辆管理统计 | `performance:vehicle:stats` |
| 车辆管理新增 | `performance:vehicle:add` |
| 车辆管理编辑 | `performance:vehicle:update` |
| 车辆管理删除 | `performance:vehicle:delete` |
| 知识产权管理入口 | `performance:intellectualProperty:page` |
| 知识产权管理详情 | `performance:intellectualProperty:info` |
| 知识产权管理统计 | `performance:intellectualProperty:stats` |
| 知识产权管理新增 | `performance:intellectualProperty:add` |
| 知识产权管理编辑 | `performance:intellectualProperty:update` |
| 知识产权管理删除 | `performance:intellectualProperty:delete` |

### 角色权限矩阵

| 权限键 | HR 管理员 | 部门经理 | 员工 | 说明 |
| --- | --- | --- | --- | --- |
| `performance:annualInspection:page` | 是 | 否 | 否 | 入口已预埋，具体动作权限见主题22冻结基线 |
| `performance:honor:page` | 是 | 否 | 否 | 入口已预埋，具体动作权限见主题22冻结基线 |
| `performance:publicityMaterial:page` | 是 | 否 | 否 | 入口已预埋，具体动作权限见主题22冻结基线 |
| `performance:designCollab:page` | 是 | 否 | 否 | 入口已预埋，具体动作权限见主题22冻结基线 |
| `performance:expressCollab:page` | 是 | 否 | 否 | 入口已预埋，具体动作权限见主题22冻结基线 |
| `performance:documentCenter:page` | 是 | 否 | 否 | 入口已预埋，具体动作权限见主题21冻结基线 |
| `performance:knowledgeBase:page` | 是 | 否 | 否 | 入口已预埋，具体动作权限见主题21冻结基线 |
| `performance:vehicle:page` | 是 | 否 | 否 | 车辆管理列表 |
| `performance:vehicle:info` | 是 | 否 | 否 | 车辆管理详情 |
| `performance:vehicle:stats` | 是 | 否 | 否 | 车辆管理统计 |
| `performance:vehicle:add` | 是 | 否 | 否 | 车辆管理新增 |
| `performance:vehicle:update` | 是 | 否 | 否 | 车辆管理编辑 |
| `performance:vehicle:delete` | 是 | 否 | 否 | 车辆管理删除 |
| `performance:intellectualProperty:page` | 是 | 否 | 否 | 知识产权列表 |
| `performance:intellectualProperty:info` | 是 | 否 | 否 | 知识产权详情 |
| `performance:intellectualProperty:stats` | 是 | 否 | 否 | 知识产权统计 |
| `performance:intellectualProperty:add` | 是 | 否 | 否 | 知识产权新增 |
| `performance:intellectualProperty:update` | 是 | 否 | 否 | 知识产权编辑 |
| `performance:intellectualProperty:delete` | 是 | 否 | 否 | 知识产权删除 |

### 权限规则

1. 行政协同域当前统一按后台管理口径只开放 `HR 管理员`。
2. 部门经理与员工当前无菜单入口、无页面权限、无动作权限。
3. 主题21 / 22 的细颗粒动作权限继续以各自冻结基线为准；本节补录车辆管理与知识产权管理的完整动作权限闭环。
4. 若后续把车辆或知识产权扩成流程型系统，必须新增独立动作权限，不得复用当前台账动作键偷跑业务能力。

## 二期主题 21：文件管理与知识库权限冻结基线（2026-04-19）

### 当前冻结结论

- 当前唯一结论：`文件管理与知识库权限已冻结，且主题21已完成阶段0冻结`

### 权限键清单

| 能力 | 权限键 |
| --- | --- |
| 文件管理入口 | `performance:documentCenter:page` |
| 文件详情 | `performance:documentCenter:info` |
| 文件统计 | `performance:documentCenter:stats` |
| 新增文件元数据 | `performance:documentCenter:add` |
| 编辑文件元数据 | `performance:documentCenter:update` |
| 删除文件元数据 | `performance:documentCenter:delete` |
| 知识库入口 | `performance:knowledgeBase:page` |
| 知识统计 | `performance:knowledgeBase:stats` |
| 新增知识条目 | `performance:knowledgeBase:add` |
| 编辑知识条目 | `performance:knowledgeBase:update` |
| 删除知识条目 | `performance:knowledgeBase:delete` |
| 知识图谱 | `performance:knowledgeBase:graph` |
| 知识搜索 | `performance:knowledgeBase:search` |
| 百问百答列表 | `performance:knowledgeBase:qaList` |
| 新增百问百答 | `performance:knowledgeBase:qaAdd` |

### 角色权限矩阵

| 权限键 | HR 管理员 | 部门经理 | 员工 | 说明 |
| --- | --- | --- | --- | --- |
| `performance:documentCenter:page` | 是 | 否 | 否 | 首批只开放后台管理口径 |
| `performance:documentCenter:info` | 是 | 否 | 否 | 仅查看元数据详情 |
| `performance:documentCenter:stats` | 是 | 否 | 否 | 仅查看汇总统计 |
| `performance:documentCenter:add` | 是 | 否 | 否 | 仅写入元数据 |
| `performance:documentCenter:update` | 是 | 否 | 否 | 包含状态切换 |
| `performance:documentCenter:delete` | 是 | 否 | 否 | 删除元数据记录 |
| `performance:knowledgeBase:page` | 是 | 否 | 否 | 首批只开放后台管理口径 |
| `performance:knowledgeBase:stats` | 是 | 否 | 否 | 仅查看知识统计 |
| `performance:knowledgeBase:add` | 是 | 否 | 否 | 新增知识条目元数据 |
| `performance:knowledgeBase:update` | 是 | 否 | 否 | 编辑知识条目元数据 |
| `performance:knowledgeBase:delete` | 是 | 否 | 否 | 删除知识条目元数据 |
| `performance:knowledgeBase:graph` | 是 | 否 | 否 | 图谱只对后台管理开放 |
| `performance:knowledgeBase:search` | 是 | 否 | 否 | 搜索只对后台管理开放 |
| `performance:knowledgeBase:qaList` | 是 | 否 | 否 | 百问百答列表只对后台管理开放 |
| `performance:knowledgeBase:qaAdd` | 是 | 否 | 否 | 百问百答新增只对后台管理开放 |

### 权限规则

1. 主题21 首批统一只开放 `HR 管理员`，不提前冻结部门经理范围和员工自助知识查询权限。
2. 主题21 不新增公开链接、访客权限、部门继承权限和外部协作者权限。
3. 若后续要把知识搜索、百问百答或文件台账下放给经理 / 员工，必须重新打开阶段0冻结。

## 二期主题 22：行政协同记录管理权限冻结基线（2026-04-19）

### 当前冻结结论

- 当前唯一结论：`行政协同记录管理权限已冻结，且主题22已完成阶段0冻结`

### 权限键清单

| 能力 | 权限键 |
| --- | --- |
| 年检材料入口 | `performance:annualInspection:page` |
| 荣誉管理入口 | `performance:honor:page` |
| 宣传资料入口 | `performance:publicityMaterial:page` |
| 美工协同入口 | `performance:designCollab:page` |
| 快递协同入口 | `performance:expressCollab:page` |
| 年检材料详情 | `performance:annualInspection:info` |
| 年检材料统计 | `performance:annualInspection:stats` |
| 年检材料新增 | `performance:annualInspection:add` |
| 年检材料编辑 | `performance:annualInspection:update` |
| 年检材料删除 | `performance:annualInspection:delete` |
| 荣誉管理详情 | `performance:honor:info` |
| 荣誉管理统计 | `performance:honor:stats` |
| 荣誉管理新增 | `performance:honor:add` |
| 荣誉管理编辑 | `performance:honor:update` |
| 荣誉管理删除 | `performance:honor:delete` |
| 宣传资料详情 | `performance:publicityMaterial:info` |
| 宣传资料统计 | `performance:publicityMaterial:stats` |
| 宣传资料新增 | `performance:publicityMaterial:add` |
| 宣传资料编辑 | `performance:publicityMaterial:update` |
| 宣传资料删除 | `performance:publicityMaterial:delete` |
| 美工协同详情 | `performance:designCollab:info` |
| 美工协同统计 | `performance:designCollab:stats` |
| 美工协同新增 | `performance:designCollab:add` |
| 美工协同编辑 | `performance:designCollab:update` |
| 美工协同删除 | `performance:designCollab:delete` |
| 快递协同详情 | `performance:expressCollab:info` |
| 快递协同统计 | `performance:expressCollab:stats` |
| 快递协同新增 | `performance:expressCollab:add` |
| 快递协同编辑 | `performance:expressCollab:update` |
| 快递协同删除 | `performance:expressCollab:delete` |

### 角色权限矩阵

| 权限键 | HR 管理员 | 部门经理 | 员工 | 说明 |
| --- | --- | --- | --- | --- |
| `performance:annualInspection:page` | 是 | 否 | 否 | 年检材料页面入口 |
| `performance:annualInspection:info` | 是 | 否 | 否 | 年检材料详情动作 |
| `performance:annualInspection:stats` | 是 | 否 | 否 | 年检材料统计动作 |
| `performance:annualInspection:add` | 是 | 否 | 否 | 年检材料新增动作 |
| `performance:annualInspection:update` | 是 | 否 | 否 | 年检材料编辑动作 |
| `performance:annualInspection:delete` | 是 | 否 | 否 | 年检材料删除动作 |
| `performance:honor:page` | 是 | 否 | 否 | 荣誉管理页面入口 |
| `performance:honor:info` | 是 | 否 | 否 | 荣誉管理详情动作 |
| `performance:honor:stats` | 是 | 否 | 否 | 荣誉管理统计动作 |
| `performance:honor:add` | 是 | 否 | 否 | 荣誉管理新增动作 |
| `performance:honor:update` | 是 | 否 | 否 | 荣誉管理编辑动作 |
| `performance:honor:delete` | 是 | 否 | 否 | 荣誉管理删除动作 |
| `performance:publicityMaterial:page` | 是 | 否 | 否 | 宣传资料页面入口 |
| `performance:publicityMaterial:info` | 是 | 否 | 否 | 宣传资料详情动作 |
| `performance:publicityMaterial:stats` | 是 | 否 | 否 | 宣传资料统计动作 |
| `performance:publicityMaterial:add` | 是 | 否 | 否 | 宣传资料新增动作 |
| `performance:publicityMaterial:update` | 是 | 否 | 否 | 宣传资料编辑动作 |
| `performance:publicityMaterial:delete` | 是 | 否 | 否 | 宣传资料删除动作 |
| `performance:designCollab:page` | 是 | 否 | 否 | 美工协同页面入口 |
| `performance:designCollab:info` | 是 | 否 | 否 | 美工协同详情动作 |
| `performance:designCollab:stats` | 是 | 否 | 否 | 美工协同统计动作 |
| `performance:designCollab:add` | 是 | 否 | 否 | 美工协同新增动作 |
| `performance:designCollab:update` | 是 | 否 | 否 | 美工协同编辑动作 |
| `performance:designCollab:delete` | 是 | 否 | 否 | 美工协同删除动作 |
| `performance:expressCollab:page` | 是 | 否 | 否 | 快递协同页面入口 |
| `performance:expressCollab:info` | 是 | 否 | 否 | 快递协同详情动作 |
| `performance:expressCollab:stats` | 是 | 否 | 否 | 快递协同统计动作 |
| `performance:expressCollab:add` | 是 | 否 | 否 | 快递协同新增动作 |
| `performance:expressCollab:update` | 是 | 否 | 否 | 快递协同编辑动作 |
| `performance:expressCollab:delete` | 是 | 否 | 否 | 快递协同删除动作 |

### 权限规则

1. 主题22 首批统一只开放 `HR 管理员`。
2. 页面入口和动作权限均以各模块自身资源名命名，避免菜单、接口和前端 service 再出现共享权限资源漂移。
3. 后端允许通过共享 service 复用权限校验逻辑，但运行态菜单、前端显隐和文档矩阵都必须以 `performance:<module>:<action>` 为准。
4. 主题22 不新增公开链接、访客权限、部门继承权限和外部协作者权限。
5. 若后续要把任一行政协同页面下放给经理 / 员工，必须重新打开阶段0冻结。

## 2026-04-19 多主题收口补充

### 新增或统一的权限资源

1. 招聘与人才资源继续统一使用：
   - `performance:recruitPlan:*`
   - `performance:resumePool:*`
   - `performance:talentAsset:*`
2. 采购补充：
   - `performance:purchaseOrder:*`
   - `performance:purchaseReport:*`
3. 班主任化补充：
   - `performance:teacherInfo:*`
   - `performance:teacherFollow:*`
   - `performance:teacherCooperation:*`
   - `performance:teacherClass:*`
   - `performance:teacherDashboard:*`
   - `performance:teacherTodo:*`
4. 行政协同补充：
   - `performance:documentCenter:*`
   - `performance:knowledgeBase:*`
   - `performance:vehicle:*`
   - `performance:intellectualProperty:*`

### 角色边界补充

1. `teacher*` 资源继续按主题19冻结口径执行：
   - 管理层 / 超级管理员：全量
   - 部门负责人：部门树范围，可分配
   - 部门员工：本人范围，不可分配
   - 只读账号：只读
2. `recruitPlan / resumePool / talentAsset / purchaseOrder / purchaseReport` 继续沿用各自主题冻结的 HR、经理、员工范围口径，不在本次收口里扩大权限。
3. `documentCenter / knowledgeBase / vehicle / intellectualProperty / annualInspection / honor / publicityMaterial / designCollab / expressCollab` 当前维持 `HR-only`，经理和员工无入口、无动作权限。

### 实施约束补充

1. 前端按钮显隐、菜单入口、后端权限校验、自动化守卫必须同时使用同一权限键。
2. 若页面目录或 service 路径重命名，权限键不得跟随文件名漂移。
3. `talentAsset` 已作为统一命名保留，禁止再回退到 `talent-asset` 风格的权限资源名。
