# 页面结构与原型映射

## 文档职责

这份文档是“页面结构参考基线与原型映射”的唯一事实源。

它只负责定义：

1. `jixiao` 已实现页面作为结构参考的映射关系
2. 哪些页面结构直接复用
3. 哪些页面只复用交互分层，不直接复用路由
4. `cool-admin-vue` 首期页面目录与组件拆分规则

## 不负责内容

这份文档不负责定义：

- 菜单权限键
- 接口字段详情
- 表结构字段类型
- 业务状态流转

## 参考来源

首期后台页面结构参考以下已实现项目：

- [performance.ts](/Users/shaotongli/Documents/jixiao/src/router/performance.ts)
- [Index.vue](/Users/shaotongli/Documents/jixiao/src/views/performance/Index.vue)
- [Assessments.vue](/Users/shaotongli/Documents/jixiao/src/views/performance/Assessments.vue)
- [AssessmentDetail.vue](/Users/shaotongli/Documents/jixiao/src/views/performance/AssessmentDetail.vue)
- [MyAssessments.vue](/Users/shaotongli/Documents/jixiao/src/views/performance/MyAssessments.vue)
- [Initiated.vue](/Users/shaotongli/Documents/jixiao/src/views/performance/Initiated.vue)
- [Pending.vue](/Users/shaotongli/Documents/jixiao/src/views/performance/Pending.vue)
- [GoalMap.vue](/Users/shaotongli/Documents/jixiao/src/views/performance/GoalMap.vue)
- [IndicatorLibrary.vue](/Users/shaotongli/Documents/jixiao/src/views/performance/IndicatorLibrary.vue)
- [Feedback.vue](/Users/shaotongli/Documents/jixiao/src/views/performance/Feedback.vue)
- [Feedback360.vue](/Users/shaotongli/Documents/jixiao/src/views/performance/Feedback360.vue)
- [PIP.vue](/Users/shaotongli/Documents/jixiao/src/views/performance/PIP.vue)
- [Promotion.vue](/Users/shaotongli/Documents/jixiao/src/views/performance/Promotion.vue)
- [Salary.vue](/Users/shaotongli/Documents/jixiao/src/views/performance/Salary.vue)

## 适配总原则

1. 复用的是“页面结构和交互分层”，不是直接复制 `jixiao` 的技术实现代码。
2. 首期必须落在 `cool-admin-vue/src/modules/performance/` 下，不新建第二套前端工程。
3. `jixiao` 的 `/performance` 首页导航卡片只作为原型参考，不作为首期必须交付页面。
4. 首期顶部一级入口继续使用 Cool 动态菜单树，不额外新增独立导航门户页。
5. 首期详情优先采用“列表页 + 抽屉/弹窗详情”，跨模块跳转场景才考虑补隐藏详情页。
6. 同一业务对象的三种视角页面可以拆成独立菜单，但必须复用同一套详情和表单组件。

## 路由结构映射

| `jixiao` 参考页 | 参考路由 | 首期落地页 | `cool-admin-vue` 落点 | 落地结论 |
| --- | --- | --- | --- | --- |
| `Index.vue` | `/performance` | 不单独落地菜单页 | 无 | 只复用“模块导航分组”思路，首期仍以左侧菜单树进入各模块 |
| `Assessments.vue` | `/performance/assessments` | 已发起考核 | `modules/performance/views/initiated/index.vue` | 复用列表筛选、表格动作、表单弹窗结构 |
| `MyAssessments.vue` | `/performance/my-assessments` | 我的考核 | `modules/performance/views/my-assessment/index.vue` | 复用“概览卡 + 当前详情 + 历史列表”三段式结构 |
| `Pending.vue` | `/performance/pending` | 待我审批 | `modules/performance/views/pending/index.vue` | 复用待办列表 + 审批弹窗结构 |
| `AssessmentDetail.vue` | `/performance/assessments/:id` | 评估详情组件 | `modules/performance/components/assessment-detail.vue` | 首期优先作为抽屉/弹窗复用组件，不单列菜单 |
| `GoalMap.vue` | `/performance/goals` | 目标地图 | `modules/performance/views/goals/index.vue` | 作为首期目标模块主参考页 |
| `Goals.vue` | `/performance/goals` | 目标简表结构 | `modules/performance/views/goals/index.vue` | 只吸收轻量列表区设计，不作为主基线 |
| `IndicatorLibrary.vue` | `/performance/indicators` | 指标库 | `modules/performance/views/indicator-library/index.vue` | 直接复用“筛选 + 列表 + 弹窗 CRUD”结构 |
| `Feedback.vue` | `/performance/feedback` | 360 环评任务页 | `modules/performance/views/feedback/index.vue` | 作为任务列表、发起、查看入口结构参考 |
| `Feedback360.vue` | `/performance/360-feedback` | 360 环评填写/汇总区 | `modules/performance/views/feedback/index.vue` 内部组件 | 复用评分/汇总区域，不额外新增首期菜单 |
| `PIP.vue` | `/performance/pip` | PIP 追踪 | `modules/performance/views/pip/index.vue` | 直接复用列表 + 详情 + 跟进弹窗结构 |
| `Promotion.vue` | `/performance/promotion` | 晋升管理 | `modules/performance/views/promotion/index.vue` | 直接复用列表 + 审批弹窗结构 |
| `Salary.vue` | `/performance/salary` | 薪资管理 | `modules/performance/views/salary/index.vue` | 复用统计概览 + 列表 + 详情结构，但字段以本项目文档为准 |
| `ActionPlan.vue` | `/performance/actions` | 不单独建菜单 | 无 | 行动计划能力并入 `PIP` 跟进记录与目标进度更新，不独立成首期模块 |

补充说明：

1. 驾驶舱在当前 `jixiao` 参考中没有完全对等页，因此继续以 `xuedao` 现有 `/data-center/dashboard` 入口和本项目指标口径文档为准。

## 首期页面结构结论

### 1. 评估模块

首期不保留 `jixiao` 的单一“绩效考核”总列表页作为菜单，而是拆成三个业务视角页面：

1. 我的考核
2. 已发起考核
3. 待我审批

三页复用以下组件：

- `assessment-form.vue`
- `assessment-detail.vue`
- `approval-drawer.vue`
- `score-item-table.vue`

### 2. 目标模块

目标模块以 [GoalMap.vue](/Users/shaotongli/Documents/jixiao/src/views/performance/GoalMap.vue) 为主参考，而不是以 [Goals.vue](/Users/shaotongli/Documents/jixiao/src/views/performance/Goals.vue) 为主。原因如下：

1. `GoalMap.vue` 已覆盖筛选、指标联动、进度展示和统计区，更接近首期目标闭环。
2. `Goals.vue` 更适合做简化列表页，可吸收其轻量列表表达，但不能替代首期主页面结构。

### 3. 360 环评模块

首期只保留一个菜单入口 `/performance/feedback`，但页面内部按三个区域组织：

1. 任务列表区
2. 发起/填写弹窗区
3. 汇总结果区

对应参考来源：

- 任务列表区参考 [Feedback.vue](/Users/shaotongli/Documents/jixiao/src/views/performance/Feedback.vue)
- 填写和汇总区参考 [Feedback360.vue](/Users/shaotongli/Documents/jixiao/src/views/performance/Feedback360.vue)

### 4. PIP 与行动计划

首期不单独复制 [ActionPlan.vue](/Users/shaotongli/Documents/jixiao/src/views/performance/ActionPlan.vue) 为独立菜单。行动计划拆解如下：

1. 与绩效改进直接相关的任务跟进，纳入 `PIP` 的跟进记录能力。
2. 与目标执行直接相关的任务推进，纳入“目标进度更新记录”。
3. 如果后续需要独立任务中心，作为二期增强，再评估是否独立菜单。

## 前端目录拆分规则

```text
cool-admin-vue/src/modules/performance/
  views/
    dashboard/index.vue
    my-assessment/index.vue
    initiated/index.vue
    pending/index.vue
    goals/index.vue
    indicator-library/index.vue
    feedback/index.vue
    pip/index.vue
    promotion/index.vue
    salary/index.vue
  components/
    assessment-form.vue
    assessment-detail.vue
    approval-drawer.vue
    score-item-table.vue
    feedback-task-form.vue
    feedback-submit-drawer.vue
    feedback-summary-drawer.vue
    goal-progress-drawer.vue
    pip-track-drawer.vue
    promotion-review-drawer.vue
    salary-change-drawer.vue
```

## 页面内部分层规则

### 列表页统一分为四层

1. 筛选区
2. 操作区
3. 列表区
4. 详情/动作区

### 详情和动作承载优先级

1. 首选抽屉
2. 次选弹窗
3. 只有长链路场景才新增隐藏详情页

## 不直接照搬的部分

以下内容只作为原型启发，不直接按原项目照搬：

1. `jixiao` 的视觉主题、玻璃拟态样式和独立页面壳组件
2. `jixiao` 的非 Cool 路由组织方式
3. `jixiao` 中超出首期文档定义的字段和页面入口
4. `jixiao` 中独立 `ActionPlan` 菜单
5. `jixiao` 中与当前唯一事实源冲突的枚举、字段或状态命名

## 实施要求

1. 页面结构实现时，若与本文件不一致，先更新本文件再改代码。
2. 如果未来要新增独立详情路由，必须先同步 [10-路由与菜单映射.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/10-路由与菜单映射.md)。
3. 如果未来要新增模块导航首页，必须先说明为什么现有 Cool 菜单树不足以承载。
