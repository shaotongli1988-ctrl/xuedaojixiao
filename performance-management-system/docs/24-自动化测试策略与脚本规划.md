# 自动化测试策略与脚本规划

## 文档职责

这份文档是“自动化测试策略和脚本规划”的唯一事实源。

它只负责定义：

1. 当前仓库已有测试脚本和能力边界
2. 首期允许使用的自动化测试方式
3. 首期不引入的测试框架范围
4. 绩效模块测试文件落点和命名规则
5. 各模块最小自动化验证要求

## 不负责内容

这份文档不负责定义：

- 业务测试矩阵明细
- UAT 验收步骤
- 联调环境账号和种子数据

这些内容分别以 `09`、`14` 文档为准。

## 当前仓库现实基线

### 后端

当前 [cool-admin-midway/package.json](/Users/shaotongli/Documents/xuedao/cool-admin-midway/package.json) 已存在：

1. `npm run test`
2. `npm run cov`

当前 [jest.config.js](/Users/shaotongli/Documents/xuedao/cool-admin-midway/jest.config.js) 已存在，说明仓库具备 `jest + ts-jest` 基础能力。

但 [test/README.md](/Users/shaotongli/Documents/xuedao/cool-admin-midway/test/README.md) 已明确说明：

1. `cool-admin-midway` 的自动化路由模式与 `jest` 主路径并不完全匹配
2. `jest` 不推荐作为该基座的全量主测试路径
3. 更适合做“纯业务逻辑、状态机、计算规则”的补充自动化验证

### 前端

当前 [cool-admin-vue/package.json](/Users/shaotongli/Documents/xuedao/cool-admin-vue/package.json) 已存在：

1. `npm run type-check`
2. `npm run build`
3. `npm run lint`

当前仓库没有：

1. `vitest.config.*`
2. `playwright.config.*`
3. `cypress.config.*`
4. 前端 `test` 脚本

因此前端首期自动化验证以 `type-check` 和 `build` 为主，不把新增测试框架作为首期前置条件。

## 首期总策略

首期自动化测试采用“保守收敛”策略：

1. 不新增第二套测试技术栈作为首期阻塞项
2. 优先复用当前仓库已有脚本
3. 对高风险纯逻辑补齐后端 `jest` 测试
4. 前端先以 `type-check` 和 `build` 作为自动化门禁
5. API 主流程继续以联调工具和关键手测为主

## 项目级红线

1. 任何行为变化都必须有自动化验证证据，不能只靠聊天确认、手工截图或口头说明通过。
2. 前端受影响时，`cool-admin-vue` 的 `npm run type-check` 和 `npm run build` 为必跑项。
3. 后端若涉及纯逻辑、状态机、等级计算、聚合统计、权限判断、默认值或兼容分支变化，必须补齐对应 `jest` 验证。
4. 若当前改动确实没有可稳定抽离的后端纯逻辑，允许不补 `jest`，但必须在任务卡中写清原因、替代验证方式和剩余风险。
5. 自动化验证结果必须可留痕，未留痕视为未执行。

## 首期允许使用的自动化方式

| 层级 | 首期是否纳入 | 方式 | 定位 |
| --- | --- | --- | --- |
| 后端单元测试 | 条件必须 | `cool-admin-midway` 的 `jest` | 涉及纯逻辑、状态、权限、聚合等行为变化时必须补齐 |
| 后端覆盖率 | 否，按需 | `npm run cov` | 非首期硬门槛 |
| 前端类型检查 | 是 | `cool-admin-vue` 的 `npm run type-check` | 必须通过 |
| 前端构建验证 | 是 | `cool-admin-vue` 的 `npm run build` | 必须通过 |
| 前端组件单测 | 否 | 暂不作为首期门槛 | 可二期评估 |
| E2E 自动化 | 否 | 暂不引入 Playwright/Cypress | 二期再评估 |

## 首期明确不引入的内容

以下内容不作为首期自动化测试基线：

1. `Playwright`
2. `Cypress`
3. `Vitest`
4. 自建浏览器端 E2E 测试平台
5. 为测试额外新建第二套前端或后端运行环境

原因：

1. 当前仓库没有现成配置
2. 与“严格沿用现有技术架构”原则不符
3. 首期更需要先保证模块主链落地和契约稳定

## 测试文件落点与命名规则

### 后端绩效模块

后端绩效测试文件统一落在：

- `cool-admin-midway/test/performance/`

推荐命名：

- `assessment.service.test.ts`
- `goal.service.test.ts`
- `dashboard.service.test.ts`
- `feedback.service.test.ts`
- `pip.service.test.ts`
- `promotion.service.test.ts`
- `salary.service.test.ts`

补充规则：

1. 优先测 service 层纯逻辑，不优先测自动化路由和复杂启动装配。
2. 若某逻辑依赖数据库查询，优先抽纯函数或小型 service 方法后再测。
3. 不为了测试强行改动整体架构，但允许把复杂计算逻辑从控制器中抽离。

### 前端绩效模块

前端首期没有单独测试文件硬要求。

首期前端自动化验证统一通过：

1. `cool-admin-vue npm run type-check`
2. `cool-admin-vue npm run build`

如果后续二期引入前端测试框架，再单独定义前端测试目录规范。

## 各模块最小自动化验证要求

### 模块 0：模块骨架与基础配置

必须通过：

1. 前端 `npm run type-check`
2. 前端 `npm run build`

### 模块 1：评估单

必须补齐至少 1 组后端 `jest` 测试，覆盖：

1. `totalScore = sum(score * weight) / sum(weight)`
2. 等级边界 `S/A/B/C`
3. `draft -> submitted`
4. `submitted -> approved / rejected`
5. 非法状态流转拒绝

同时必须通过：

1. 前端 `npm run type-check`
2. 前端 `npm run build`

### 模块 2：目标地图

必须补齐至少 1 组后端 `jest` 测试，覆盖：

1. `progressRate` 计算
2. `targetValue > 0`
3. `startDate <= endDate`
4. `completed` 后不可继续更新

同时必须通过：

1. 前端 `npm run type-check`
2. 前端 `npm run build`

### 模块 3：驾驶舱基础聚合

必须补齐至少 1 组后端 `jest` 测试，覆盖：

1. `averageScore`
2. `pendingApprovalCount`
3. `goalCompletionRate`
4. `departmentDistribution`
5. `gradeDistribution`

同时必须通过：

1. 前端 `npm run type-check`
2. 前端 `npm run build`

### 模块 4：指标库

若改动涉及下面语义，必须补齐至少 1 组后端 `jest`，优先覆盖：

1. 重复编码校验
2. 启用/禁用状态过滤

若当前阶段仅落标准 CRUD 壳层，且无独立纯逻辑，允许暂不补 `jest`，但必须在任务卡说明原因。

同时必须通过：

1. 前端 `npm run type-check`
2. 前端 `npm run build`

### 模块 5：360 环评

必须补齐至少 1 组后端 `jest` 测试，覆盖：

1. 单任务同评价人只能提交一次
2. 截止后不可补交
3. 汇总分按简单平均分计算

同时必须通过：

1. 前端 `npm run type-check`
2. 前端 `npm run build`

### 模块 6：PIP

必须补齐至少 1 组后端 `jest` 测试，覆盖：

1. `draft -> active`
2. `active -> completed / closed`
3. 非法状态拒绝
4. 独立创建时必须有原因

同时必须通过：

1. 前端 `npm run type-check`
2. 前端 `npm run build`

### 模块 7：晋升管理

必须补齐至少 1 组后端 `jest` 测试，覆盖：

1. `draft -> reviewing`
2. `reviewing -> approved / rejected`
3. 非法状态拒绝
4. 独立创建时必须有原因

同时必须通过：

1. 前端 `npm run type-check`
2. 前端 `npm run build`

### 模块 8：薪资管理

必须补齐至少 1 组后端 `jest` 测试，覆盖：

1. `draft -> confirmed`
2. `confirmed -> archived`
3. `confirmed` 后只能通过 `changeAdd` 调整
4. `beforeAmount / afterAmount` 计算正确

同时必须通过：

1. 前端 `npm run type-check`
2. 前端 `npm run build`

### 模块 9：交付收口

必须输出自动化执行结果摘要，至少包含：

1. 后端执行了哪些 `jest` 文件
2. 前端 `type-check` 结果
3. 前端 `build` 结果
4. 哪些模块未补自动化测试及原因

## 执行命令基线

### 后端

全量基础命令：

```bash
cd /Users/shaotongli/Documents/xuedao/cool-admin-midway
npm run test
```

按文件执行示例：

```bash
cd /Users/shaotongli/Documents/xuedao/cool-admin-midway
npx jest test/performance/assessment.service.test.ts --runInBand
```

### 前端

```bash
cd /Users/shaotongli/Documents/xuedao/cool-admin-vue
npm run type-check
npm run build
```

## 交付时必须输出的测试证据

1. 后端 `jest` 执行命令
2. 后端通过的测试文件列表
3. 前端 `type-check` 结果
4. 前端 `build` 结果
5. 未执行项和剩余风险说明

## 二期主题 1：`8001` 统一治理自动化验证入口

本节定义并记录主题 1 阶段 1 的真实自动化验证与 GUI 点测留痕。

### 当前仓库已存在的验证入口

1. backend 默认启动口径：
   - `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && npm run dev`
2. 标准 smoke 入口：
   - `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && npm run smoke:stage2:performance`
3. 显式 `8001` smoke 入口：
   - `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && STAGE2_SMOKE_BASE_URL=http://127.0.0.1:8001 npm run smoke:stage2:performance`
4. frontend 自动化入口：
   - `cd /Users/shaotongli/Documents/xuedao/cool-admin-vue && npm run type-check`
   - `cd /Users/shaotongli/Documents/xuedao/cool-admin-vue && npm run build`

### 主题 1 阶段 1 通过前必须补齐的留痕

1. `STAGE2_SMOKE_BASE_URL=http://127.0.0.1:8001 npm run smoke:stage2:performance` 的真实执行结果。
2. `cool-admin-vue` 在 `/dev -> 8001` 口径下的 `type-check` 与 `build` 留痕。
3. `9000 -> /dev -> 8001` 最低 GUI 点测结果。
4. 若任一项缺失，主题 1 不能按 [20-阶段门禁与完成定义.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/20-阶段门禁与完成定义.md) 进入“已完成文档同步”。

### 当前仓库状态说明

1. `8001` 标准 smoke 已真实通过：
   - `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && npm run smoke:stage2:performance`
   - 结果：`PASS 41 / FAIL 0 / SKIP 0 / Conclusion: PASSED`
2. `8006` 回退 smoke 已真实通过：
   - `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && npm run smoke:stage2:performance:8006`
   - 结果：`PASS 41 / FAIL 0 / SKIP 0 / Conclusion: PASSED`
3. `cool-admin-vue` 在 `/dev -> 8001` 口径下的自动化门禁已通过：
   - `cd /Users/shaotongli/Documents/xuedao/cool-admin-vue && corepack pnpm run type-check`
   - `cd /Users/shaotongli/Documents/xuedao/cool-admin-vue && corepack pnpm run build`
4. `9000 -> /dev -> 8001` 最低 GUI 点测已补齐：
   - `hr_admin`：dashboard 与 `goals / indicator-library / pip / promotion / salary` 页面正常
   - `manager_rd`：dashboard 页面正常，页面文本未出现 `销售中心`
   - `employee_platform`：进入“我的考核”正常，无 dashboard 菜单；直达 dashboard 为 `404`
5. GUI 点测产物留痕：
   - `/tmp/codex-8001-gui/report.json`
   - `/tmp/codex-8001-gui/*.png`
6. 上述结果是主题 1 在 2026-04-17 的历史收口证据。
7. 截至 2026-04-18，shared latest 已正式回退到 `8006`；若后续再次尝试把 current latest 提升回 `8001`，必须重新补齐同一组 smoke、前端门禁和 GUI 留痕，不能继承本段通过结论。

## 二期主题 4：自动建议引擎测试矩阵与任务拆分冻结基线

本节只冻结自动建议引擎的“验证与拆分策略”，不代表该主题已可进入设计或实现。

### 当前窗口结论

1. 当前唯一结论：`自动建议引擎测试矩阵与任务拆分已冻结`
2. 当前主题总体准入结论以阶段 0 最终验收卡为准，已收敛为 `自动建议引擎已完成阶段0冻结，可进入开发`。
3. 本节继续只负责“验证与拆分策略”，不替代 `04 / 05 / 06 / 12` 的正式事实源。

### 后续开发拆分顺序

1. 串行步骤 1：冻结触发来源、建议对象、触发阈值和非目标边界。
2. 串行步骤 2：冻结建议状态、人工处理动作、角色责任和审计责任。
3. 串行步骤 3：冻结接口、字段、脱敏、错误语义，并同步回写 `04 / 05 / 06 / 07 / 09 / 12`。
4. 串行步骤 4：实现建议记录模型、幂等去重和基础校验骨架。
5. 并行步骤 5：
   - 后端只读链路：列表 / 详情 / 数据范围裁剪
   - 前端只读链路：列表页、空态、无权限态、错误态
   - 自动化脚本草拟：`jest`、smoke、权限回归
6. 串行步骤 6：实现人工处理动作、正式单据联动、撤销链路和审计写回。
7. 串行步骤 7：完成最小自动化验证、真实 smoke、权限回归与文档收口。

### 串行约束

1. 规则冻结未完成前，不允许并行做接口、状态机或实现拆分。
2. 状态 / 权限 / 审计责任未冻结前，不允许并行做动作接口或动作 UI。
3. 建议记录模型未稳定前，不允许把只读链路和动作链路混合并行实现。
4. 最终回归必须在所有实现合并后统一串行执行，不能把局部通过写成主题关闭。

### 可并行边界

1. 在接口 / 字段 / 脱敏冻结完成后，只读后端、只读前端和自动化脚本草拟可以并行。
2. 在动作契约冻结完成后，动作前端交互和后端动作实现可以并行，但必须共用同一状态机和审计口径。

### 最小自动化验证门槛

1. 后端最小 `jest` 必须覆盖：
   - 规则命中与不命中
   - 重复触发幂等
   - 权限拒绝
   - 非法状态流转拒绝
   - 撤销 / 审计字段校验
2. 前端若新增建议列表或动作页，必须通过：
   - `cd /Users/shaotongli/Documents/xuedao/cool-admin-vue && corepack pnpm run type-check`
   - `cd /Users/shaotongli/Documents/xuedao/cool-admin-vue && corepack pnpm run build`
3. 真实 smoke 至少覆盖：
   - 有权角色查询建议列表成功
   - 无权角色被拒绝
   - 合法人工处理成功
   - 非法重复处理被拒绝
4. 文档阶段当前不执行上述实现类验证；进入实现后，缺任一项都不得按主题完成收口。

## 二期主题 5：自动审批流测试矩阵与任务拆分冻结基线

本节只冻结自动审批流的“验证与拆分策略”，不代表该主题已可进入设计或实现。

### 当前窗口结论

1. 当前唯一结论：`自动审批流测试矩阵与任务拆分已冻结`
2. 当前主题主卡已完成阶段 0 冻结，可进入开发。
3. 本节继续只负责“验证与拆分策略”，不替代 `03 / 04 / 05 / 06 / 12 / 15` 的正式事实源。

### 后续开发拆分顺序

1. 串行步骤 1：冻结首批适用业务对象、非目标和与现有手工审批主链的兼容边界。
2. 串行步骤 2：冻结审批节点模型、状态机、审批人解析、驳回 / 转办 / 超时 / 回退语义。
3. 串行步骤 3：冻结接口、字段、存储、审计、回滚和兼容迁移口径，并同步回写 `03 / 04 / 05 / 06 / 09 / 15`。
4. 串行步骤 4：实现审批配置模型、审批实例模型和基础校验骨架。
5. 并行步骤 5：
   - 后端只读链路：审批实例查询、节点详情、审计详情
   - 前端只读链路：审批流查看页、空态、无权限态、错误态
   - 自动化脚本草拟：`jest`、smoke、权限回归、兼容回退回归
6. 串行步骤 6：实现审批动作链路、兼容回退、转办 / 驳回 / 超时处理和审计写回。
7. 串行步骤 7：完成最小自动化验证、真实 smoke、权限回归、兼容回退回归与文档收口。

### 串行约束

1. 主链兼容边界未冻结前，不允许并行做状态机、接口或实现拆分。
2. 节点模型 / 审批人解析未冻结前，不允许并行做动作接口或动作 UI。
3. 接口 / 存储 / 回滚口径未冻结前，不允许进入任何真实实现。
4. 审批实例模型与兼容回退链路必须先稳定，之后才能实现动作链路，不能并行推进。

### 可并行边界

1. 在接口 / 字段 / 审计 / 回滚口径冻结完成后，只读后端、只读前端和自动化脚本草拟可以并行。
2. 在动作契约冻结完成后，动作前端交互和后端动作实现可以并行，但必须共用同一状态机、同一审批人解析口径和同一审计模型。

### 最小自动化验证门槛

1. 后端最小 `jest` 必须覆盖：
   - 节点模型命中与不命中
   - 审批人解析成功与失败
   - 权限拒绝
   - 非法状态流转拒绝
   - 兼容回退与审计字段校验
2. 前端若新增审批流配置页、查看页或动作页，必须通过：
   - `cd /Users/shaotongli/Documents/xuedao/cool-admin-vue && corepack pnpm run type-check`
   - `cd /Users/shaotongli/Documents/xuedao/cool-admin-vue && corepack pnpm run build`
3. 真实 smoke 至少覆盖：
   - 合法审批实例查询成功
   - 非法审批人被拒绝
   - 合法节点推进成功
   - 兼容回退到手工审批主链成功
4. 文档阶段当前不执行上述实现类验证；进入实现后，缺任一项都不得按主题完成收口。

## 二期主题 6：跨模块驾驶舱测试矩阵与任务拆分冻结基线

本节冻结跨模块驾驶舱进入实现前必须遵守的“验证与拆分策略”，并与主题 6 已完成的阶段 0 冻结正文保持一致。

### 当前窗口结论

1. 当前唯一结论：`跨模块驾驶舱测试矩阵与任务拆分已冻结`
2. 当前主题 6 已完成阶段 0 冻结，可进入实现；实现必须以 `04 / 06 / 09 / 11 / 12 / 16 / 30` 的正式事实源为准。
3. 本节继续只负责“验证与拆分策略”，不替代主文档中的接口、权限、口径与缓存正文。

### 后续开发拆分顺序

1. 串行步骤 1：按已冻结来源快照契约，实现招聘 / 培训 / 会议三域的读取适配层。
2. 串行步骤 2：实现跨模块聚合接口、错误语义、更新时间字段和缓存 key 维度。
3. 串行步骤 3：实现后端只读聚合骨架和基础校验。
4. 串行步骤 4：实现权限裁剪、延迟提示和不可用态。
5. 并行步骤 5：
   - 后端读链路：跨模块聚合查询、来源状态拼装、缓存读写
   - 前端读链路：聚合卡片 / 图表、空态、无权限态、错误态、延迟提示
   - 自动化脚本草拟：最小 smoke、权限回归、数据延迟回归、缓存一致性回归
6. 串行步骤 6：完成权限回归、数据延迟回归、缓存一致性回归，并补齐文档与交付留痕。
7. 串行步骤 7：执行主题 6 最终冻结复核，确认无未关闭 `P0 / P1` 后再决定是否进入实现。

### 串行约束

1. 权限边界、组织映射和缓存维度未落地前，不允许进入任何真实聚合实现。
2. 聚合接口与新鲜度字段未落地前，不允许并行推进前端页面、smoke 脚本和缓存实现。
3. 权限回归、数据延迟回归和缓存一致性回归必须在聚合读模型稳定后统一串行执行，不能把局部通过写成主题关闭。

### 可并行边界

1. 在“聚合接口 / 权限边界 / 缓存维度”冻结完成后，后端读链路、前端只读展示和自动化脚本草拟可以并行。
2. 由于“来源快照契约”已冻结，招聘 / 培训 / 会议三域的读适配层现在可以并行，但最终仍要回到统一聚合接口和统一权限 / 缓存回归做收口。

### 最小自动化验证门槛

1. 后端最小 `jest` 必须覆盖：
   - 来源快照存在 / 缺失 / 延迟三类结果
   - 聚合计算在空值、分母为 `0`、单域缺数下的稳定输出
   - 权限拒绝
   - 缓存 key 维度隔离
   - 旧缓存回退时的越权保护与更新时间字段校验
2. 前端若新增跨模块驾驶舱页面或卡片，必须通过：
   - `cd /Users/shaotongli/Documents/xuedao/cool-admin-vue && corepack pnpm run type-check`
   - `cd /Users/shaotongli/Documents/xuedao/cool-admin-vue && corepack pnpm run build`
3. 真实 smoke 至少覆盖：
   - 有权角色查询聚合结果成功
   - 员工访问被拒绝
   - 单域延迟时显示延迟标记
   - 角色 / 周期 / 组织切换后不串缓存
4. 文档阶段当前不执行上述实现类验证；进入实现后，缺任一项都不得按主题完成收口。

## 例外规则

1. 如果某模块当前阶段没有可稳定抽离的纯逻辑，允许暂不补 `jest`，但必须在任务卡中写清原因。
2. 若未补 `jest`，不能替代前端 `type-check/build` 和主路径联调。
3. 若未来确需引入新测试框架，必须先回写本文件、`13` 版本记录和流程门禁文档，再做依赖变更。

## 二期主题 7：培训课程管理测试矩阵与任务拆分冻结基线

### 当前窗口结论

1. 当前唯一结论：`培训课程管理测试矩阵与任务拆分已冻结`
2. 主题7已完成阶段0冻结，本节继续作为后续实现的验证与拆分策略基线。

### 后续开发拆分顺序

1. 串行步骤 1：冻结课程字段、状态与权限边界。
2. 串行步骤 2：实现 `course` 只读 / 写入接口骨架。
3. 并行步骤 3：
   - 后端课程 CRUD
   - 前端课程列表 / 详情 / 编辑页
   - 报名列表只读查询
4. 串行步骤 4：补齐权限回归、状态回归与文档收口。

### 最小自动化验证门槛

1. 后端最小 `jest` 必须覆盖课程状态流、删除限制和权限拒绝。
   - 额外覆盖 `published` 仅允许修改 `description / endDate`
2. 前端必须通过：
   - `corepack pnpm run type-check`
   - `corepack pnpm run build`
3. 真实 smoke 至少覆盖：
   - `HR` 课程主链成功
   - 经理只读成功
   - 员工拒绝

## 二期主题 8：招聘面试管理测试矩阵与任务拆分冻结基线

### 当前窗口结论

1. 当前唯一结论：`招聘面试管理测试矩阵与任务拆分已冻结`
2. 主题8已完成阶段0冻结，本节继续作为后续实现的验证与拆分策略基线。

### 后续开发拆分顺序

1. 串行步骤 1：冻结面试字段、状态与权限边界。
2. 串行步骤 2：实现 `interview` 只读 / 写入接口骨架。
3. 并行步骤 3：
   - 后端面试 CRUD
   - 前端面试列表 / 详情 / 编辑页
4. 串行步骤 4：补齐部门树权限回归与状态回归。

### 最小自动化验证门槛

1. 后端最小 `jest` 必须覆盖面试状态流、部门树权限和删除限制。
   - 额外覆盖终态不可补录分数 / 备注
2. 前端必须通过：
   - `corepack pnpm run type-check`
   - `corepack pnpm run build`
3. 真实 smoke 至少覆盖：
   - `HR` 创建 / 编辑成功
   - 经理部门树范围成功
   - 员工拒绝

## 二期主题 9：会议管理测试矩阵与任务拆分冻结基线

### 当前窗口结论

1. 当前唯一结论：`会议管理测试矩阵与任务拆分已冻结`
2. 主题9已完成阶段0冻结，本节继续作为后续实现的验证与拆分策略基线。

### 后续开发拆分顺序

1. 串行步骤 1：冻结会议字段、状态、签到与权限边界。
2. 串行步骤 2：实现 `meeting` 只读 / 写入接口骨架。
3. 并行步骤 3：
   - 后端会议 CRUD + 签到
   - 前端会议列表 / 详情 / 编辑页
4. 串行步骤 4：补齐签到状态回归、权限回归与文档收口。

### 最小自动化验证门槛

1. 后端最小 `jest` 必须覆盖会议状态流、签到合法性和权限拒绝。
   - 额外覆盖会议级签到口径和参与人明细不返回
2. 前端必须通过：
   - `corepack pnpm run type-check`
   - `corepack pnpm run build`
3. 真实 smoke 至少覆盖：
   - `HR` / 经理会议主链成功
   - 非 `in_progress` 状态签到拒绝
   - 员工拒绝
