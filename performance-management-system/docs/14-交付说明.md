# 交付说明

## 文档职责

这份文档是“交付和上线准备”的唯一事实源。

它只负责定义：

1. 部署前提
2. 初始化步骤
3. 菜单和权限初始化
4. 联调入口
5. 验收入口
6. 已知限制

## 不负责内容

这份文档不负责定义：

- 业务需求边界
- 表结构设计
- 接口字段详情

## 本地启动顺序

### 环境前提

1. Node 版本满足 [cool-admin-midway/package.json](/Users/shaotongli/Documents/xuedao/cool-admin-midway/package.json) 中 `>=18.0.0` 要求。
2. 前后端依赖分别安装在：
   - [cool-admin-midway](/Users/shaotongli/Documents/xuedao/cool-admin-midway)
   - [cool-admin-vue](/Users/shaotongli/Documents/xuedao/cool-admin-vue)
3. 数据库、基础配置和现有 `xuedao` 本地环境按原项目已有方式准备，不额外新造环境体系。
4. 阶段 2 联调前，`cool-admin-midway` 必须安装完整 Midway 运行时依赖，并能基于现有本地配置完成启动复验；仅安装 `cool-admin-midway/.shared-deps/js/` 内的测试依赖，不视为联调环境就绪。
5. 本地 `npm run dev` 读取的数据库配置以 [cool-admin-midway/src/config/config.local.ts](/Users/shaotongli/Documents/xuedao/cool-admin-midway/src/config/config.local.ts) 为准；`.env` 不是这份交付说明定义的当前阶段 2 硬阻塞项，也不是唯一事实源。
6. 截至 2026-04-17，本地 latest backend 联调口径要求“复用现有库结构”，不能把 TypeORM `synchronize` 当作 fresh 实例启动前提；最新验证基线以 [cool-admin-midway/src/config/config.local.ts](/Users/shaotongli/Documents/xuedao/cool-admin-midway/src/config/config.local.ts) 当前配置为准。

### 本地启动建议顺序

0. 若环境尚未执行最新 schema migration，先在 [cool-admin-midway](/Users/shaotongli/Documents/xuedao/cool-admin-midway) 执行：
   - `npm run migration:up`
1. 先启动后端：
   - 在 [cool-admin-midway](/Users/shaotongli/Documents/xuedao/cool-admin-midway) 执行 `npm run dev`
2. 再启动前端：
   - 在 [cool-admin-vue](/Users/shaotongli/Documents/xuedao/cool-admin-vue) 执行 `npm run dev`
3. 开发自检时至少执行：
   - 前端 `npm run type-check`
   - 前端 `npm run build`
   - 后端按本次改动范围执行 `npm run test` 或最小接口自测

## 交付物清单

首期绩效模块交付必须至少包含以下内容：

1. 后端 `performance` 模块代码
2. 前端 `performance` 模块页面与复用组件
3. 菜单和权限初始化数据
4. 对应唯一事实源文档更新
5. 测试和验证记录
6. 交付限制和已知未开放能力说明

本轮与角色偏好持久化直接相关的新增交付物：

1. 数据库 migration：
   - [20260423090000-add-base-user-active-performance-persona.mjs](/Users/shaotongli/Documents/xuedao/cool-admin-midway/scripts/migrations/20260423090000-add-base-user-active-performance-persona.mjs)
2. 后端 persona 偏好读写闭环：
   - `GET /admin/base/comm/performanceAccessContext`
   - `POST /admin/base/comm/performanceAccessContext/activePersona`
3. `Web` 工作台 persona 切换改为走专用保存接口。
4. `cool-uni` 启动补同步 `performanceAccessContext`，确保与 `Web` 共用同一数据库值。

## 菜单和权限初始化方式

首期绩效模块菜单和权限初始化固定按现有 Cool 基座方式执行：

1. 不新增新的权限初始化机制。
2. 绩效模块菜单数据按 `base/menu.json` 同结构组织。
3. 按钮权限通过菜单树 `type = 2` 节点导入。
4. 前端继续使用现有动态菜单装配逻辑加载菜单。
5. 交付时只需要补：
   - 绩效模块菜单数据
   - 绩效模块权限键
   - 与角色的授权关系

当前已落地的唯一事实源：

1. 菜单树和权限键以 [cool-admin-midway/src/modules/base/menu.json](/Users/shaotongli/Documents/xuedao/cool-admin-midway/src/modules/base/menu.json) 为准。
2. 阶段 2 本地联调脚本以 [cool-admin-midway/scripts/seed-stage2-performance.mjs](/Users/shaotongli/Documents/xuedao/cool-admin-midway/scripts/seed-stage2-performance.mjs) 为准；脚本会按现有菜单树同步模块 1 / 2 主链、模块 3 dashboard、模块 4 `HR` 指标库入口，以及 `采购&资产 -> 物资管理 Phase 1` 四个材料路由的菜单授权关系。

## 联调入口

### 运行态门禁

1. 截至 2026-04-18，阶段 2 smoke 在打业务接口前，必须先请求：
   - `/admin/base/open/runtimeMeta`
2. 运行态门禁固定校验：
   - `gitHash`
   - `sourceHash`
   - `port`
   - `seedMeta.version`
   - `seedMeta.scopes`
3. 阶段 2 seed 脚本 [seed-stage2-performance.mjs](/Users/shaotongli/Documents/xuedao/cool-admin-midway/scripts/seed-stage2-performance.mjs) 会同步回写 `base_sys_param.keyName = stage2.performance.seedMeta`。
4. 若 `runtimeMeta` 与当前仓库工作树或期望 seed 范围不一致，smoke 必须立即失败，不再继续进入登录或业务接口校验。
5. 这条门禁只负责识别“旧实例 / 错 seed / 错端口 / 工作树漂移”，不替代业务 smoke 本身。
6. 若需要统一重置 latest backend，可执行：
   - `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && npm run latest:reset:verify`
7. 该命令固定串行执行：
   - `local-db start`
   - `npm run build`
   - `dev-port-guard` 释放 latest 端口
   - `npm start`
   - `runtimeMeta` 等待 fresh 实例就绪
   - `seed-stage2-performance.mjs`
   - `smoke-stage2-performance.mjs`
8. 如需先清理同仓库其他端口实例，可使用：
   - `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && LATEST_RESET_KILL_ALL=1 npm run latest:reset:verify`
9. 自 2026-04-21 起，若环境已经执行过 `20260421110000-backfill-base-super-admin-login-link` 这条超管回填 migration，发布前必须额外执行：
   - `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && npm run audit:base:super-admin-link`
10. 该审计脚本只负责识别“旧版 migration 是否可能删掉 admin 历史角色绑定、审批指针是否仍指向 admin、当前是否保留超管角色绑定”，不会自动修复历史数据；若输出 `FAIL`，必须结合备份或人工审计处理后才能放行。

### 后端联调入口

首期绩效模块后端接口统一挂在：

- `/admin/performance/*`

具体接口以 [04-API设计.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/04-API设计.md) 为准。

### 前端联调入口

首期前端入口统一落在：

- [cool-admin-vue/src/modules/performance](/Users/shaotongli/Documents/xuedao/cool-admin-vue/src/modules/performance)

页面结构和拆分规则以 [18-页面结构与原型映射.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/18-页面结构与原型映射.md) 为准。

## 2026-04-17 当前提测入口

### 当前推荐提测口径

1. 前端页面入口：`http://127.0.0.1:9000`
2. 前端代理口径：`/dev -> <当前显式指定的 latest backend 实例>`
3. 后端接口入口：`<当前显式指定的 latest backend 实例>/admin/performance/*`

说明：

1. 自 2026-04-19 起，标准 smoke 不再内置默认 backend 端口；必须显式传入目标实例。
2. 自 2026-04-19 起，前端 `/dev` 代理也不再回退到历史 `8006`；启动 [cool-admin-vue](/Users/shaotongli/Documents/xuedao/cool-admin-vue) 前必须显式设置 `COOL_ADMIN_PROXY_TARGET` 或 `VITE_DEV_PROXY_TARGET`。
3. 自 2026-04-19 起，`cool-admin-midway` 的 `latest:reset:verify` 也不再默认回退到 `8006`；必须显式传 `LATEST_RESET_PORT` 或 `--port`。
4. 若实际提测或联调使用其他已验证实例，只要该实例已加载当前实现并完整重放 `seed / smoke / 必要 GUI 留痕`，同样可作为有效提测入口。
5. `8002 / 9003` 只保留为模块 4 / 6 的历史联调收口证据，不再作为当前提测首选入口。
6. `8001` 仍保留为主题 1 历史治理目标与诊断入口，但不再作为 current latest 默认入口。

## 当前首期真实交付覆盖（2026-04-17）

1. 当前首期业务交付范围以模块 1、2、3、4、5、6、7、8 任务卡为准：
   - 模块 1：评估单主链
   - 模块 2：目标地图
   - 模块 3：驾驶舱基础聚合
   - 模块 4：指标库
   - 模块 5：360 环评
   - 模块 6：PIP
   - 模块 7：晋升管理
   - 模块 8：薪资管理
2. 模块 9 不新增独立业务范围，只负责驾驶舱最终聚合、阶段 5 联调收口和交付事实源同步；当前已完成收口并关闭任务卡。
3. 上述模块的最小自动化验证、真实联调和阶段结论以各自任务卡与 [2026-04-17-模块9-驾驶舱最终聚合与交付收口.md](/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-17-模块9-驾驶舱最终聚合与交付收口.md) 为准。
4. 模块 0 任务卡当前已关闭；此前 clean-start 日志噪音已由 latest backend fresh 启动与标准 smoke 证据关闭。当前有效本地联调基线已切换为 latest backend `8006` 和 latest frontend `9000`。

## 当前有效联调与验收口径（2026-04-17）

1. 当前文档记录的联调入口必须显式写明目标 backend 实例，不再默认推断固定端口。
2. 当前文档记录的前端验收入口仍为 `http://127.0.0.1:9000`，但 `/dev` 必须指向本次显式指定并已验证的 latest backend 实例。
3. 本地环境前提仍以 [cool-admin-midway/src/config/config.local.ts](/Users/shaotongli/Documents/xuedao/cool-admin-midway/src/config/config.local.ts) 为准；当前口径是“复用现有 schema、不依赖 TypeORM auto-sync”。
4. 历史上用于模块级关单的等效实例口径仍然有效，但不再作为当前阶段 5 的整体交付入口：
   - 模块 6 曾接受 `9003 -> 8002`
   - 模块 7 曾在 `18005 / 18006` 完成真实联调与标准 smoke
   - 模块 8 曾接受 `8002 / 18006`
5. 若实际联调切换到其他端口或环境，必须先重放等效 seed / 菜单授权，再重跑 smoke 或最小人工点测；端口号本身不是放行条件，真实验证留痕才是。

### 标准 smoke 口径（2026-04-19）

1. 标准命令固定写法为：
   - `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && STAGE2_SMOKE_BASE_URL=http://127.0.0.1:<目标端口> npm run smoke:stage2:performance`
2. 若未显式传入 `STAGE2_SMOKE_BASE_URL` 或 `--base-url`，脚本应直接失败，而不是回退到历史端口。
3. 若执行 `npm run latest:reset:verify`，固定写法为：
   - `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && LATEST_RESET_PORT=<目标端口> npm run latest:reset:verify`
4. 前端本地联调固定写法为：
   - `cd /Users/shaotongli/Documents/xuedao/cool-admin-vue && COOL_ADMIN_PROXY_TARGET=http://127.0.0.1:<目标端口> npm run dev`
5. 当前是否放行，取决于“目标实例是否与当前工作树、runtimeMeta 和 seed 范围一致”，不取决于端口号本身。

## 二期主题 1：共享默认 `8001` 口径统一治理冻结（2026-04-17）

说明：

1. 本节只定义二期主题 1 的冻结目标，不改变本文件上文已经定义的“当前默认示例入口”。
2. 主题 1 已完成阶段 1 收口；但截至 2026-04-18，shared latest 已正式回退为 `8006 / 9000 -> /dev -> 8006`，`8001` 退回为历史治理目标与诊断入口。

### 当前基线与目标基线

1. 当前首期默认 backend latest 示例入口：`http://127.0.0.1:8006`
2. 当前首期默认 frontend latest 示例入口：`http://127.0.0.1:9000 -> /dev -> http://127.0.0.1:8006`
3. 当前 `8001` 定义：主题 1 的历史治理目标与诊断口径，不是 current latest 通过依据
4. 当前稳定 backend 默认配置示例：`http://127.0.0.1:8006`
5. 当前稳定 backend latest 配置示例：`http://127.0.0.1:8006`
6. 当前稳定 frontend 联调 / 验收配置示例：`http://127.0.0.1:9000 -> /dev -> http://127.0.0.1:8006`

### 阶段 1 当前仓库证据状态

1. 当前仓库已存在的实现证据：
   - backend 默认端口基线当前已回退为 `8006`，见 [config.default.ts](/Users/shaotongli/Documents/xuedao/cool-admin-midway/src/config/config.default.ts) 与 [port.ts](/Users/shaotongli/Documents/xuedao/cool-admin-midway/src/comm/port.ts)
   - backend `npm run dev` 已接入 [dev-port-guard.mjs](/Users/shaotongli/Documents/xuedao/cool-admin-midway/scripts/dev-port-guard.mjs)，当前用于阻断同仓库 `8006 / 其他端口` 漂移，见 [package.json](/Users/shaotongli/Documents/xuedao/cool-admin-midway/package.json)
   - 标准 smoke 脚本默认入口当前已定义为 `http://127.0.0.1:8006`，见 [smoke-stage2-performance.mjs](/Users/shaotongli/Documents/xuedao/cool-admin-midway/scripts/smoke-stage2-performance.mjs)
2. 当前仓库仍未统一的实现证据：
   - 无；backend 默认端口、smoke 默认入口与 frontend `/dev` 代理已统一回 `8006`
3. 当前仓库未见的验证留痕：
   - 已见 `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && npm run smoke:stage2:performance` 的通过记录，结果 `PASS 41 / FAIL 0 / SKIP 0 / Conclusion: PASSED`
   - 已见 `http://127.0.0.1:9000 -> /dev -> http://127.0.0.1:8006` 的历史 GUI 点测通过记录，结果见 [2026-04-17-模块9-驾驶舱最终聚合与交付收口.md](/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-17-模块9-驾驶舱最终聚合与交付收口.md)
   - 已见二期主题 1 阶段 1 主实现、验证和最终验收任务卡
4. 因此截至当前仓库状态：
   - 主题 1 的阶段 1 文档同步已完成
   - 当前仓库默认 current latest 示例仍记为 `8006 / 9000 -> /dev -> 8006`
   - 但阶段放行不再要求必须使用该端口；任何已加载当前实现并补齐等效验证留痕的实例都可作为有效入口

### 改动范围

1. 统一 backend 默认入口、latest 入口和 smoke 入口的端口口径。
2. 统一 frontend `/dev` 代理所指向的 latest backend 入口。
3. 统一交付说明、回滚说明、任务卡和验收口径中的运行入口表述。
4. 明确历史端口的退场规则和只保留为历史证据的范围。

### 不改范围

1. 不修改任何绩效业务 API、状态机、权限矩阵或数据权限规则。
2. 不修改共享鉴权、共享菜单、共享权限实现本身的业务语义。
3. 不扩展 seed 的业务样例范围，不降低现有 smoke 覆盖标准。
4. 不把 `8002 / 9003 / 18005 / 18006` 重新定义为当前默认入口。

### 入口定义

1. 默认入口定义：
   - 当前 shared latest backend 默认配置示例为 `http://127.0.0.1:8006`
2. latest 入口定义：
   - 当前 latest backend 配置示例为 `http://127.0.0.1:8006`
3. 本地开发入口：
   - backend：`cool-admin-midway npm run dev` 默认配置示例为 `8006`
   - frontend：`http://127.0.0.1:9000 -> /dev -> http://127.0.0.1:8006`
4. 联调入口：
   - 默认示例：`http://127.0.0.1:8006/admin/performance/*`
   - 等效口径：任意已加载当前实现并完成等效 seed / smoke / 必要 GUI 留痕的目标实例
5. smoke 执行入口：
   - 默认示例：`cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && STAGE2_SMOKE_BASE_URL=http://127.0.0.1:8006 npm run smoke:stage2:performance`
   - 若实际联调实例不是 `8006`，应替换为目标实例后执行同等验证
6. 回滚后的入口：
   - 若后续再次尝试把 `8001` 提升回 current latest 且失败，应回退到最近一组已验证稳定入口
   - 当前仓库记录的默认回退示例仍为 `http://127.0.0.1:8006` 与 `http://127.0.0.1:9000 -> /dev -> http://127.0.0.1:8006`

### 历史入口作废规则

1. `8002 / 9003 / 18005 / 18006` 只保留为历史模块关单证据，不再自动继承为当前默认入口。
2. `8006` 当前仍是仓库默认 current latest 配置示例。
3. `8001` 当前不得继续写成已生效默认入口，除非后续重新完成 shared runtime 治理与验收。

### 验收通过标准

1. 目标联调实例必须已加载 latest backend，实现口径与当前仓库最新实现一致。
2. 对目标联调实例执行等效 smoke，结果必须达到通过门槛。
3. 对目标联调入口执行最低 GUI 点测矩阵，或复用同一实例上的既有留痕。
4. 本文件必须同时写清“仓库默认示例入口”和“已验证实例可等效放行”的规则，并把 `8001` 收敛为历史治理目标 / 诊断入口。

### 当前文档同步判断（截至当前仓库）

1. 当前可以把 `8006` 写成“仓库默认示例入口”，因为 [proxy.ts](/Users/shaotongli/Documents/xuedao/cool-admin-vue/src/config/proxy.ts) 默认已指向 `8006`，且仓库已留痕 `8006` smoke 与历史 GUI 通过结果。
2. 当前可以确认：
   - backend 默认口径和 smoke 默认口径已经统一到 `8006`
   - frontend latest 验证口径已经统一到 `9000 -> /dev -> 8006`
3. 因此主题 1 当前文档口径更新为：
   - 仓库默认 current latest 示例：`8006 / 9000 -> /dev -> 8006`
   - 历史治理 / 诊断入口：`8001 / 9000 -> /dev -> 8001`
   - 等效放行规则：已加载当前实现并补齐同等验证证据的其他实例也可作为有效入口
   - 阶段 1 文档同步状态：`done`

### 失败回退标准

1. 若后续再次尝试提升 `8001` 且其 smoke 失败、GUI 最低矩阵失败、实际仍指向旧实例，或文档 / 代理 / 运行事实不一致，则不得重新把 `8001` 写成 current latest。
2. 失败后必须回退到最近一组已验证稳定入口，并按 [15-回滚说明.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/15-回滚说明.md) 的口径重新验证；当前仓库默认示例仍是 `8006 / 9000 -> /dev -> 8006`。

## 当前阶段 5 发布与交付入口（2026-04-17）

1. 代码交付入口以 GitHub `main` 分支首期交付提交 `be995ed` 为准，仓库摘要见 [29-首期GitHub交付摘要.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/29-首期GitHub交付摘要.md)。
2. 当前本地 / 测试环境后端发布校验默认示例入口为 `http://127.0.0.1:8006/admin/performance/*`。
3. 当前本地 / 测试环境前端发布校验默认示例入口为 `http://127.0.0.1:9000`，并通过 `/dev` 代理到 latest backend `8006`。
4. 当前初始化入口固定为 [cool-admin-midway/scripts/seed-stage2-performance.mjs](/Users/shaotongli/Documents/xuedao/cool-admin-midway/scripts/seed-stage2-performance.mjs)；若环境重建或切换实例，必须先执行该脚本，再进入发布校验。
5. 当前发布前最小自动化校验默认命令为 `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && npm run smoke:stage2:performance`；若实际发布实例不是 `8006`，必须把 `STAGE2_SMOKE_BASE_URL` 替换为目标实例后重新执行。

### 当前有效启动顺序（2026-04-17）

1. 在 [cool-admin-midway](/Users/shaotongli/Documents/xuedao/cool-admin-midway) 启动 latest backend：`npm run dev`
2. 立即执行标准初始化脚本：`node ./scripts/seed-stage2-performance.mjs`
3. 对目标实例执行 latest 集成 smoke：`npm run smoke:stage2:performance`
4. 在 [cool-admin-vue](/Users/shaotongli/Documents/xuedao/cool-admin-vue) 启动 latest frontend：`npm run dev`
5. 使用 `http://127.0.0.1:9000 -> /dev -> http://127.0.0.1:8006` 和当前账号矩阵执行发布前页面核对

说明：

1. 若 backend 实例端口不是 `8006`，第 3 步和第 5 步都必须同步替换为目标实例。
2. 若环境、菜单或样例数据被重置，必须重新执行第 2 步，不能直接沿用历史通过结论。

## 联调样例基线

### 角色样例

联调环境至少准备以下 4 类账号角色：

1. `HR 管理员`
2. `部门经理`
3. `普通员工`
4. `360 环评价人`

说明：

## 二期主题 11 联调准备入口（2026-04-18）

1. 主题11窗口C当前最小联调脚本与真实通过命令为：
   - `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && node ./scripts/smoke-stage2-procurement-supplier.mjs`
   - `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && THEME11_SMOKE_BASE_URL=http://127.0.0.1:8012 THEME11_PURCHASE_ORDER_INFO_ID=1 THEME11_SUPPLIER_INFO_ID=1 THEME11_ACTIVE_ORDER_DELETE_ID=2 THEME11_REFERENCED_SUPPLIER_DELETE_ID=2 node ./scripts/smoke-stage2-procurement-supplier.mjs`
2. 主题11联调最小账号固定为：
   - `hr_admin / 123456`
   - `manager_rd / 123456`
   - `employee_platform / 123456`
3. 当前默认联调入口复用 latest backend：
   - `THEME11_SMOKE_BASE_URL=http://127.0.0.1:8006`
4. 如需覆盖字段脱敏和删除限制，可按脚本约定补充可选环境变量：
   - `THEME11_SUPPLIER_INFO_ID`
   - `THEME11_PURCHASE_ORDER_INFO_ID`
   - `THEME11_ACTIVE_ORDER_DELETE_ID`
   - `THEME11_REFERENCED_SUPPLIER_DELETE_ID`
5. 截至 2026-04-18，主题11真实联调已在 fresh backend `8012` 通过，结果为 `PASS 22 / FAIL 0 / SKIP 0`。
6. `8006` 首次联调返回主题11接口 `404`，根因是本地端口仍挂旧 backend 进程；这属于本地运行态漂移，不构成主题11代码阻塞，也不改写本文件上文定义的 shared latest 入口。

1. 如果同一账号兼任多个角色，会降低权限回归价值。
2. 首期推荐至少保留一名与被评价员工不同的独立环评价人。

### 组织架构样例

联调环境至少准备以下部门树口径：

1. 一级部门：`总部`
2. 二级部门：`研发中心`、`销售中心`、`人力资源部`
3. 三级部门：`平台组`、`业务组`

最小关系要求：

1. 至少存在一个“经理 -> 直属部门 -> 下级部门”的树形结构。
2. 至少存在一个非本部门员工，用于验证越权不可见。
3. 至少存在一个 `HR` 可见但部门经理不可见的薪资样例。

### 最小种子数据口径

联调前至少准备以下业务数据：

1. 指标库：
   - 至少 3 条启用指标
   - 覆盖 `assessment`、`goal`、`feedback` 三种类型
2. 评估单：
   - 1 条 `draft`
   - 1 条 `submitted`
   - 1 条 `approved`
   - 1 条 `rejected`
3. 目标：
   - 1 条 `draft`
   - 1 条 `in-progress`
   - 1 条 `completed`
4. 环评：
   - 1 个进行中的任务
   - 至少 2 名评价人
   - 至少 1 条已提交反馈
5. PIP：
   - 1 条 `draft`
   - 1 条 `active`
6. 晋升：
   - 1 条 `draft`
   - 1 条 `reviewing`
7. 薪资：
   - 1 条 `draft`
   - 1 条 `confirmed`

### 联调账号约束

1. 账号名称、手机号、邮箱可以按环境自定义，但角色归属必须稳定。
2. 不在文档中记录真实生产密码。
3. 如果联调环境使用初始化脚本生成账号，脚本输出必须和本节角色口径一致。

### 当前标准 seed / smoke 基线（2026-04-17）

1. [cool-admin-midway/scripts/seed-stage2-performance.mjs](/Users/shaotongli/Documents/xuedao/cool-admin-midway/scripts/seed-stage2-performance.mjs) 已落地模块 1 / 2 / 3 / 4 / 5 / 6 / 7 / 8 联调所需的部门树、角色、账号、菜单授权和最小业务样例。
2. 当前脚本内固定的账号标识为：`hr_admin`、`manager_rd`、`employee_platform`、`feedback_peer`，并额外提供 `employee_sales` 作为跨部门越权校验样例。
3. 当前脚本会同步当前首期交付范围内的绩效菜单入口：`/data-center/dashboard`、`/performance/my-assessment`、`/performance/initiated`、`/performance/pending`、`/performance/goals`、`/performance/indicator-library`、`/performance/feedback`、`/performance/pip`、`/performance/promotion`、`/performance/salary`；菜单显隐和按钮权限仍按角色授权关系裁剪。
4. 当前脚本已补齐首期联调最小样例：评估单、目标、dashboard 指标聚合、指标库、360 环评、PIP、晋升、薪资均有最小样例；dashboard 口径同时依赖 `assessment / goal / feedback / salary` 联调数据。
5. 截至 2026-04-17，已执行 `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && node ./scripts/smoke-stage2-performance.mjs --base-url http://127.0.0.1:8005`，结果为 `PASS 24 / FAIL 0 / SKIP 0 / Conclusion: PASSED`，说明共享登录 / 鉴权 / 权限上下文链路已闭环。
6. 截至 2026-04-17，已在 `http://127.0.0.1:8005` 完成 dashboard 真实联调复核：`hr_admin`、`manager_rd` 可访问 `/admin/performance/dashboard/summary`，`employee_platform` 被正确拒绝；经理筛选越权部门时返回空范围聚合结构。
7. 本次通过结论覆盖模块 1 / 2 / 3 当前联调基线；后续若再次修改共享基础层或这些模块的受保护接口链路，必须重新执行同一 smoke 脚本，不能继承本次通过状态。
8. 截至 2026-04-17，如默认 `8001` 被旧实例占用且无法在当前环境替换，可接受“已加载最新代码并完成真实 smoke 的隔离实例”作为模块联调口径；端口号本身不是模块关单条件，关键是种子、菜单权限、真实账号和 smoke 结果完整留痕。
9. 截至 2026-04-17，标准 [smoke-stage2-performance.mjs](/Users/shaotongli/Documents/xuedao/cool-admin-midway/scripts/smoke-stage2-performance.mjs) 已扩展 dashboard 断言；后续真实 smoke 除评估单、目标、指标库、PIP、晋升、薪资外，还必须覆盖 `HR / 部门经理` 访问 `/admin/performance/dashboard/summary` 成功、`employee_platform` 被拒绝，以及经理越权部门返回空范围结构。
10. 截至 2026-04-18，current latest backend 已正式回退为 `http://127.0.0.1:8006`；`npm run smoke:stage2:performance` 默认口径也已同步回退到 `8006`。
11. 截至 2026-04-18，latest 前端页面点测入口仍以 `http://127.0.0.1:9000` 为准，`/dev` 默认已回退到 latest backend `8006`。
12. 当前 latest GUI 稳定基线继续以 `9000 -> /dev -> 8006` 的既有人工点测留痕为准；若后续二期主题需要新的页面态收口，必须在该口径下重新补图。

## 二期主题 6：跨模块驾驶舱实现阶段验证入口（2026-04-18）

说明：

1. 本节只记录主题 6 实现阶段窗口 D 已真实执行的验证和当前阻塞，不替代最终验收结论。
2. 本节不改变上文“current latest 入口已回退为 `8006 / 9000 -> /dev -> 8006`”这一当前项目口径。

### 当前已执行的验证命令

1. 后端 build：
   - `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && npm run build`
2. 后端定向测试：
   - `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && npm run test -- test/performance/dashboard.service.test.ts`
   - `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && npm run test -- test/performance/cross-dashboard-source-adapter.service.test.ts`
3. 前端门禁：
   - `cd /Users/shaotongli/Documents/xuedao/cool-admin-vue && corepack pnpm run type-check`
   - `cd /Users/shaotongli/Documents/xuedao/cool-admin-vue && corepack pnpm run build`
4. 种子数据：
   - `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && node ./scripts/seed-stage2-performance.mjs`
5. 真实 smoke：
   - `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && STAGE2_SMOKE_BASE_URL=http://127.0.0.1:8001 npm run smoke:stage2:performance`
   - `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && STAGE2_SMOKE_BASE_URL=http://127.0.0.1:8006 npm run smoke:stage2:performance`

### 当前真实结果

1. 后端 build：通过
2. `dashboard.service.test.ts`：通过，`6` 个测试全部通过
3. `cross-dashboard-source-adapter.service.test.ts`：通过，`3` 个测试全部通过
4. 前端 `type-check / build`：通过
5. `8006` smoke：通过，`PASS 51 / FAIL 0 / SKIP 0 / Conclusion: PASSED`
6. `8001` smoke：失败，`PASS 41 / FAIL 9 / SKIP 0 / Conclusion: FAILED`

### `8006` 已通过的主题 6 关键断言

1. `HR` 可成功访问 `/admin/performance/dashboard/crossSummary`
2. `manager_rd` 可成功访问部门树范围 `crossSummary`
3. `manager_rd` 越权部门请求被拒绝，错误消息为 `无权查看该部门范围跨模块驾驶舱`
4. `employee_platform` 访问 `crossSummary` 被拒绝，错误消息为 `无权限查看跨模块驾驶舱`

### 当前未关闭阻塞

1. 历史 `8001` 实例未加载主题 6 最新实现：
   - `crossSummary` 返回 `404`
   - 同轮 `feedback/export` 与 `pip/export` 也返回 `404`
2. 因此当前只能确认：
   - 主题 6 实现已在 current latest `8006` 上通过真实联调
   - `8001` 失败结果仅保留为 shared runtime 回退的历史诊断证据

### 当前证据入口

1. 主题 6 验证卡：
   - [2026-04-18-二期主题6-跨模块驾驶舱指标-验证.md](/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-18-二期主题6-跨模块驾驶舱指标-验证.md)
2. 主题 6 主卡：
   - [2026-04-18-二期主题6-跨模块驾驶舱指标.md](/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-18-二期主题6-跨模块驾驶舱指标.md)
3. 主题 6 阶段 0 冻结与测试矩阵：
   - [2026-04-18-二期主题6-跨模块驾驶舱指标-阶段0冻结.md](/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-18-二期主题6-跨模块驾驶舱指标-阶段0冻结.md)
   - [2026-04-18-二期主题6-跨模块驾驶舱指标-测试矩阵与任务拆分冻结.md](/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-18-二期主题6-跨模块驾驶舱指标-测试矩阵与任务拆分冻结.md)

## 首期提测范围

### 本轮纳入提测

1. 模块 1：评估单主链
2. 模块 2：目标地图
3. 模块 3：驾驶舱
4. 模块 4：指标库
5. 模块 5：360 环评
6. 模块 6：PIP
7. 模块 7：晋升管理
8. 模块 8：薪资管理

### 本轮不纳入提测

1. `cool-uni`
2. 自动审批流、自动建议引擎
3. 薪资导出、环评导出、PIP 导出、晋升导出
4. 招聘、培训、会议等跨模块驾驶舱指标
5. 共享默认 `8001` 入口统一治理

## 提测账号与最小数据集

### 当前账号清单

| 角色 | 账号 | 主要测试范围 | 备注 |
| --- | --- | --- | --- |
| `HR 管理员` | `hr_admin` | 驾驶舱、评估发起、指标库、PIP、晋升、薪资 | 测试环境口令统一 `123456` |
| `部门经理` | `manager_rd` | 审批评估、目标地图、环评任务、PIP、晋升 | 测试环境口令统一 `123456` |
| `普通员工` | `employee_platform` | 我的考核、目标更新、无权场景回归 | 测试环境口令统一 `123456` |
| `360 环评价人` | `feedback_peer` | 环评反馈提交 | 测试环境口令统一 `123456` |
| `跨部门样例员工` | `employee_sales` | 越权不可见、跨部门数据隔离回归 | 测试环境口令统一 `123456` |

说明：

1. 以上口令仅用于本地 / 测试环境，不代表生产环境。
2. 若当前库数据不完整，先执行标准 seed 再开始提测。

### 最小数据集准备命令

1. `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && node ./scripts/seed-stage2-performance.mjs`
2. `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && STAGE2_SMOKE_BASE_URL=http://127.0.0.1:8006 npm run smoke:stage2:performance`

### 最小数据集口径

1. 指标库：至少 3 条启用指标，覆盖 `assessment / goal / feedback`
2. 评估单：至少各 1 条 `draft / submitted / approved / rejected`
3. 目标：至少各 1 条 `draft / in-progress / completed`
4. 环评：至少 1 个进行中任务、2 名评价人、1 条已提交反馈
5. PIP：至少 1 条 `draft`、1 条 `active`
6. 晋升：至少 1 条 `draft`、1 条 `reviewing`
7. 薪资：至少 1 条 `draft`、1 条 `confirmed`

## 二期主题 3：`cool-uni` 设计阶段登录与验证口径（2026-04-17）

说明：

1. 本节只定义二期主题 3 阶段 1 设计评审所依赖的登录入口、账号和验证口径，不表示 `cool-uni` 已交付或已进入提测。
2. 移动端设计阶段不单独创建后端实例，不单独创建移动端账号体系。

### 登录入口复用口径

1. 验证码入口：`http://127.0.0.1:8001/admin/base/open/captcha`
2. 登录入口：`http://127.0.0.1:8001/admin/base/open/login`
3. 刷新 token 入口：`http://127.0.0.1:8001/admin/base/open/refreshToken`
4. 权限 / 菜单入口：`http://127.0.0.1:8001/admin/base/comm/permmenu`
5. 退出入口：`http://127.0.0.1:8001/admin/base/comm/logout`

补充规则：

1. `cool-uni` 设计阶段只允许复用以上现有入口，不新增移动端专用登录、鉴权或菜单接口。
2. 若后续实现阶段发现现有入口无法支撑移动端登录闭环，只能先输出接口缺口清单，再回写 `04 / 06 / 09 / 12`，不能直接新增接口。

### 设计阶段账号口径

| 用途 | 账号 | 说明 |
| --- | --- | --- |
| 员工主路径 | `employee_platform` | 验证“我的考核 / 目标 / 360 环评”主路径 |
| 经理主路径 | `manager_rd` | 验证“待我审批 / 目标 / 360 环评”主路径 |
| 越权拒绝 | `employee_sales` | 验证跨部门数据不可见 |

补充说明：

1. 本地 / 测试环境口令继续以标准 seed 为准，统一为 `123456`。
2. `HR`、`feedback_peer` 不作为主题 3 首批移动端主角色，不作为本主题设计阶段的必备账号。

### 设计阶段最小验证口径

1. 登录成功后，工作台只展示首批白名单内且当前账号有权访问的入口。
2. `employee_platform` 不得看到“待我审批”、驾驶舱、指标库、PIP、晋升、薪资、导出。
3. `manager_rd` 不得在移动端看到驾驶舱、已发起考核、指标库、PIP、晋升、薪资、导出。
4. `employee_sales` 用于验证 `manager_rd` 不得越权看到销售部门数据。
5. 当前阶段若只能通过 PC 页面和现有接口证明权限 / 数据范围口径成立，可作为设计证据；但不得写成 `cool-uni` 页面已验证通过。

## 二期阶段 1 当前已完成交付包摘要（2026-04-17）

说明：

1. 本节只汇总二期当前已完成收口的主题 1-3，不改变首期交付边界，也不表示二期其余候选项已放开。
2. 当前汇总结论必须以各主题最终验收卡和版本记录为准，不得替代后续新主题的单独冻结与验收。

### 当前已关闭主题

1. 主题 1：`8001` 统一治理
   - 当前结论：已完成阶段 1 收口
   - 当前 latest 入口：`8006 / 9000 -> /dev -> 8006`
   - 当前历史治理 / 诊断入口：`8001 / 9000 -> /dev -> 8001`
2. 主题 2：导出增强
   - 当前结论：已完成阶段 1 收口
   - 当前首批范围：`360 环评汇总导出`、`PIP 摘要导出`
   - 当前边界：不包含薪资金额导出、晋升导出、通用导出平台、异步导出中心
3. 主题 3：`cool-uni`
   - 当前结论：已完成阶段 1 收口
   - 当前首批页面：登录、工作台、我的考核、待我审批、目标、`360` 环评
   - 当前边界：不包含驾驶舱、指标库、`PIP`、晋升、薪资个人结果页、任意导出页

### 当前总判断

1. 主题 1-3 的最终验收卡当前均为 `done`。
2. 主题 1-3 当前未见未关闭 `P0 / P1`。
3. 当前二期交付包的唯一正式汇总结论为：`二期主题1-3已完成当前阶段交付收口`。

## 角色偏好持久化补充口径（2026-04-23）

1. 当前账号的绩效 persona 偏好统一存放在 `base_sys_user.activePerformancePersonaKey`。
2. `Web` 与 `cool-uni` 都必须通过后端 `performanceAccessContext` 读取当前生效视角，不允许各端维护独立的本地持久化 persona 真相。
3. `Web` 切换 persona 时，必须调用 `POST /admin/base/comm/performanceAccessContext/activePersona` 写库；不能继续依赖只读 `GET` 参数假切换。
4. `cool-uni` 启动时即使已存在本地 `perms / workbenchPages / performanceAccessContext` 缓存，也必须至少补拉一次最新 `performanceAccessContext`，以数据库值为准覆盖本地缓存。
5. 若数据库中未保存值，或保存值已不再属于当前账号 `availablePersonas`，后端必须回退 `defaultPersonaKey`；这属于可恢复兼容场景，不阻断登录和页面进入。
6. 发布前必须先执行 `npm run migration:up`，确认 `20260423090000` 已应用后再启动后端或联调。

### 2026-04-23 真实人工联调入口

1. `Web` 本轮真实 GUI 联调入口：
   - `http://127.0.0.1:9011 -> /dev -> http://127.0.0.1:8001`
   - 真实留痕见 [report.md](/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/artifacts/persona-web-switch/report.md)
2. `cool-uni` 本轮真实 H5 启动命令：
   - `cd /Users/shaotongli/Documents/xuedao/cool-uni && corepack pnpm exec uni --platform h5 --host 127.0.0.1 --port 9933`
3. `cool-uni` 当前不能用裸 `vite` 是否能打开页面来判定 persona 闭环；必须以 `uni --platform h5` 的真实入口为准。
4. 若端口被占，`uni` H5 运行时可能自动漂移到其他端口；本轮实际运行时证据落在同源 H5 页面上下文，详见 [report.md](/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/artifacts/persona-uni-cold-start/report.md)。

### 2026-04-23 当前放行判断

1. “`Web` 和 `cool-uni` 共用同一个数据库值”的单一事实源主链已经有真实证据：
   - `Web` 浏览器切换成功
   - 后端 API 返回已切换 persona
   - 数据库持久化成功
   - `cool-uni` 启动同步代码在真实运行时读回相同 persona
2. `cool-uni` 应用侧 H5 mount 阻塞已关闭：`curl http://127.0.0.1:9943/main.ts` 已确认自动注入 `@dcloudio/uni-h5` 与 `.mount("#app")`，且 `vite build / uni build -p h5` 已重新通过。
3. `cool-uni` 页面级 GUI 留痕也已完成：真实 Chrome DevTools 会话已在 `http://127.0.0.1:9943/#/pages/index/home` 截到首页“当前账号 · 绩效运营视角”及对应开放工作台卡片。
4. 因此本轮可放行的结论升级为：`Web` 与 `cool-uni` 共用数据库 persona 值的真实人工联调已闭环，当前不再保留该项 `P1`。

## 自动化验证摘要

| 范围 | 命令 | 当前结果 |
| --- | --- | --- |
| 前端全局 | `cd /Users/shaotongli/Documents/xuedao/cool-admin-vue && corepack pnpm run type-check` | 通过 |
| 前端全局 | `cd /Users/shaotongli/Documents/xuedao/cool-admin-vue && corepack pnpm run build` | 通过 |
| 模块 1 / 2 | `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && npm run test -- test/performance/assessment.service.test.ts test/performance/goal.service.test.ts` | 通过 |
| 模块 3 | `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && npm run test -- test/performance/dashboard.service.test.ts` | 通过 |
| 模块 4 | `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && npm run test -- test/performance/indicator.service.test.ts` | 通过 |
| 模块 5 | `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && npm run test -- test/performance/feedback.service.test.ts` | 通过 |
| 模块 6 | `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && npm run test -- test/performance/pip.service.test.ts` | 通过 |
| 模块 7 | `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && npm run test -- test/performance/promotion.service.test.ts` | 通过 |
| 模块 8 | `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && npx jest test/performance/salary.service.test.ts --runInBand` | 通过 |
| latest 集成 smoke | `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && npm run smoke:stage2:performance` | 通过，`PASS 41 / FAIL 0 / SKIP 0 / Conclusion: PASSED` |

补充说明：

1. latest GUI 稳定基线当前以 `9000 -> /dev -> 8006` 为准，`hr_admin / manager_rd / employee_platform` 的最低权限矩阵已有既有留痕可复用；若主题 6 最终验收要求新增页面态证据，应在该口径下补齐。
2. 当前没有新增前端 E2E 自动化框架；页面验收仍以人工点测和真实 smoke 留痕为准。

## 业务验收执行清单

| 场景 | 用谁测 | 怎么测 | 预期结果 | 当前证据状态 |
| --- | --- | --- | --- | --- |
| 评估闭环 | `hr_admin` + `employee_platform` + `manager_rd` | HR/经理创建评估，员工保存并提交自评，经理审批通过/驳回 | 三张列表口径、状态流转、权限显隐与事实源一致 | 已完成，见 [2026-04-17-阶段4-业务验收记录.md](/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-17-阶段4-业务验收记录.md) |
| 目标闭环 | `hr_admin` 或 `manager_rd` + `employee_platform` | 创建目标、编辑目标、员工更新进度、目标自动完成 | `progressRate` 正确，`completed` 后不可继续更新 | 已完成，见 [2026-04-17-阶段4-业务验收记录.md](/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-17-阶段4-业务验收记录.md) |
| 驾驶舱权限与显示 | `hr_admin` + `manager_rd` + `employee_platform` | 登录 `9000`，进入 dashboard，切换经理部门筛选，验证员工无入口 | HR/经理可见驾驶舱，经理无越权部门，员工无 dashboard 入口 | 已完成，见 [2026-04-17-阶段4-业务验收记录.md](/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-17-阶段4-业务验收记录.md) |
| 指标库 CRUD | `hr_admin` + `manager_rd` + `employee_platform` | HR 新增、编辑、删除指标；经理/员工尝试访问 | HR 成功，非 HR 被拒绝，重复编码被拒绝 | 已完成，见 [2026-04-17-阶段4-业务验收记录.md](/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-17-阶段4-业务验收记录.md) |
| 环评任务与反馈 | `manager_rd` + `feedback_peer` + `employee_platform` | 经理创建任务，评价人提交反馈，员工仅查看汇总 | 单人单次提交、截止校验、员工不可见他人单条反馈 | 已完成，见 [2026-04-17-阶段4-业务验收记录.md](/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-17-阶段4-业务验收记录.md) |
| PIP 主链 | `hr_admin` + `manager_rd` + `employee_platform` | 创建 PIP、启动、跟进、完成/关闭，员工尝试访问 | 状态流转正确，非授权用户被拒绝 | 已完成，见 [2026-04-17-阶段4-业务验收记录.md](/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-17-阶段4-业务验收记录.md) |
| 晋升主链 | `manager_rd` + `hr_admin` + `employee_platform` | 创建晋升单、提交评审、审批通过/驳回，员工尝试访问 | `draft -> reviewing -> approved/rejected` 正确，员工被拒绝 | 已完成，见 [2026-04-17-阶段4-业务验收记录.md](/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-17-阶段4-业务验收记录.md) |
| 薪资主链 | `hr_admin` + `manager_rd` + `employee_platform` | HR 新增薪资、确认、追加调整、归档；经理/员工尝试访问 | 仅 HR 可见金额与操作，`confirmed` 后不可直接改金额 | 已完成，见 [2026-04-17-阶段4-业务验收记录.md](/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-17-阶段4-业务验收记录.md) |

## 阶段 3 提测前收口结论

### 当前结论

1. 按首期业务提测包口径，当前已满足 [20-阶段门禁与完成定义.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/20-阶段门禁与完成定义.md) 中“阶段 3 提测前收口”的必备证据：
   - 提测范围已冻结
   - 测试入口、账号、最小数据集、步骤、预期已明确
   - 自动化验证结果已留痕
   - latest smoke 已通过
   - 业务模块 1-9 当前任务卡均已回写完成证据
2. 按项目级最新事实源口径，模块 0 已由 later proof 关闭，当前业务提测包不再存在模块 0 共享运行时 `P1` 阻塞。
3. 因此当前对外发布口径为：
   - 业务提测包：可提测
   - 共享运行时 clean 启动整治：已由 latest backend `8006` fresh 启动与标准 smoke 证据关闭

### 初始化脚本输入模板

如果后续为联调环境编写初始化脚本，输入模板建议固定为下面结构：

```json
{
  "users": [
    {
      "key": "hr_admin",
      "role": "hr",
      "name": "HR管理员",
      "deptKey": "hr"
    },
    {
      "key": "manager_rd",
      "role": "manager",
      "name": "研发经理",
      "deptKey": "rd-center"
    },
    {
      "key": "employee_platform",
      "role": "employee",
      "name": "平台员工",
      "deptKey": "platform-group"
    },
    {
      "key": "feedback_peer",
      "role": "feedback_user",
      "name": "环评价人",
      "deptKey": "biz-group"
    }
  ],
  "departments": [
    {
      "key": "headquarters",
      "name": "总部",
      "parentKey": null
    },
    {
      "key": "rd-center",
      "name": "研发中心",
      "parentKey": "headquarters"
    },
    {
      "key": "sales-center",
      "name": "销售中心",
      "parentKey": "headquarters"
    },
    {
      "key": "hr",
      "name": "人力资源部",
      "parentKey": "headquarters"
    },
    {
      "key": "platform-group",
      "name": "平台组",
      "parentKey": "rd-center"
    },
    {
      "key": "biz-group",
      "name": "业务组",
      "parentKey": "rd-center"
    }
  ],
  "performanceSeed": {
    "indicators": 3,
    "assessments": {
      "draft": 1,
      "submitted": 1,
      "approved": 1,
      "rejected": 1
    },
    "goals": {
      "draft": 1,
      "in-progress": 1,
      "completed": 1
    },
    "feedbackTasks": {
      "running": 1,
      "feedbackUsersPerTask": 2,
      "submittedRecords": 1
    },
    "pips": {
      "draft": 1,
      "active": 1
    },
    "promotions": {
      "draft": 1,
      "reviewing": 1
    },
    "salaries": {
      "draft": 1,
      "confirmed": 1
    }
  }
}
```

模板约束：

1. `key` 只作为初始化映射键，不作为最终数据库主键。
2. `role` 必须与权限矩阵中的角色语义对应。
3. `deptKey` 必须能映射到部门树中的真实节点。
4. `performanceSeed` 只表达最小数量口径，不表达具体业务字段值。
5. 真实环境初始化脚本可以扩展字段，但不能削弱本模板定义的最小角色和最小数据集。

## 提测前检查

1. 菜单、按钮权限和角色授权关系已导入。
2. 三张评估列表口径与默认排序符合文档定义。
3. 驾驶舱只展示首期真实指标，不展示跨模块虚构数据。
4. 环评、PIP、晋升、薪资的未开放导出能力仍处于关闭状态。
5. 薪资金额、审批意见、环评明细内容经过权限裁剪。
6. `jixiao` 仅作为页面结构参考，没有把其独立框架代码直接搬入项目。
7. 联调环境已准备最小角色账号和最小种子数据。

## 验收入口

### 业务验收主路径

1. 评估闭环：
   - HR/经理创建评估
   - 员工保存并提交自评
   - 部门经理审批
2. 目标闭环：
   - 新建目标
   - 更新进度
   - 查看统计
3. 模型增强：
   - 指标库 CRUD
   - 环评任务创建、填写、汇总
4. 状态扩展：
   - PIP 创建与跟进
   - 晋升创建与评审
5. 敏感模块：
   - HR 录入薪资
   - 调整记录生效

### 技术验收主路径

1. 前端 `type-check` 通过
2. 前端 `build` 通过
3. 后端最小验证通过
4. 当前阶段 smoke 通过
5. 菜单初始化、权限显隐和接口鉴权一致
6. 文档与代码路径一致

## 当前阶段 5 交付说明结论（2026-04-17）

1. 从文档承载角度看，阶段 5 所需的当前有效入口、启动顺序、初始化步骤、提测账号、最小数据集、提测 / 验收口径、未开放能力和已知限制已经齐备，交付说明本身足以支持首期上线前交付。
2. 阶段 4 业务验收结论已在 [2026-04-17-阶段4-业务验收记录.md](/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-17-阶段4-业务验收记录.md) 中正式回写为 `通过`；最新 [2026-04-17-问题台账与最终回归收口.md](/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-17-问题台账与最终回归收口.md) 仍无未关闭 `P0 / P1`。
3. 当前有效交付基线以 latest backend `8006`、latest frontend `9000 -> /dev -> 8006`、标准 seed / smoke `PASS 41 / FAIL 0 / SKIP 0`、latest GUI 稳定基线和阶段 4 业务验收记录为准。
4. 因此就 [14-交付说明.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/14-交付说明.md) 而言，当前已满足 [20-阶段门禁与完成定义.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/20-阶段门禁与完成定义.md) 中阶段 5 的交付说明门槛，可进入最终放行评审。
5. 本结论不表示“已上线”；最终放行仍需同时满足 [15-回滚说明.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/15-回滚说明.md) 可执行，且发布时点最新问题台账未新增 `P0 / P1`。

## 阶段 4 通过后的阶段 5 收口动作

1. 将阶段 4 业务验收结论正式回写到最新问题台账或对应阶段收口卡，确认当前仍无未关闭 `P0 / P1`。
2. 以当前有效入口 `9000 -> /dev -> 8006` 和 `8006/admin/performance/*` 复核发布前入口未漂移；若端口、代理或实例变化，先重放标准 seed，再重跑标准 smoke。
3. 按 [15-回滚说明.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/15-回滚说明.md) 复核代码、菜单、权限、数据四类回滚路径仍可执行，并保留回滚后最小验证入口。
4. 持续保持首期未开放能力关闭状态，不因阶段 4 通过而临时放开薪资导出、环评导出、PIP 导出、晋升导出、`cool-uni` 或自动审批流 / 自动建议引擎。

## 当前二期新增主题交付覆盖（2026-04-18）

1. 当前二期新增主题的统一摘要入口已升级为 [2026-04-18-二期主题1-14-总收口.md](/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-18-二期主题1-14-总收口.md)。
2. 当前已完成主题级最终验收的新增主题覆盖：
   - [2026-04-18-二期主题10-合同管理-最终验收.md](/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-18-二期主题10-合同管理-最终验收.md)
   - [2026-04-18-二期主题11-采购与供应商管理-最终验收.md](/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-18-二期主题11-采购与供应商管理-最终验收.md)
   - [2026-04-18-二期主题12-招聘人才资产增强-最终验收.md](/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-18-二期主题12-招聘人才资产增强-最终验收.md)
   - [2026-04-18-二期主题13-人才发展与认证增强-最终验收.md](/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-18-二期主题13-人才发展与认证增强-最终验收.md)
   - [2026-04-18-二期主题14-培训学习与考试增强-最终验收.md](/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-18-二期主题14-培训学习与考试增强-最终验收.md)
3. 主题14 当前 shared latest 验收口径已补齐：
   - `http://127.0.0.1:8006/admin/performance/courseRecite/page` 已从历史 `404` 收敛为登录失效返回，说明路由已挂载
   - `STAGE2_SMOKE_BASE_URL=http://127.0.0.1:8006 node scripts/smoke-stage2-performance.mjs` 结果为 `PASS 93 / FAIL 0 / SKIP 0 / Conclusion: PASSED`
4. 因此截至 2026-04-18，当前仓库的二期新增主题交付总览可以统一写为：
   - 已完成主题包：`1-14`
   - 当前 shared latest 示例入口：`8006 / 9000 -> /dev -> 8006`
   - 当前总收口结论：`二期主题1-14已完成当前阶段交付收口`

## 已知限制

1. 首期不做 `cool-uni`。
2. 首期不做自动审批流和自动建议引擎。
3. 首期不交付薪资导出、环评导出、PIP 导出、晋升导出。
4. 首期不交付跨招聘、培训、会议模块的真实驾驶舱指标。
5. 截至 2026-04-17，模块 0 任务卡已为 `done`：`cool-admin-midway` latest backend 已完成 fresh 启动、标准 seed 和标准 smoke，结果 `PASS 41 / FAIL 0 / SKIP 0 / Conclusion: PASSED`，此前 clean-start 证据缺口已关闭。
6. 上述历史 `P2` 治理项已于 [2026-04-18-项目级P2治理收口.md](/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-18-项目级P2治理收口.md) 完成收口；当前不再存在未关闭项目级 `P2`，但若后续再次改动 shared latest、默认代理或事实源文档，必须另开治理卡复核。

## 首期导出交付边界

1. 首期只交付：
   - 绩效评估导出
   - 目标地图导出
2. 首期不交付：
   - 薪资导出
   - 环评导出
   - PIP 导出
   - 晋升导出
3. 导出字段必须按角色裁剪后才能上线。
