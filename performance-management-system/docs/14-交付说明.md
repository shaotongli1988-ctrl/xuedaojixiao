# 交付说明

## 文档职责

这份文档是“交付和上线准备”的唯一事实源。

它只负责定义：

1. 部署前提
2. 初始化步骤
3. 菜单和权限初始化
4. 联调入口
5. 验收入口
6. 已知限制

## 不负责内容

这份文档不负责定义：

- 业务需求边界
- 表结构设计
- 接口字段详情

## 本地启动顺序

### 环境前提

1. Node 版本满足 [cool-admin-midway/package.json](/Users/shaotongli/Documents/xuedao/cool-admin-midway/package.json) 中 `>=18.0.0` 要求。
2. 前后端依赖分别安装在：
   - [cool-admin-midway](/Users/shaotongli/Documents/xuedao/cool-admin-midway)
   - [cool-admin-vue](/Users/shaotongli/Documents/xuedao/cool-admin-vue)
3. 数据库、基础配置和现有 `xuedao` 本地环境按原项目已有方式准备，不额外新造环境体系。
4. 阶段 2 联调前，`cool-admin-midway` 必须安装完整 Midway 运行时依赖，并能基于现有本地配置完成启动复验；仅安装 `cool-admin-midway/.shared-deps/js/` 内的测试依赖，不视为联调环境就绪。
5. 本地 `npm run dev` 读取的数据库配置以 [cool-admin-midway/src/config/config.local.ts](/Users/shaotongli/Documents/xuedao/cool-admin-midway/src/config/config.local.ts) 为准；`.env` 不是这份交付说明定义的当前阶段 2 硬阻塞项，也不是唯一事实源。
6. 截至 2026-04-17，本地 latest backend 联调口径要求“复用现有库结构”，不能把 TypeORM `synchronize` 当作 fresh 实例启动前提；最新验证基线以 [cool-admin-midway/src/config/config.local.ts](/Users/shaotongli/Documents/xuedao/cool-admin-midway/src/config/config.local.ts) 当前配置为准。

### 本地启动建议顺序

1. 先启动后端：
   - 在 [cool-admin-midway](/Users/shaotongli/Documents/xuedao/cool-admin-midway) 执行 `npm run dev`
2. 再启动前端：
   - 在 [cool-admin-vue](/Users/shaotongli/Documents/xuedao/cool-admin-vue) 执行 `npm run dev`
3. 开发自检时至少执行：
   - 前端 `npm run type-check`
   - 前端 `npm run build`
   - 后端按本次改动范围执行 `npm run test` 或最小接口自测

## 交付物清单

首期绩效模块交付必须至少包含以下内容：

1. 后端 `performance` 模块代码
2. 前端 `performance` 模块页面与复用组件
3. 菜单和权限初始化数据
4. 对应唯一事实源文档更新
5. 测试和验证记录
6. 交付限制和已知未开放能力说明

## 菜单和权限初始化方式

首期绩效模块菜单和权限初始化固定按现有 Cool 基座方式执行：

1. 不新增新的权限初始化机制。
2. 绩效模块菜单数据按 `base/menu.json` 同结构组织。
3. 按钮权限通过菜单树 `type = 2` 节点导入。
4. 前端继续使用现有动态菜单装配逻辑加载菜单。
5. 交付时只需要补：
   - 绩效模块菜单数据
   - 绩效模块权限键
   - 与角色的授权关系

当前已落地的唯一事实源：

1. 菜单树和权限键以 [cool-admin-midway/src/modules/base/menu.json](/Users/shaotongli/Documents/xuedao/cool-admin-midway/src/modules/base/menu.json) 为准。
2. 阶段 2 本地联调脚本以 [cool-admin-midway/scripts/seed-stage2-performance.mjs](/Users/shaotongli/Documents/xuedao/cool-admin-midway/scripts/seed-stage2-performance.mjs) 为准；脚本会按现有菜单树同步模块 1 / 2 主链、模块 3 dashboard 和模块 4 `HR` 指标库入口所需菜单授权关系。

## 联调入口

### 后端联调入口

首期绩效模块后端接口统一挂在：

- `/admin/performance/*`

具体接口以 [04-API设计.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/04-API设计.md) 为准。

### 前端联调入口

首期前端入口统一落在：

- [cool-admin-vue/src/modules/performance](/Users/shaotongli/Documents/xuedao/cool-admin-vue/src/modules/performance)

页面结构和拆分规则以 [18-页面结构与原型映射.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/18-页面结构与原型映射.md) 为准。

## 2026-04-17 当前提测入口

### 当前推荐提测口径

1. 前端页面入口：`http://127.0.0.1:9000`
2. 前端代理口径：`/dev -> http://127.0.0.1:8006`
3. 后端接口入口：`http://127.0.0.1:8006/admin/performance/*`

说明：

1. 当前首期提测统一以 latest frontend `9000` 和 latest backend `8006` 为准。
2. `8002 / 9003` 只保留为模块 4 / 6 的历史联调收口证据，不再作为当前提测首选入口。
3. `8001` 仍可能指向旧实例，不能作为当前提测通过依据。

## 当前首期真实交付覆盖（2026-04-17）

1. 当前首期业务交付范围以模块 1、2、3、4、5、6、7、8 任务卡为准：
   - 模块 1：评估单主链
   - 模块 2：目标地图
   - 模块 3：驾驶舱基础聚合
   - 模块 4：指标库
   - 模块 5：360 环评
   - 模块 6：PIP
   - 模块 7：晋升管理
   - 模块 8：薪资管理
2. 模块 9 不新增独立业务范围，只负责驾驶舱最终聚合、阶段 5 联调收口和交付事实源同步；当前已完成收口并关闭任务卡。
3. 上述模块的最小自动化验证、真实联调和阶段结论以各自任务卡与 [2026-04-17-模块9-驾驶舱最终聚合与交付收口.md](/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-17-模块9-驾驶舱最终聚合与交付收口.md) 为准。
4. 模块 0 任务卡当前已关闭；此前 clean-start 日志噪音已由 latest backend `8006` fresh 启动与标准 smoke `PASS 41 / FAIL 0 / SKIP 0` 证据关闭。当前有效本地联调基线仍以 latest backend `8006` 和 latest frontend `9000` 为准。

## 当前有效联调与验收口径（2026-04-17）

1. 当前阶段 5 的整体有效后端联调入口为 `http://127.0.0.1:8006`。
2. 当前阶段 5 的整体有效前端验收入口为 `http://127.0.0.1:9000`，并通过 `/dev` 代理到 latest backend `8006`。
3. 本地环境前提仍以 [cool-admin-midway/src/config/config.local.ts](/Users/shaotongli/Documents/xuedao/cool-admin-midway/src/config/config.local.ts) 为准；当前口径是“复用现有 schema、不依赖 TypeORM auto-sync”。
4. 历史上用于模块级关单的等效实例口径仍然有效，但不再作为当前阶段 5 的整体交付入口：
   - 模块 6 曾接受 `9003 -> 8002`
   - 模块 7 曾在 `18005 / 18006` 完成真实联调与标准 smoke
   - 模块 8 曾接受 `8002 / 18006`
5. 若再次切换端口或环境，必须先重放等效 seed / 菜单授权，再重跑 smoke 或最小人工点测，不能直接继承旧端口的通过结论。

## 联调样例基线

### 角色样例

联调环境至少准备以下 4 类账号角色：

1. `HR 管理员`
2. `部门经理`
3. `普通员工`
4. `360 环评价人`

说明：

1. 如果同一账号兼任多个角色，会降低权限回归价值。
2. 首期推荐至少保留一名与被评价员工不同的独立环评价人。

### 组织架构样例

联调环境至少准备以下部门树口径：

1. 一级部门：`总部`
2. 二级部门：`研发中心`、`销售中心`、`人力资源部`
3. 三级部门：`平台组`、`业务组`

最小关系要求：

1. 至少存在一个“经理 -> 直属部门 -> 下级部门”的树形结构。
2. 至少存在一个非本部门员工，用于验证越权不可见。
3. 至少存在一个 `HR` 可见但部门经理不可见的薪资样例。

### 最小种子数据口径

联调前至少准备以下业务数据：

1. 指标库：
   - 至少 3 条启用指标
   - 覆盖 `assessment`、`goal`、`feedback` 三种类型
2. 评估单：
   - 1 条 `draft`
   - 1 条 `submitted`
   - 1 条 `approved`
   - 1 条 `rejected`
3. 目标：
   - 1 条 `draft`
   - 1 条 `in-progress`
   - 1 条 `completed`
4. 环评：
   - 1 个进行中的任务
   - 至少 2 名评价人
   - 至少 1 条已提交反馈
5. PIP：
   - 1 条 `draft`
   - 1 条 `active`
6. 晋升：
   - 1 条 `draft`
   - 1 条 `reviewing`
7. 薪资：
   - 1 条 `draft`
   - 1 条 `confirmed`

### 联调账号约束

1. 账号名称、手机号、邮箱可以按环境自定义，但角色归属必须稳定。
2. 不在文档中记录真实生产密码。
3. 如果联调环境使用初始化脚本生成账号，脚本输出必须和本节角色口径一致。

### 当前阶段 2 本地联调基线

1. [cool-admin-midway/scripts/seed-stage2-performance.mjs](/Users/shaotongli/Documents/xuedao/cool-admin-midway/scripts/seed-stage2-performance.mjs) 已落地模块 1 / 2 / 3 / 4 / 5 / 6 / 7 / 8 联调所需的部门树、角色、账号、菜单授权和最小业务样例。
2. 当前脚本内固定的账号标识为：`hr_admin`、`manager_rd`、`employee_platform`、`feedback_peer`，并额外提供 `employee_sales` 作为跨部门越权校验样例。
3. 当前脚本会同步当前首期交付范围内的绩效菜单入口：`/data-center/dashboard`、`/performance/my-assessment`、`/performance/initiated`、`/performance/pending`、`/performance/goals`、`/performance/indicator-library`、`/performance/feedback`、`/performance/pip`、`/performance/promotion`、`/performance/salary`；菜单显隐和按钮权限仍按角色授权关系裁剪。
4. 当前脚本已补齐首期联调最小样例：评估单、目标、dashboard 指标聚合、指标库、360 环评、PIP、晋升、薪资均有最小样例；dashboard 口径同时依赖 `assessment / goal / feedback / salary` 联调数据。
5. 截至 2026-04-17，已执行 `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && node ./scripts/smoke-stage2-performance.mjs --base-url http://127.0.0.1:8005`，结果为 `PASS 24 / FAIL 0 / SKIP 0 / Conclusion: PASSED`，说明共享登录 / 鉴权 / 权限上下文链路已闭环。
6. 截至 2026-04-17，已在 `http://127.0.0.1:8005` 完成 dashboard 真实联调复核：`hr_admin`、`manager_rd` 可访问 `/admin/performance/dashboard/summary`，`employee_platform` 被正确拒绝；经理筛选越权部门时返回空范围聚合结构。
7. 本次通过结论覆盖模块 1 / 2 / 3 当前联调基线；后续若再次修改共享基础层或这些模块的受保护接口链路，必须重新执行同一 smoke 脚本，不能继承本次通过状态。
8. 截至 2026-04-17，如默认 `8001` 被旧实例占用且无法在当前环境替换，可接受“已加载最新代码并完成真实 smoke 的隔离实例”作为模块联调口径；端口号本身不是模块关单条件，关键是种子、菜单权限、真实账号和 smoke 结果完整留痕。
9. 截至 2026-04-17，标准 [smoke-stage2-performance.mjs](/Users/shaotongli/Documents/xuedao/cool-admin-midway/scripts/smoke-stage2-performance.mjs) 已扩展 dashboard 断言；后续真实 smoke 除评估单、目标、指标库、PIP、晋升、薪资外，还必须覆盖 `HR / 部门经理` 访问 `/admin/performance/dashboard/summary` 成功、`employee_platform` 被拒绝，以及经理越权部门返回空范围结构。
10. 截至 2026-04-17，fresh latest backend `http://127.0.0.1:8006` 已执行 `node ./scripts/seed-stage2-performance.mjs` 与 `STAGE2_SMOKE_BASE_URL=http://127.0.0.1:8006 npm run smoke:stage2:performance`，结果 `PASS 41 / FAIL 0 / SKIP 0 / Conclusion: PASSED`；旧本地实例 `8001 / 8002` 不再作为 latest smoke 证据口径。
11. 截至 2026-04-17，latest 前端页面点测入口以 `http://127.0.0.1:9000` 为准，`/dev` 已代理到 latest backend `8006`；执行人工页面点测时，不应继续使用仍指向旧 `8001` 口径的历史入口。
12. 截至 2026-04-17，已在 `http://127.0.0.1:9000 -> /dev -> http://127.0.0.1:8006` 完成 latest GUI 最低人工点测矩阵：`hr_admin` 的 `dashboard / goals / indicator-library / pip / promotion / salary` 页面均可正常打开，`manager_rd` 的 dashboard 页面和部门筛选权限范围正常，`employee_platform` 无 dashboard 菜单且直达后未进入 dashboard 页面；此前 dashboard `roleIds` 与 salary `userId` 运行时告警均已复验关闭。

## 首期提测范围

### 本轮纳入提测

1. 模块 1：评估单主链
2. 模块 2：目标地图
3. 模块 3：驾驶舱
4. 模块 4：指标库
5. 模块 5：360 环评
6. 模块 6：PIP
7. 模块 7：晋升管理
8. 模块 8：薪资管理

### 本轮不纳入提测

1. `cool-uni`
2. 自动审批流、自动建议引擎
3. 薪资导出、环评导出、PIP 导出、晋升导出
4. 招聘、培训、会议等跨模块驾驶舱指标
5. 共享默认 `8001` 入口统一治理

## 提测账号与最小数据集

### 当前账号清单

| 角色 | 账号 | 主要测试范围 | 备注 |
| --- | --- | --- | --- |
| `HR 管理员` | `hr_admin` | 驾驶舱、评估发起、指标库、PIP、晋升、薪资 | 测试环境口令统一 `123456` |
| `部门经理` | `manager_rd` | 审批评估、目标地图、环评任务、PIP、晋升 | 测试环境口令统一 `123456` |
| `普通员工` | `employee_platform` | 我的考核、目标更新、无权场景回归 | 测试环境口令统一 `123456` |
| `360 环评价人` | `feedback_peer` | 环评反馈提交 | 测试环境口令统一 `123456` |

说明：

1. 以上口令仅用于本地 / 测试环境，不代表生产环境。
2. 若当前库数据不完整，先执行标准 seed 再开始提测。

### 最小数据集准备命令

1. `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && node ./scripts/seed-stage2-performance.mjs`
2. `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && STAGE2_SMOKE_BASE_URL=http://127.0.0.1:8006 npm run smoke:stage2:performance`

### 最小数据集口径

1. 指标库：至少 3 条启用指标，覆盖 `assessment / goal / feedback`
2. 评估单：至少各 1 条 `draft / submitted / approved / rejected`
3. 目标：至少各 1 条 `draft / in-progress / completed`
4. 环评：至少 1 个进行中任务、2 名评价人、1 条已提交反馈
5. PIP：至少 1 条 `draft`、1 条 `active`
6. 晋升：至少 1 条 `draft`、1 条 `reviewing`
7. 薪资：至少 1 条 `draft`、1 条 `confirmed`

## 自动化验证摘要

| 范围 | 命令 | 当前结果 |
| --- | --- | --- |
| 前端全局 | `cd /Users/shaotongli/Documents/xuedao/cool-admin-vue && corepack pnpm run type-check` | 通过 |
| 前端全局 | `cd /Users/shaotongli/Documents/xuedao/cool-admin-vue && corepack pnpm run build` | 通过 |
| 模块 1 / 2 | `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && npm run test -- test/performance/assessment.service.test.ts test/performance/goal.service.test.ts` | 通过 |
| 模块 3 | `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && npm run test -- test/performance/dashboard.service.test.ts` | 通过 |
| 模块 4 | `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && npm run test -- test/performance/indicator.service.test.ts` | 通过 |
| 模块 5 | `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && npm run test -- test/performance/feedback.service.test.ts` | 通过 |
| 模块 6 | `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && npm run test -- test/performance/pip.service.test.ts` | 通过 |
| 模块 7 | `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && npm run test -- test/performance/promotion.service.test.ts` | 通过 |
| 模块 8 | `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && npx jest test/performance/salary.service.test.ts --runInBand` | 通过 |
| latest 集成 smoke | `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && STAGE2_SMOKE_BASE_URL=http://127.0.0.1:8006 npm run smoke:stage2:performance` | 通过，`PASS 41 / FAIL 0 / SKIP 0 / Conclusion: PASSED` |

补充说明：

1. latest GUI 最低人工点测已在 `9000 -> /dev -> 8006` 上完成，覆盖 `hr_admin / manager_rd / employee_platform` 的最低权限矩阵。
2. 当前没有新增前端 E2E 自动化框架；页面验收仍以人工点测和真实 smoke 留痕为准。

## 业务验收执行清单

| 场景 | 用谁测 | 怎么测 | 预期结果 | 当前证据状态 |
| --- | --- | --- | --- | --- |
| 评估闭环 | `hr_admin` + `employee_platform` + `manager_rd` | HR/经理创建评估，员工保存并提交自评，经理审批通过/驳回 | 三张列表口径、状态流转、权限显隐与事实源一致 | 技术联调已完成；业务验收待执行 |
| 目标闭环 | `hr_admin` 或 `manager_rd` + `employee_platform` | 创建目标、编辑目标、员工更新进度、目标自动完成 | `progressRate` 正确，`completed` 后不可继续更新 | 技术联调已完成；业务验收待执行 |
| 驾驶舱权限与显示 | `hr_admin` + `manager_rd` + `employee_platform` | 登录 `9000`，进入 dashboard，切换经理部门筛选，验证员工无入口 | HR/经理可见驾驶舱，经理无越权部门，员工无 dashboard 入口 | latest GUI 已执行 |
| 指标库 CRUD | `hr_admin` + `manager_rd` + `employee_platform` | HR 新增、编辑、删除指标；经理/员工尝试访问 | HR 成功，非 HR 被拒绝，重复编码被拒绝 | 技术联调已完成；GUI 业务验收待执行 |
| 环评任务与反馈 | `manager_rd` + `feedback_peer` + `employee_platform` | 经理创建任务，评价人提交反馈，员工仅查看汇总 | 单人单次提交、截止校验、员工不可见他人单条反馈 | 技术联调已完成；业务验收待执行 |
| PIP 主链 | `hr_admin` + `manager_rd` + `employee_platform` | 创建 PIP、启动、跟进、完成/关闭，员工尝试访问 | 状态流转正确，非授权用户被拒绝 | 技术联调已完成；GUI 业务验收待执行 |
| 晋升主链 | `manager_rd` + `hr_admin` + `employee_platform` | 创建晋升单、提交评审、审批通过/驳回，员工尝试访问 | `draft -> reviewing -> approved/rejected` 正确，员工被拒绝 | 技术联调已完成；GUI 业务验收待执行 |
| 薪资主链 | `hr_admin` + `manager_rd` + `employee_platform` | HR 新增薪资、确认、追加调整、归档；经理/员工尝试访问 | 仅 HR 可见金额与操作，`confirmed` 后不可直接改金额 | 技术联调已完成；latest GUI 最低打开验证已执行 |

## 阶段 3 提测前收口结论

### 当前结论

1. 按首期业务提测包口径，当前已满足 [20-阶段门禁与完成定义.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/20-阶段门禁与完成定义.md) 中“阶段 3 提测前收口”的必备证据：
   - 提测范围已冻结
   - 测试入口、账号、最小数据集、步骤、预期已明确
   - 自动化验证结果已留痕
   - latest smoke 已通过
   - 业务模块 1-9 当前任务卡均已回写完成证据
2. 按项目级最新事实源口径，模块 0 已由 later proof 关闭，当前业务提测包不再存在模块 0 共享运行时 `P1` 阻塞。
3. 因此当前对外发布口径为：
   - 业务提测包：可提测
   - 共享运行时 clean 启动整治：已由 latest backend `8006` fresh 启动与标准 smoke 证据关闭

### 初始化脚本输入模板

如果后续为联调环境编写初始化脚本，输入模板建议固定为下面结构：

```json
{
  "users": [
    {
      "key": "hr_admin",
      "role": "hr",
      "name": "HR管理员",
      "deptKey": "hr"
    },
    {
      "key": "manager_rd",
      "role": "manager",
      "name": "研发经理",
      "deptKey": "rd-center"
    },
    {
      "key": "employee_platform",
      "role": "employee",
      "name": "平台员工",
      "deptKey": "platform-group"
    },
    {
      "key": "feedback_peer",
      "role": "feedback_user",
      "name": "环评价人",
      "deptKey": "biz-group"
    }
  ],
  "departments": [
    {
      "key": "headquarters",
      "name": "总部",
      "parentKey": null
    },
    {
      "key": "rd-center",
      "name": "研发中心",
      "parentKey": "headquarters"
    },
    {
      "key": "sales-center",
      "name": "销售中心",
      "parentKey": "headquarters"
    },
    {
      "key": "hr",
      "name": "人力资源部",
      "parentKey": "headquarters"
    },
    {
      "key": "platform-group",
      "name": "平台组",
      "parentKey": "rd-center"
    },
    {
      "key": "biz-group",
      "name": "业务组",
      "parentKey": "rd-center"
    }
  ],
  "performanceSeed": {
    "indicators": 3,
    "assessments": {
      "draft": 1,
      "submitted": 1,
      "approved": 1,
      "rejected": 1
    },
    "goals": {
      "draft": 1,
      "in-progress": 1,
      "completed": 1
    },
    "feedbackTasks": {
      "running": 1,
      "feedbackUsersPerTask": 2,
      "submittedRecords": 1
    },
    "pips": {
      "draft": 1,
      "active": 1
    },
    "promotions": {
      "draft": 1,
      "reviewing": 1
    },
    "salaries": {
      "draft": 1,
      "confirmed": 1
    }
  }
}
```

模板约束：

1. `key` 只作为初始化映射键，不作为最终数据库主键。
2. `role` 必须与权限矩阵中的角色语义对应。
3. `deptKey` 必须能映射到部门树中的真实节点。
4. `performanceSeed` 只表达最小数量口径，不表达具体业务字段值。
5. 真实环境初始化脚本可以扩展字段，但不能削弱本模板定义的最小角色和最小数据集。

## 提测前检查

1. 菜单、按钮权限和角色授权关系已导入。
2. 三张评估列表口径与默认排序符合文档定义。
3. 驾驶舱只展示首期真实指标，不展示跨模块虚构数据。
4. 环评、PIP、晋升、薪资的未开放导出能力仍处于关闭状态。
5. 薪资金额、审批意见、环评明细内容经过权限裁剪。
6. `jixiao` 仅作为页面结构参考，没有把其独立框架代码直接搬入项目。
7. 联调环境已准备最小角色账号和最小种子数据。

## 验收入口

### 业务验收主路径

1. 评估闭环：
   - HR/经理创建评估
   - 员工保存并提交自评
   - 部门经理审批
2. 目标闭环：
   - 新建目标
   - 更新进度
   - 查看统计
3. 模型增强：
   - 指标库 CRUD
   - 环评任务创建、填写、汇总
4. 状态扩展：
   - PIP 创建与跟进
   - 晋升创建与评审
5. 敏感模块：
   - HR 录入薪资
   - 调整记录生效

### 技术验收主路径

1. 前端 `type-check` 通过
2. 前端 `build` 通过
3. 后端最小验证通过
4. 当前阶段 smoke 通过
5. 菜单初始化、权限显隐和接口鉴权一致
6. 文档与代码路径一致

## 当前阶段 5 文档放行结论（2026-04-17）

1. 当前交付说明、回滚说明、版本记录已覆盖首期真实业务模块 1 / 2 / 3 / 4 / 5 / 6 / 7 / 8，以及模块 9 的最终收口事实。
2. 当前有效联调与验收基线以 latest backend `8006`、latest frontend `9000 -> /dev -> 8006`、标准 smoke 与 latest GUI 人工点测结果为准。
3. 当前有效环境前提、联调账号、最小种子数据、交付限制和已知限制均已记录，可直接支持提测、验收与交付准备。
4. 本结论只表示“阶段 5 上线前交付”的文档条件已满足；不替代最新问题台账、实际发布执行和回滚演练本身。

## 已知限制

1. 首期不做 `cool-uni`。
2. 首期不做自动审批流和自动建议引擎。
3. 首期不交付薪资导出、环评导出、PIP 导出、晋升导出。
4. 首期不交付跨招聘、培训、会议模块的真实驾驶舱指标。
5. 截至 2026-04-17，模块 0 任务卡已为 `done`：`cool-admin-midway` latest backend `8006` 已完成 fresh 启动、标准 seed 和标准 smoke，结果 `PASS 41 / FAIL 0 / SKIP 0 / Conclusion: PASSED`，此前 clean-start 证据缺口已关闭。
6. 当前仍需作为 `P2` 治理项持续跟踪的，是默认 `8001` 旧实例口径、个别事实源路由漂移和守卫脚本治理，不影响当前业务模块 `1-9` 已完成真实联调和 latest smoke 的事实。

## 首期导出交付边界

1. 首期只交付：
   - 绩效评估导出
   - 目标地图导出
2. 首期不交付：
   - 薪资导出
   - 环评导出
   - PIP 导出
   - 晋升导出
3. 导出字段必须按角色裁剪后才能上线。
