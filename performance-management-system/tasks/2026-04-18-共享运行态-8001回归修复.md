<!-- 文件职责：记录 shared latest `8001` 运行态回归排查、正式回退到 `8006` 的决策依据和收口结论；不负责主题 6 业务事实源冻结或任何新功能实现；依赖 14/15/20/25/26 文档、主题 1 最终验收卡、主题 6 验证卡与 cool-admin-midway 实际运行日志；维护重点是把“为什么不再以 `8001` 作为 current latest”留痕清楚。 -->
# 共享运行态任务卡：8001 回归修复与 current latest 回退

## 基本信息

- 任务名称：shared latest `8001` 运行态回归修复
- 当前阶段：二期 / shared latest / 回归修复
- 当前状态：done
- 任务等级：L3
- 涉及端：`cool-admin-midway`、`performance-management-system`
- 负责人：Codex
- 创建时间：2026-04-18
- 最近更新时间：2026-04-18

## 结论

- 当前唯一正式结论：
  - `8001` shared latest 回归未恢复；根据正式决策，current latest 已回退到 `8006`
- 当前是否存在未关闭 `P0 / P1`
  - `P0`：无
  - `P1`：无

## 根因

1. 当前 `8001` 监听 PID 仍来自同仓库历史 dev 实例：
   - `lsof -a -p 82375 -d cwd,1,2`
   - 证据：
     - `cwd = /Users/shaotongli/Documents/xuedao/cool-admin-midway`
     - `stdout/stderr = /private/tmp/cool-admin-module4-dev.log`
2. 当前仓库代码与 `dist` 已包含主题 6 和历史导出接口：
   - `src/modules/performance/controller/admin/dashboard.ts` 已存在 `@Get('/crossSummary')`
   - `src/modules/performance/controller/admin/feedback.ts` 已存在 `@Post('/export')`
   - `src/modules/performance/controller/admin/pip.ts` 已存在 `@Post('/export')`
   - `dist/modules/performance/controller/admin/dashboard.js` 也已存在 `crossSummary`
3. 但 current latest `8001` 实例仍返回路由级 `404`：
   - `GET /admin/performance/dashboard/crossSummary`
   - `POST /admin/performance/feedback/export`
   - `POST /admin/performance/pip/export`
4. 本轮尝试通过同仓库守卫和 shell 终止旧实例失败：
   - `npm run dev:port:free` 返回 `kill EPERM`
   - `kill -TERM 82375` 返回 `operation not permitted`
5. 进一步证据表明 watch / keepalive 已尝试按最新代码重启，但被旧 `8001` 实例占端口阻断：
   - `/private/tmp/cool-admin-module4-dev.log` 在本轮 `touch controller` 后持续出现 `Error: listen EADDRINUSE: address already in use :::8001`
   - 同日志同时记录 `NotFoundError: /admin/performance/dashboard/crossSummary Not Found`、`feedback/export Not Found`、`pip/export Not Found`

## 本轮执行命令

1. 后端构建：
   - `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && npm run build`
   - 结果：通过
2. 联调 seed：
   - `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && node ./scripts/seed-stage2-performance.mjs`
   - 结果：通过
3. current latest smoke：
   - `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && STAGE2_SMOKE_BASE_URL=http://127.0.0.1:8001 npm run smoke:stage2:performance`
   - 结果：失败
   - 汇总：`PASS 41 / FAIL 9 / SKIP 0 / Conclusion: FAILED`
4. 运行态诊断：
   - `lsof -nP -iTCP:8001 -sTCP:LISTEN`
   - `lsof -a -p 82375 -d cwd,1,2`
   - `curl -sS -o /tmp/8001_cross_after_touch.txt -w '%{http_code}\n' 'http://127.0.0.1:8001/admin/performance/dashboard/crossSummary'`
   - `curl -sS -o /tmp/8001_feedback_after_touch.txt -w '%{http_code}\n' -X POST 'http://127.0.0.1:8001/admin/performance/feedback/export' -H 'Content-Type: application/json' -d '{}'`
   - `curl -sS -o /tmp/8001_pip_after_touch.txt -w '%{http_code}\n' -X POST 'http://127.0.0.1:8001/admin/performance/pip/export' -H 'Content-Type: application/json' -d '{}'`
   - 结果：
     - 三个接口均返回 `HTTP 200`，但响应体是路由级 `404 Not Found`
5. 旧实例释放尝试：
   - `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && npm run dev:port:free`
   - `kill -TERM 82375`
   - 结果：均失败，旧 `8001` 监听仍在

## Smoke 失败明细

1. `hr_admin dashboard:crossSummary`
   - `code=404 message=/admin/performance/dashboard/crossSummary Not Found`
2. `manager_rd dashboard:crossSummary`
   - `code=404 message=/admin/performance/dashboard/crossSummary Not Found`
3. `employee_platform dashboard:crossSummary`
   - 期望 `无权限查看跨模块驾驶舱`
   - 实际 `/admin/performance/dashboard/crossSummary Not Found`
4. `hr_admin / manager_rd / employee_platform feedback:export`
   - 统一返回 `/admin/performance/feedback/export Not Found`
5. `hr_admin / manager_rd / employee_platform pip:export`
   - 统一返回 `/admin/performance/pip/export Not Found`

## 当前判断

1. 当前仓库代码实现不是阻塞点：
   - build 通过
   - `src` 与 `dist` 均存在缺失路由
2. 当前唯一阻塞点是 shared latest `8001` 运行实例本身：
   - 旧 detached 进程未退出
   - 新 latest child 无法抢占 `8001`
3. 用户已明确选择“不继续修 `8001`，正式把 current latest 改回 `8006`”：
   - 因此本卡不再把“恢复 `8001`”作为当前阻塞项
   - 本卡转为 shared runtime 回退决策的诊断证据
4. `8001` 当前保留为历史治理目标与排查入口：
   - 若后续再次尝试把 current latest 提升回 `8001`，必须另开任务重新治理

## 下一步

1. 以 `8006 / 9000 -> /dev -> 8006` 作为 current latest 继续推进主题 6 最终验收。
2. 若后续需要重新推进主题 1 的 `8001` 统一治理，必须重开 shared runtime 治理任务并重新补齐 smoke / GUI / 文档同步证据。
