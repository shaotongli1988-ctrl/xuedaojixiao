<!-- 文件职责：记录“导航栏与侧栏改造”窗口B的菜单分组与业务域映射实现范围、写集与验证门槛；不负责壳层样式细节、不负责业务页面实现；依赖业务域归属冻结与现有菜单 store / 分组开关；维护重点是把当前统一技术挂载组稳定映射到正式业务域分组。 -->
# 导航栏与侧栏改造 B：菜单分组与业务域映射

## 基本信息

- 任务名称：导航栏与侧栏改造 B - 菜单分组与业务域映射
- 当前阶段：二期 / 阶段1 / 前端实现
- 当前状态：reviewing
- 任务等级：L4
- 涉及端：`cool-admin-vue`
- 负责人：Codex
- 创建时间：2026-04-18
- 最近更新时间：2026-04-18

## 当前结论

- 当前正式结论：`窗口B菜单分组与业务域映射实现已完成，待人工点测后关单`

## Scope

1. 按已冻结业务域重组当前菜单 group/list
2. 启用并稳定 `isGroup` 导航分组链路
3. 保持现有动态菜单、权限列表和首页路径定位逻辑可用

## Non-goals

1. 不改后端返回菜单数据结构
2. 不改权限资源名或按钮权限
3. 不改业务页面内部行为
4. 不做新的菜单配置平台

## 建议写集

1. `/Users/shaotongli/Documents/xuedao/cool-admin-vue/src/config/index.ts`
2. `/Users/shaotongli/Documents/xuedao/cool-admin-vue/src/modules/base/store/menu.ts`
3. `/Users/shaotongli/Documents/xuedao/cool-admin-vue/src/modules/base/pages/main/components/bmenu.tsx`
4. 本任务卡

## 冻结业务域映射

1. `绩效中心`
   - `dashboard / my-assessment / initiated / pending / indicator-library / feedback / goals / pip / promotion / salary`
2. `招聘中心`
   - `talentAsset / resumePool / interview`
3. `培训中心`
   - `course / course-learning / capability / certificate`
4. `会议管理`
   - `meeting`
5. `合同管理`
   - `contract`
6. `采购管理`
   - `purchase-order / supplier`

## 禁止事项

1. 不修改 `cool-admin-midway/src/modules/base/menu.json`
2. 不引入第二套本地硬编码菜单源
3. 不把 `modules/performance/` 技术目录当成一级业务域命名依据
4. 不反向改写已冻结的页面归属

## Acceptance

1. 一级导航分组按业务域正确显示
2. 左侧栏只显示当前一级业务域下的菜单
3. 当前路径切换、首次进入和刷新后都能正确定位一级分组
4. 权限过滤后，空业务域不误显示

## Verification Plan

1. `corepack pnpm run type-check`
2. `corepack pnpm run build`
3. 角色点测：
   - `HR` 可见完整业务域
   - `经理` 看不到越权业务入口
   - `员工` 不误见后台管理域
4. 路由点测：
   - 直达二级页后一级分组能正确高亮
   - 刷新后一级分组不丢失

## 风险

1. `menu.ts` 是共享菜单核心，分组错误会影响全部后台入口
2. `isGroup` 打开后会改变左侧菜单来源，必须回归首页和路径定位

## 当前阻塞项

- 当前唯一阻塞项：`角色级人工点测证据尚未回写`

## 已完成改动

1. 已启用 `config.app.menu.isGroup = true`
2. 已在 `menu.ts` 内将统一 `/performance` 技术挂载组按冻结业务域拆分为六个一级分组
3. 已补 `activeGroupIndex / currentGroup` 运行态状态，供顶栏和左侧栏联动
4. 已保留现有后端动态菜单源，不改 `menu.json` 结构
5. 已将当前真实存在的 `suggestion` 页面保留在 `绩效中心` 分组，避免因事实源落后而丢入口

## 执行命令

1. `cd /Users/shaotongli/Documents/xuedao/cool-admin-vue && corepack pnpm run type-check`
2. `cd /Users/shaotongli/Documents/xuedao/cool-admin-vue && corepack pnpm run build`

## 执行结果

1. `type-check`：通过
2. `build`：通过

## 剩余风险

1. 当前仍缺 `HR / 部门经理 / 员工` 三角色的真实菜单显隐点测留痕
2. 一级导航目前保留其他顶级后台组并与六个业务域并存，符合“不改后端菜单源”的范围，但仍需最终人工确认是否接受该运行态呈现

## 下一步

1. 交由窗口 C 补回角色权限与刷新直达点测
2. 无新增入口漂移后推进为 `done`
