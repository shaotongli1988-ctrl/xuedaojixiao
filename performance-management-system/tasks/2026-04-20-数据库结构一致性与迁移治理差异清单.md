# 数据库结构一致性与迁移治理差异清单

## 2026-04-20 实施进展

- 已完成：
  - `performance_vehicle` 正式 migration 落地
  - `performance_intellectual_property` 正式 migration 落地
  - `performance_suggestion` 正式 migration 落地
  - `performance_assessment / assessment_score / goal / goal_progress / indicator / pip / pip_record / promotion / promotion_record / salary / salary_change` 正式 migration 基线补齐
  - `performance_course / course_enrollment / course_recite / course_practice / course_exam / capability_model / capability_item / capability_portrait / certificate / certificate_record` 正式 migration 基线补齐
  - `performance_interview / goal_ops_department_config / goal_ops_plan / goal_ops_report / meeting / talent_asset / resume_pool / hiring / recruit_plan / job_standard` 正式 migration 基线补齐
  - `performance_office_collab / teacher_info / teacher_follow / teacher_class / teacher_agent / teacher_agent_relation / teacher_attribution / teacher_attribution_conflict / teacher_agent_audit / contract / document_center / knowledge_base / knowledge_qa` 正式 migration 基线补齐
  - `performance_supplier / purchase_order / asset_info / asset_assignment / asset_maintenance / asset_procurement / asset_transfer / asset_inventory / asset_depreciation / asset_disposal / feedback_task / feedback_record / approval_config / approval_config_node / approval_instance / approval_instance_node / approval_action_log` 正式 migration 基线补齐
  - `vehicle.ts` 与 `intellectualProperty.ts` 已移除运行时懒建表逻辑
  - `ensure-local-performance-schema.mjs` 已改为统一调用 `run-migrations.mjs up`
  - `20260420114500-add-performance-interview-source-snapshot.mjs` 已补空库兼容，不再因为 `performance_interview` 缺失导致整条 migration 链失败
  - `seed-stage2-performance.mjs` 已移除全部 `performance_*` 建表与补列逻辑，仅保留数据写入
  - `performance` 域 migration 基线按基座口径统一：
    - 公共字段沿用 `BaseEntity`
    - `createTime / updateTime` 统一为 `varchar(255)`
    - `tenantId` 统一为 `int(11)`
    - 表字符集与排序规则统一为 `utf8mb4 / utf8mb4_general_ci`
    - 历史 `bigint` 偏移结构按实体契约回收到基座整数口径
  - `seed-stage2-performance.mjs` 已改为读取 `LOCAL_DB_HOST / LOCAL_DB_PORT / LOCAL_DB_USER / LOCAL_DB_PASSWORD / LOCAL_DB_NAME`
  - 课程学习 seed 写入已按基座实体口径移除非实体字段 `taskType`

- 已验证：
  - `npm test -- --runInBand test/performance/vehicle.service.test.ts test/performance/intellectual-property.service.test.ts test/performance/suggestion.service.test.ts`
  - `npx tsc -p tsconfig.json --noEmit`
  - `node --check`：
    - `cool-admin-midway/scripts/seed-stage2-performance.mjs`
    - `cool-admin-midway/scripts/migrations/20260420141000-add-performance-learning-capability-certificate-tables.mjs`
    - `cool-admin-midway/scripts/migrations/20260420142000-add-performance-recruitment-goalops-meeting-tables.mjs`
    - `cool-admin-midway/scripts/migrations/20260420143000-add-performance-teacher-office-knowledge-tables.mjs`
    - `cool-admin-midway/scripts/migrations/20260420144000-add-performance-procurement-asset-feedback-approval-tables.mjs`
  - 临时库 `cool_schema_verify_temp2`：
    - `LOCAL_DB_NAME=cool_schema_verify_temp2 node ./scripts/ensure-local-performance-schema.mjs`
    - `LOCAL_DB_NAME=cool_schema_verify_temp2 node ./scripts/run-migrations.mjs status`
  - 全新临时库 `cool_schema_verify_temp4`：
    - `LOCAL_DB_NAME=cool_schema_verify_temp4 node ./scripts/ensure-local-performance-schema.mjs`
    - 暴露出 `seed-stage2-performance.mjs` 数据库连接曾写死到 `cool`，已修复
  - 全新临时库 `cool_schema_verify_temp7`：
    - 先复制 stage2 seed 所需最小基座依赖表：`base_sys_menu / base_sys_department / base_sys_user / base_sys_role / base_sys_role_menu / base_sys_role_department / base_sys_user_role / base_sys_param / space_info`
    - `LOCAL_DB_NAME=cool_schema_verify_temp7 node ./scripts/ensure-local-performance-schema.mjs`
    - `LOCAL_DB_NAME=cool_schema_verify_temp7 node ./scripts/seed-stage2-performance.mjs`
  - 实测结果：
    - `cool_schema_verify_temp7` 中 `performance_*` 表数量 = `71`
    - `cool_schema_verify_temp7.cool_schema_migration` 已应用 migration 数量 = `12`
    - `cool-admin-midway/src/modules/performance/entity/*.ts` 实体文件数量 = `71`
    - `seed-stage2-performance.mjs` 中已不再存在 `CREATE TABLE IF NOT EXISTS performance_*`
    - `seed-stage2-performance.mjs` 中已不再存在 `ensureTableColumn(...)`
    - `cool_schema_verify_temp7.base_sys_menu` 中路由探针数量 = `3`
    - `cool_schema_verify_temp7.performance_course_recite` 种子行数 = `3`

## 结论

- 当前本地库 `base_sys_* + performance_*` 与代码实体定义数量一致。
- `performance` 域表结构治理路径现已统一到正式 migration。
- 当前口径：
  - schema 唯一事实源：`cool-admin-midway/scripts/migrations/*.mjs`
  - 代码映射事实源：`cool-admin-midway/src/modules/performance/entity/*.ts`
  - `ensure-local-performance-schema.mjs` 只负责执行 migration
  - `seed-stage2-performance.mjs` 只负责写数据，不再建表/补列
  - 运行时 service 不再承担 schema 初始化职责
- 从本次已执行的 migration + 最小基座依赖前提下的 seed 闭环结果看，`performance` 域“结构存在”和“治理一致”已经对齐。

## 核验范围

- 实体来源：
  - `cool-admin-midway/src/modules/base/entity/sys/*.ts`
  - `cool-admin-midway/src/modules/performance/entity/*.ts`
- 正式迁移：
  - `cool-admin-midway/scripts/migrations/*.mjs`
- 本地兜底：
  - `cool-admin-midway/scripts/ensure-local-performance-schema.mjs`
- seed 数据：
  - `cool-admin-midway/scripts/seed-stage2-performance.mjs`
- 运行时懒建表收口：
  - `cool-admin-midway/src/modules/performance/service/vehicle.ts`
  - `cool-admin-midway/src/modules/performance/service/intellectualProperty.ts`

## 一致性结果

- `base_sys_* + performance_*` 实体总数：`81`
- 本地数据库 `cool` 中对应表总数：`81`
- 临时库 `cool_schema_verify_temp7` 中 `performance_*` 表总数：`71`
- `missingInDb = []`
- `extraInDb = []`

这说明当前环境里的“表是否存在”与“表由谁治理”都已经对齐。

补充：

- `seed-stage2-performance.mjs` 不是“从零装系统”脚本。
- 它的已验证前提是：基座最小依赖表已存在。
- 在这个前提下，`performance` 域已不再依赖 seed 自建表。

## 已纳入正式迁移的表

来自 `cool-admin-midway/scripts/migrations`：

1. `20260419231000-add-performance-material-tables.mjs`
   - `performance_material_catalog`
   - `performance_material_stock`
   - `performance_material_inbound`
   - `performance_material_issue`
   - `performance_material_stock_log`
2. `20260420001000-add-performance-work-plan-table.mjs`
   - `performance_work_plan`
3. `20260420103000-add-performance-asset-assignment-request-table.mjs`
   - `performance_asset_assignment_request`
4. `20260420114500-add-performance-interview-source-snapshot.mjs`
   - `performance_interview.sourceSnapshot` 列补丁
5. `20260420123000-add-performance-vehicle-table.mjs`
   - `performance_vehicle`
6. `20260420124000-add-performance-intellectual-property-table.mjs`
   - `performance_intellectual_property`
7. `20260420125000-add-performance-suggestion-table.mjs`
   - `performance_suggestion`
8. `20260420132000-add-performance-core-mainline-tables.mjs`
   - `performance_assessment`
   - `performance_assessment_score`
   - `performance_goal`
   - `performance_goal_progress`
   - `performance_indicator`
   - `performance_pip`
   - `performance_pip_record`
   - `performance_promotion`
   - `performance_promotion_record`
   - `performance_salary`
   - `performance_salary_change`
9. `20260420141000-add-performance-learning-capability-certificate-tables.mjs`
10. `20260420142000-add-performance-recruitment-goalops-meeting-tables.mjs`
11. `20260420143000-add-performance-teacher-office-knowledge-tables.mjs`
12. `20260420144000-add-performance-procurement-asset-feedback-approval-tables.mjs`

结论：当前 `performance` 域所有实体表均已纳入当前仓库正式 migration 体系。

## 由 Seed 负责建表的表

- 无

结论：`seed-stage2-performance.mjs` 已退出 schema 治理路径。

## 由本地兜底脚本负责的表

- 无独立建表表项，脚本仅执行 migration runner

结论：本地兜底脚本已不再定义 schema。

## 由服务层运行时懒建表的表

- 无

结论：运行时首次访问触发建表的治理路径已被移除。

## 风险分级

### 已关闭的 `P1` 治理缺口

1. 迁移体系不完整
   - 已通过 `20260420141000 / 20260420142000 / 20260420143000 / 20260420144000` 补齐。
2. 多套建表路径并存
   - 已收口为 migration 单一路径。
3. 运行时懒建表不可控
   - `vehicle`、`intellectual_property` 已收回正式 migration。
4. 历史遗留表来源不清
   - 主链表和 stage2 表已全部纳入当前仓库正式 schema 归档。

### 当前未见 `P0`

- 本地环境里实体与表存在性一致，尚未发现“表不存在导致主链不可用”的系统性缺口。

## 当前冻结口径

- `migration` 负责正式 schema
- `seed` 只负责测试数据和账号/菜单/业务样例
- `ensure-local` 只允许调正式 migration
- `service` 不再承担建表职责
- `performance` 域 schema 以基座公共字段与 entity 契约为唯一结构准绳
