<!-- 文件职责：记录二期阶段 1 主题 3 窗口 D「cool-uni 360 环评与统一状态/守卫实现」的范围、实现证据、验证结果与剩余风险；不负责其他业务页面实现、后端接口开发或主文档主写；依赖主题 3 冻结卡、设计验收卡、权限/接口/信息架构设计卡与 cool-uni 现有壳工程；维护重点是只能在既有验证码登录、token、permmenu、feedback 接口边界内完成环评主链与页面状态守卫。 -->
# 二期阶段 1 任务卡：主题 3 - cool-uni 实现 D 环评与守卫

## 基本信息

- 任务名称：二期阶段 1 - 主题 3 `cool-uni` 实现 D「360 环评 + 统一页面状态与路由守卫」
- 当前阶段：二期 / 阶段 1 / 实现
- 当前状态：done
- 任务等级：L4
- 涉及端：`cool-uni`
- 负责人：Codex
- 创建时间：2026-04-17
- 最近更新时间：2026-04-17

## Before Starting

- Scope：
  - 落地 `360 环评` 列表、任务详情、填写反馈、查看汇总。
  - 落地未登录跳转、无权限占位、空状态、错误态、来源缺失返回工作台。
  - 复用现有 `captcha / login / refreshToken / permmenu / logout / feedback` 接口。
- Non-goals：
  - 不做导出。
  - 不做 `PIP / 晋升 / 驾驶舱 / HR` 后台移动化。
  - 不新增 `cool-admin-midway` 业务接口。
  - 不改 `performance-management-system/docs` 主文档。
- Affected modules：
  - `/Users/shaotongli/Documents/xuedao/cool-uni/`
  - 本任务卡
- Acceptance criteria：
  - 员工可查看本人相关环评任务、任务详情、填写本人反馈、查看汇总。
  - 经理可查看部门树范围内环评任务、详情和汇总。
  - 未登录访问受保护页会跳登录页。
  - 无权限、空数据、错误态、来源缺失均有明确页面行为。
  - `type-check / build` 通过。
- Impact surface：
  - `cool-uni` 路由
  - `cool-uni` 登录态与权限缓存
  - `cool-uni` 本地 service 封装
  - `cool-uni` 反馈页面与统一状态组件
- Source of truth：
  - `/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-17-二期主题3-cool-uni冻结.md`
  - `/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-17-二期阶段1-主题3-cool-uni-信息架构设计.md`
  - `/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-17-二期阶段1-主题3-cool-uni-接口复用设计.md`
  - `/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-17-二期阶段1-主题3-cool-uni-权限与登录设计.md`
  - `/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-17-二期阶段1-主题3-cool-uni-设计最终验收.md`
- Requirement conflicts or unclear points that must be resolved before coding：
  - 无；设计阶段已验收通过，本窗口按既有冻结范围实现。
- High-risk surfaces：
  - 登录态复用
  - `permmenu` 权限显隐
  - 环评数据范围与脱敏
  - `401 / 403 / 空数据 / 异常态` 页面反馈
- Required delivery evidence：
  - 代码变更
  - `type-check`
  - `build`
  - 最小点测记录
- Required test matrix：
  - 正常：列表、详情、填写、汇总
  - 异常：接口失败
  - 边界：来源缺失、记录不存在、草稿/已关闭状态
  - 权限：未登录、无权限、员工/经理范围差异
- Key assumptions and constraints：
  - 允许在 `cool-uni` 内做实现所需的最小共享登录态适配。
  - 若环境账号或后端数据不足，仅记录为验证风险，不扩大代码范围。

## During Implementation

- Change summary：
  - 新增 `cool-uni` 360 环评四页：
    - `pages/performance/feedback/list.vue`
    - `pages/performance/feedback/detail.vue`
    - `pages/performance/feedback/submit.vue`
    - `pages/performance/feedback/summary.vue`
  - 新增环评页共享能力：
    - `service/performance/feedback.ts` 复用后端 `feedback page/info/submit/summary`
    - `types/performance-feedback.ts`、`pages/performance/feedback/utils.ts`
    - `pages/performance/components/page-state.vue`、`pages/performance/components/status-pill.vue`
  - 路由与登录态守卫收口：
    - `router/index.ts` 在受保护路由进入前补 `user.hydrate()`
    - `cool/service/request.ts` 增加 `403` 页面态透传
    - 列表到详情、详情到提交/汇总全部补齐来源参数；来源缺失统一 toast 后回工作台
  - 工程兼容收口：
    - `pages.json` 挂载首批 `feedback/*` 页面
    - `cool/service/index.ts` 注册 `performance.feedback`
    - `vite.config.ts`、`tsconfig.json`、`shim/vue.ts`、`package.json`、`pnpm-lock.yaml` 保持当前 `cool-uni` 构建链路可编译
- Reused modules or patterns：
  - 复用现有 `admin/base/open` 验证码登录、`admin/base/comm/permmenu` 权限菜单、`useStore` 登录态、`useCool` 路由/请求注入。
  - 复用后端既有 `admin/performance/feedback/page|info|submit|summary`，未新增移动端专用接口。
  - 页面状态统一收敛到 `page-state`，状态标签统一收敛到 `status-pill`。
- New dependencies and why existing tools were insufficient：
  - 无新增生产依赖。
  - 本窗口仅在现有 `cool-uni + vite + uni-app` 工程内补兼容配置与锁文件，不引入新技术栈。
- Verification run after each meaningful change：
  - 页面与服务接线后执行：`cd /Users/shaotongli/Documents/xuedao/cool-uni && corepack pnpm exec tsc --noEmit -p tsconfig.json`
    - 结果：通过。
  - 路由守卫与来源守卫补齐后执行：`cd /Users/shaotongli/Documents/xuedao/cool-uni && UNI_INPUT_DIR=. corepack pnpm exec vite build`
    - 结果：通过；存在 `sass @import` 与 `NODE_ENV=production` 提示，但未阻断构建。
  - 联调验证执行：使用真实后端 `http://127.0.0.1:8001` 做验证码登录 + `feedback add/page/info/submit/summary` 探针。
    - 结果：通过，见下方正常/权限/脱敏证据。
- Normal-path evidence：
  - `employee_platform` 可看到本人相关任务列表并打开详情。
  - `feedback_peer` 可在新建任务上进入详情并提交一次反馈，`submit` 返回 `code=1000`。
  - `manager_rd` 查看同一任务汇总，`records.length = 1`，可见完整记录。
- Error-path evidence：
  - 请求封装透传 `403`，详情/提交/汇总页可进入无权限页面态。
  - 列表/详情/汇总请求失败时统一进入页面级错误态并提供“重试”动作。
- Boundary-case evidence：
  - 二级页来源缺失或无效时，页面 toast `页面来源已失效，已返回工作台` 后回工作台。
  - 详情不存在时进入“记录不存在或已失效”状态。
  - 员工查看汇总时即使任务已关闭，也只展示汇总指标，不展示他人单条反馈明细。
- Permission checks：
  - 未登录访问受保护页：`router/index.ts` 拦截并跳登录页。
  - 列表页无权限：`user.canAccessRoute('/pages/performance/feedback/list') === false` 时展示无权限占位。
  - 详情/提交/汇总无权限：后端 `403` 进入无权限状态页。
  - 经理范围：`manager_rd` 新建/查看部门树范围内员工任务，联调探针确认列表可见、汇总可见明细。
  - 员工范围：`employee_platform` 仅查看本人任务，联调探针确认同任务汇总 `records.length = 0`。
- State-flow checks：
  - 任务详情 `running` 时可进入提交页；提交成功后返回详情，服务端将单评价人任务自动推进为 `closed`。
  - 关闭后员工详情无 `currentUserRecord`，页面仍可查看详情与汇总指标，但不可再次提交。
- Contract checks：
  - 移动端仅调用：
    - `POST /admin/performance/feedback/page`
    - `GET /admin/performance/feedback/info`
    - `POST /admin/performance/feedback/submit`
    - `GET /admin/performance/feedback/summary`
  - 未新增后端字段、权限键或移动端专用接口。
- Change-type verification mapping：
  - `cool-uni` 页面变更对应 `type-check / build / 最小点测`。
- High-risk trigger changes：
  - 登录态、权限缓存、受保护路由、反馈详情/汇总脱敏展示。
- Compatibility or fallback impact：
  - 列表页动作收敛为“查看详情”，提交与汇总只从详情页进入，避免绕过来源守卫。
  - 若登录态丢失或 `permmenu` 未完成 hydration，路由前置守卫直接回登录，不落入业务页空白态。
- Privacy/PII review result：
  - 员工汇总页仅展示汇总指标，不展示他人 `content / score / feedbackUserName` 记录明细。
  - 经理汇总页仅显示本部门树范围任务的已授权反馈记录，未扩展导出或缓存落盘。
- Docs or operational updates made：
  - 仅回写本任务卡。

## Blockers And Waivers

- Open `P0` blockers：
  - 无
- Open `P1` blockers：
  - 无
- Open `P2` issues：
  - `cool-uni` 当前未固化 `vue-tsc` 脚本，窗口内已用 `tsc --noEmit + vite build` 作为类型与构建门禁。
    - owner：二期主题 3 移动端主窗口
    - deadline：进入统一提测前
    - cleanup action：补齐 `vue-tsc` 命令与 `package.json` type-check 脚本，统一后续移动端门禁口径
- Approved waivers：
  - 无

## Before Commit

- Diff review result：
  - 仅落在 `cool-uni` 和本任务卡允许范围内；未改后端接口、共享鉴权基础层或主文档。
- Verification summary：
  - `tsc --noEmit`：通过。
  - `UNI_INPUT_DIR=. vite build`：通过。
  - 真实后端联调探针：通过。
  - 进入联调后复跑窗口 D 主链探针：通过。
- What was run：
  - `cd /Users/shaotongli/Documents/xuedao/cool-uni && corepack pnpm exec tsc --noEmit -p tsconfig.json`
  - `cd /Users/shaotongli/Documents/xuedao/cool-uni && UNI_INPUT_DIR=. corepack pnpm exec vite build`
  - `node <<'EOF' ... EOF`
    - 登录：`hr_admin / manager_rd / employee_platform / feedback_peer`
    - 接口：`feedback/add -> page -> info -> submit -> summary`
  - `node <<'EOF' ... EOF`
    - 联调复跑：`feedback/add -> manager page -> employee page -> peer info -> submit -> manager summary -> employee summary -> employee info`
- What passed：
  - 前端类型检查通过。
  - 前端构建通过。
  - 联调探针创建任务 `id=14` 成功；`feedback_peer` 提交成功；`manager_rd` 汇总明细 `records.length=1`；`employee_platform` 汇总明细 `records.length=0`；员工详情提交后 `status=closed`。
  - 联调复跑创建任务 `id=15` 成功；经理可见、员工可见本人任务、评价人可提交、经理可见 1 条汇总记录、员工汇总和详情均完成脱敏、任务提交后自动关闭。
- What was not run：
  - `vue-tsc`
    - 原因：当前工程未提供可执行 `vue-tsc` 命令，`corepack pnpm exec vue-tsc --noEmit -p tsconfig.json` 返回 `Command "vue-tsc" not found`。
- Remaining risk：
  - 当前 `cool-uni` 的官方 type-check 门禁仍未统一到 `vue-tsc`，后续其他窗口若引入更复杂 SFC 泛型场景，仍建议补齐统一脚本。
- Tests added or updated, or why not：
  - 本窗口未新增自动化测试文件。
  - 原因：当前 `cool-uni` 仓库未内置移动端页面级测试框架，窗口目标要求是 `type-check / build / 最小点测`；已以真实后端探针补齐行为证据。
- Commit scope statement：
  - 本窗口不提交，按任务范围交付工作区改动。
- Commit summary：
  - `cool-uni` 完成 360 环评四页、统一页面状态与路由守卫收口，并通过 `tsc + build + 真实后端联调探针` 验证。

## Before Release Or Handoff

- Acceptance-to-verification mapping：
  - 环评任务查看、详情、填写、汇总：
    - 由 `feedback list/detail/submit/summary` 页面实现 + 真实探针验证通过。
  - 员工只看本人、经理只看部门树范围：
    - 由后端服务端数据裁剪 + 探针结果证明员工汇总无明细、经理汇总有明细。
  - 未登录/无权限/空状态/错误态/来源缺失：
    - 由 `router/index.ts`、`cool/service/request.ts`、`page-state.vue`、各页来源校验实现。
  - `type-check / build`：
    - `tsc --noEmit`、`vite build` 通过。
- Contract alignment result：
  - 与冻结卡、接口复用设计卡一致；仅复用既有 `feedback` 四个接口，无新增接口、字段或权限键。
- Config changes：
  - 无新增环境变量。
  - 构建执行口径为：`UNI_INPUT_DIR=. corepack pnpm exec vite build`。
- Migration plan：
  - 无数据库变更。
- Rollout steps：
  - 安装当前 `pnpm-lock.yaml` 对应依赖。
  - 启动现有后端并确保 `http://127.0.0.1:8001` 可访问。
  - 执行 `tsc --noEmit` 与 `UNI_INPUT_DIR=. vite build`。
  - 使用验证码登录链路做移动端联调。
- Rollback path：
  - 回退本窗口在 `cool-uni` 与本任务卡中的改动。
  - 若需清理联调探针写入的环评任务数据，重跑 `/Users/shaotongli/Documents/xuedao/cool-admin-midway/scripts/seed-stage2-performance.mjs` 恢复 stage2 基线。
- Feature flag, kill switch, or staged rollout plan：
  - 不涉及。
- Data recovery path：
  - 页面实现本身无持久化变更。
  - 联调探针创建了任务 `id=14`、`id=15` 并写入反馈记录；恢复方式为重跑 stage2 seed。
- Monitoring and diagnostic entry points：
  - 前端入口：`/pages/performance/feedback/list`
  - 后端接口：`/admin/performance/feedback/page|info|submit|summary`
  - 诊断点：登录态缓存、`permmenu`、`403` 页面态、来源参数 `source`
- Final `P0` and `P1` blocker check：
  - `P0`：无
  - `P1`：无
- Approved waivers：
  - 无。
- Final release risk statement：
  - 当前窗口 D 已满足进入联调的实现与验证门槛；剩余仅有 `vue-tsc` 脚本未固化这一 `P2` 工程治理项，不阻断本窗口收口。
