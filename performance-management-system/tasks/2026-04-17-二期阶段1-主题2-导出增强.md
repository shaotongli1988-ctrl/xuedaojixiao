<!-- 文件职责：记录二期阶段 1 主题 2「导出增强」后端主实现的范围、验证证据、阻塞与收口结论；不负责前端页面、文档主文件或首期范围外导出能力；依赖 04、06、09、12 文档与主题 2 冻结任务卡；维护重点是只能落地 360 环评汇总导出和 PIP 摘要导出，且不得突破权限与脱敏边界。 -->
# 二期阶段 1 任务卡：主题 2 - 导出增强

## 基本信息

- 任务名称：二期阶段 1 - 主题 2「导出增强」后端主实现
- 当前阶段：二期 / 阶段 1 / 后端主实现
- 当前状态：done
- 任务等级：L4
- 涉及端：`cool-admin-midway`、`performance-management-system`
- 负责人：Codex
- 创建时间：2026-04-17
- 最近更新时间：2026-04-17

## Before Starting

- Scope：
  - 落地 `feedback/export` 与 `pip/export` 两类后端导出接口和服务。
  - 输出字段严格限定为冻结包中的摘要字段矩阵。
  - 权限键独立校验，员工无权导出，经理仅可导出所辖部门树内摘要。
  - 导出成功/失败都记录最小审计留痕。
- Non-goals：
  - 不开发前端页面。
  - 不实现晋升导出或薪资导出。
  - 不开发通用导出平台、异步导出中心或 cool-uni。
  - 不改 docs 主文档。
- Affected modules：
  - `cool-admin-midway/src/modules/performance/`
  - `cool-admin-midway/src/modules/base/menu.json`
  - `cool-admin-midway/scripts/`
  - `cool-admin-midway/test/performance/`
  - 本任务卡
- Acceptance criteria：
  - 新增 `/admin/performance/feedback/export` 与 `/admin/performance/pip/export`。
  - 无权限时返回 `无权限导出该数据`。
  - 无数据时返回 `当前筛选条件下无可导出数据`。
  - 超过 `5000` 行时返回 `导出结果超过上限，请缩小筛选范围后重试`。
  - 导出内容不包含任何冻结包明令禁止的全文和敏感字段。
  - 导出行为写入审计留痕。
- Impact surface：
  - 后端导出接口
  - 权限键校验
  - 数据范围裁剪
  - 脱敏字段裁剪
  - 审计留痕
- Source of truth：
  - [04-API设计.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/04-API设计.md)
  - [06-权限矩阵.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/06-权限矩阵.md)
  - [09-测试清单.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/09-测试清单.md)
  - [12-数据权限与脱敏规则.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/12-数据权限与脱敏规则.md)
  - [2026-04-17-二期主题2-导出增强冻结.md](/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-17-二期主题2-导出增强冻结.md)
- Requirement conflicts or unclear points that must be resolved before coding：
  - API 事实源冻结了 `xlsx` 文件格式，但仓库当前 `assessment/goal` 已有导出契约是“后端返回摘要数组，前端生成 xlsx”，且本窗口禁止改前端；本轮后端沿用既有数组契约，不新增后端二进制下载流。
  - 当前 shell 环境中的旧 `8001` 监听实例无法被沙箱直接终止；运行态联调改用 fresh `8006` 实例完成等效验证，并在任务卡中留痕。
- High-risk surfaces：
  - 导出权限独立校验
  - 数据范围边界
  - 脱敏字段裁剪
  - 审计留痕
- Required delivery evidence：
  - 主任务卡回写
  - 运行态菜单权限注册
  - seed / smoke 角色闭环证据
  - 后端接口 / 服务实现
  - 最小自动化验证
  - 风险与阻塞说明
- Required test matrix：
  - 正常路径：HR / 经理可导出各自范围内摘要。
  - 异常路径：员工无权导出；无数据报错。
  - 边界路径：超过 `5000` 行报错；导出字段不包含禁导字段。
- Key assumptions and constraints：
  - 审计留痕复用现有 `base_sys_log` 承载，不新增共享基础层改动。
  - 本轮仅补 `performance:feedback:export`、`performance:pip:export` 的最小运行态权限闭环，不扩写其他导出主题。

## During Implementation

- Change summary：
  - 新增 `POST /admin/performance/feedback/export`，服务侧完成独立导出权限校验、部门树范围裁剪、摘要字段白名单映射、5000 行上限校验和 `base_sys_log` 审计留痕。
  - 新增 `POST /admin/performance/pip/export`，服务侧完成独立导出权限校验、部门树范围裁剪、摘要字段白名单映射、5000 行上限校验和 `base_sys_log` 审计留痕。
  - 新增两组后端纯逻辑测试，覆盖权限拒绝、超限、摘要字段导出和审计留痕。
  - 补齐 `cool-admin-midway/src/modules/base/menu.json` 中 `performance:feedback:export`、`performance:pip:export` 运行态权限注册。
  - 补齐 `seed-stage2-performance.mjs` 的 HR / 经理导出权限绑定与 `smoke-stage2-performance.mjs` 的真实导出权限、字段裁剪、经理范围和员工拒绝校验。
- Reused modules or patterns：
  - 复用 `assessment/goal` 的导出接口形态与 `feedback/pip` 现有数据范围裁剪逻辑。
- New dependencies and why existing tools were insufficient：
  - 无。
- Verification run after each meaningful change：
  - 服务与控制器补丁完成后，执行目标测试：
    - `npm run test -- test/performance/feedback.service.test.ts test/performance/pip.service.test.ts`
  - 目标测试通过后，执行最小后端编译验证：
    - `npm run build`
  - 运行态权限与联调闭环补丁完成后，执行：
    - `node ./scripts/seed-stage2-performance.mjs`
    - `env STAGE2_SMOKE_BASE_URL=http://127.0.0.1:8006 node ./scripts/smoke-stage2-performance.mjs`
    - `corepack pnpm run type-check`
    - `corepack pnpm run build`
- Normal-path evidence：
  - `feedback/export` 成功返回 `taskId / assessmentId / employeeId / title / deadline / averageScore / submittedCount / totalCount`，测试已断言不包含禁导出的 `records[]` 明细字段。
  - `pip/export` 成功返回 `id / assessmentId / employeeId / employeeName / ownerId / ownerName / title / startDate / endDate / status / createTime / updateTime`，测试已断言不包含 `improvementGoal / sourceReason / resultSummary / trackRecords`。
  - fresh `8006` 实例上，`hr_admin` 与 `manager_rd` 的 `feedback/export`、`pip/export` 真实请求通过，`employee_platform` 被拒绝。
- Error-path evidence：
  - `feedback/export` 缺少独立导出权限时返回 `无权限导出该数据`，并写失败审计。
  - `pip/export` 结果超过 `5000` 行时返回 `导出结果超过上限，请缩小筛选范围后重试`，并写失败审计。
  - fresh `8006` 实例上，`employee_platform` 的 `feedback/export`、`pip/export` 返回 `无权限导出该数据`。
- Boundary-case evidence：
  - `feedback/export` 成功测试已验证只导出摘要字段。
  - `pip/export` 超限测试已验证冻结上限 `5000` 生效。
  - fresh `8006` 实例上，`manager_rd` 的 `pip/export` 仅返回平台组 3 条摘要，不包含 `联调-PIP-隐藏-销售员工`；`feedback/export` / `pip/export` smoke 断言均验证禁止字段不出现。
- Permission checks：
  - 服务侧新增 `performance:feedback:export` / `performance:pip:export` 独立权限校验，不继承 `page/info`。
  - 非 `HR` 导出范围仅限所辖部门树；不再沿用员工本人或评价人可见的明细查看兜底逻辑。
- State-flow checks：
  - 不涉及状态机新增。
- Contract checks：
  - 路径、权限键、统一错误语义、导出上限与审计字段均按 `04 / 06 / 12` 冻结口径落地。
- Change-type verification mapping：
  - backend service/controller：导出权限、字段裁剪、错误语义、审计留痕测试。
- High-risk trigger changes：
  - 新增导出权限键与脱敏导出行为。
- Compatibility or fallback impact：
  - 不改现有 assessment/goal 导出契约。
- Privacy/PII review result：
  - 已确认 `feedback/export` 不导出任何 `records[]` 单条反馈字段，`pip/export` 不导出改进目标、来源原因、结果总结和跟进全文。
- Docs or operational updates made：
  - 回写本任务卡、验证卡、最终验收卡和版本记录。

## Blockers And Waivers

- Open `P0` blockers：无
- Open `P1` blockers：无
- Open `P2` issues：
  - 当前 shell 环境中的旧 `8001` 监听实例无法通过沙箱内 `SIGTERM` 直接替换；本次真实联调改用 fresh `8006` 实例完成。该事项属于本地运行时治理，不再阻塞主题 2 关闭。
- Approved waivers：无

## Before Commit

- Diff review result：
  - 本轮仅修改 `base/menu.json`、`seed-stage2-performance.mjs`、`smoke-stage2-performance.mjs` 与主题 2 相关任务卡、版本记录；未扩写薪资、晋升、通用导出平台或共享基础层业务逻辑。
- Verification summary：
  - 后端目标测试通过，后端最小构建通过；前端 `type-check/build` 通过；fresh `8006` 实例 seed + smoke 通过，运行态权限、字段边界、经理范围和员工拒绝已有真实联调证据。
- What was run：
  - `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && npm run test -- test/performance/feedback.service.test.ts test/performance/pip.service.test.ts`
  - `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && npm run build`
  - `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && node ./scripts/seed-stage2-performance.mjs`
  - `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && env STAGE2_SMOKE_BASE_URL=http://127.0.0.1:8006 node ./scripts/smoke-stage2-performance.mjs`
  - `cd /Users/shaotongli/Documents/xuedao/cool-admin-vue && corepack pnpm run type-check`
  - `cd /Users/shaotongli/Documents/xuedao/cool-admin-vue && corepack pnpm run build`
- What passed：
  - `feedback.service.test.ts`
  - `pip.service.test.ts`
  - `build`
- What was not run：
  - `8001` 旧监听实例下的 fresh smoke 未重放；原因是当前环境内旧进程无法被沙箱直接终止，已改用隔离 fresh `8006` 实例完成等效真实联调。
- Remaining risk：
  - 当前运行环境仍存在陈旧 `8001` 监听实例，但仓库代码、菜单权限、种子数据和 fresh 联调实例已闭环；该环境问题不再阻塞主题 2。
- Tests added or updated, or why not：
  - 已新增并更新 `feedback.service.test.ts`、`pip.service.test.ts`。
- Commit scope statement：
  - 仅处理 `feedback/export`、`pip/export` 及最小测试与任务卡。
- Commit summary：
  - 新增反馈汇总导出和 PIP 摘要导出后端实现，补齐独立导出权限、字段白名单、审计留痕和最小自动化验证。

## Before Release Or Handoff

- Acceptance-to-verification mapping：
  - 两条导出接口：控制器与服务已落地。
  - 权限拒绝：`feedback` 测试已覆盖。
  - 超限：`pip` 测试已覆盖。
  - 字段白名单：`feedback/pip` 成功测试已覆盖。
  - 审计留痕：`feedback/pip` 成功和失败测试均已覆盖。
  - 运行态权限：`menu.json + seed + smoke` 已补齐，fresh `8006` smoke 结果 `PASS 47 / FAIL 0 / SKIP 0 / Conclusion: PASSED`。
  - 经理数据范围：fresh `8006` smoke 已断言 `manager_rd` 的 `pip/export` 不返回销售员工隐藏样例。
  - 员工拒绝导出：fresh `8006` smoke 已断言 `employee_platform` 两个导出接口均返回 `无权限导出该数据`。
- Contract alignment result：
  - 与冻结包保持一致，未扩写晋升、薪资或通用导出平台。
- Config changes：
  - 无新增环境配置。
- Migration plan：
  - 无数据库结构变更。
- Rollout steps：
  - 已完成 `base/menu.json` 两条导出权限注册、角色 seed 下发和 fresh 实例真实联调；当前无需额外 rollout 步骤。
- Rollback path：
  - 如需回退，本次仅回退 `feedback/pip export` 相关 controller/service/test 改动，不涉及数据库或共享基础层。
- Feature flag, kill switch, or staged rollout plan：
  - 无，保持同步导出接口最小改动。
- Data recovery path：
  - 无业务数据写入变更，审计写入复用现有日志表。
- Monitoring and diagnostic entry points：
  - `base_sys_log`
- Final `P0` and `P1` blocker check：
  - `P0`：无
  - `P1`：无
- Approved waivers：无
- Final release risk statement：
  - 主题 2 当前范围内的代码、运行态权限、真实角色导出链路和最小自动化验证已闭环；唯一剩余事项是本地旧 `8001` 进程治理，不阻塞主题关闭。

## 当前结论

- 唯一结论：导出增强后端主实现已完成并通过运行态权限闭环，可进入最终验收关闭。

## 下一步

1. 保持 `feedback/export` 与 `pip/export` 的菜单权限、seed 和 smoke 口径同步维护，后续新增导出主题不得复用本卡结论。
2. 若后续需要把 current latest 实例重新统一回 `8001`，应先处理旧 `8001` 监听进程，再重放同一套 seed / smoke。
