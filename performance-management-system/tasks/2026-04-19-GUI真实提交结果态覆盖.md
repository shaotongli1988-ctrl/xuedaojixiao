<!-- 文件职责：记录关键写操作提交结果态主题在通用 GUI runner 下的真实执行结果、报告路径和剩余风险；不负责替代业务模块实现卡、API 测试或 smoke 证据；依赖 write-submit-result 场景、通用 GUI runner 与当前 9007 -> 8006 运行基线；维护重点是只回写真实执行通过的提交结果态留痕，不把未执行或需复杂输入的写操作宣称为已覆盖。 -->
# 任务卡：GUI 真实提交结果态覆盖

## 基本信息

- 任务名称：GUI 真实提交结果态覆盖
- 当前阶段：二期 / GUI 留痕补强
- 当前状态：done
- 涉及端：`performance-management-system`、`cool-admin-vue`
- 负责人：Codex
- 创建时间：2026-04-19
- 最近更新时间：2026-04-19

## 当前结论

- 当前唯一正式结论：`GUI 真实提交结果态覆盖已完成`

## 范围

1. 复用仓库通用 GUI runner 覆盖最小真实提交结果态
2. 首批仅覆盖 `发起晋升并保存` 与 `会议签到确认`
3. 同时保留 `经理受限` 与 `员工无入口` 证据

## 非目标

1. 不改业务代码
2. 不补需要复杂下拉、多选、日期范围输入的整套新建表单
3. 不替代既有 smoke、接口测试和联调证据

## 执行命令

1. `node /Users/shaotongli/Documents/xuedao/performance-management-system/test/gui/run-cdp-gui-check.mjs --config /Users/shaotongli/Documents/xuedao/performance-management-system/test/gui/scenarios/write-submit-result.json`

## 执行结果

1. 为通用 runner 增加 `inputByLabel` 最小能力，用于按表单标签写入文本型字段
2. 首次执行暴露两处场景假设偏差：`发起原因` 非当前必填项，经理详情页不应预设为固定受限文案
3. 调整场景断言后复跑通过，最终结果为 `PASS 3 / FAIL 0`
4. 当前有效 GUI 入口口径保持为 `http://127.0.0.1:9007 -> /dev -> http://127.0.0.1:8006`

## 产物

1. 场景配置：`/Users/shaotongli/Documents/xuedao/performance-management-system/test/gui/scenarios/write-submit-result.json`
2. `report.json`：`/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/artifacts/write-submit-result/report.json`
3. `report.md`：`/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/artifacts/write-submit-result/report.md`
4. 关键截图：
   - `/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/artifacts/write-submit-result/write-submit-hr-promotion-form.png`
   - `/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/artifacts/write-submit-result/write-submit-hr-promotion-saved.png`
   - `/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/artifacts/write-submit-result/write-submit-hr-meeting-checkin-result.png`
   - `/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/artifacts/write-submit-result/write-submit-manager-restricted.png`
   - `/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/artifacts/write-submit-result/write-submit-employee-promotion-404.png`

## GUI 结论

1. `hr_admin` 成功覆盖 `评估详情 -> 发起晋升 -> 填写岗位 -> 保存成功落表` 的真实提交结果态
2. `hr_admin` 成功覆盖 `会议签到 -> 确认 -> 返回列表` 的真实提交确认链路
3. `manager_rd` 成功覆盖 `待我审批 -> 评估单详情` 的可进入链路
4. `employee_platform` 成功覆盖 `晋升管理` 的 `404` 无入口拒绝态
5. 最终 GUI 结论：`PASS 3 / FAIL 0，关键写操作提交结果态首批留痕已完成`

## 剩余风险

1. 当前仅覆盖文本输入型表单与确认型提交，不覆盖多选评价人、日期范围、下拉选择等复杂输入表单的真实提交

## 未执行项

1. 未补 `发起环评任务` 的评价人选择与提交结果态
2. 未补 `PIP 新建保存` 的日期范围与负责人选择结果态
3. 未补提交后的跨角色审批、关闭、完成等后续状态机结果态

## 下一步

1. 若继续补 GUI，优先增强 runner 的 `select/date-range` 能力，再覆盖 `环评创建` 和 `PIP 新建`
