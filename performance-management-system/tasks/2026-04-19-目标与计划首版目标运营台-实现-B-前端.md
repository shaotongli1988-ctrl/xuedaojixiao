<!-- 文件职责：记录“目标与计划首版目标运营台”窗口B前端实现的真实改动、验证结果和收口结论；不负责后端 seed/smoke 与最终验收总判断；依赖 goals-plan 设计 spec、实现计划和窗口A/C事实源；维护重点是只能按真实代码与真实前端验证结果回写。 -->
# 目标与计划首版目标运营台任务卡：实现 B - 前端

## 基本信息

- 任务名称：目标与计划首版目标运营台 - 实现 B - 前端
- 当前阶段：阶段1 / 前端主实现
- 当前状态：done
- 任务等级：L4
- 涉及端：`cool-admin-vue`
- 负责人：Codex
- 创建时间：2026-04-19
- 最近更新时间：2026-04-19

## 当前结论

- 当前正式结论：`窗口B前端已完成；/performance/goals 已从旧目标地图页改造成“目标&计划总览”目标运营台，并完成基于 opsAccessProfile 的真实权限裁剪，前端 type-check / build 通过`

## 范围确认

1. 已完成：
   - 将 `/performance/goals` 改造成单页目标运营台。
   - 同页落地 `今日运营区 / 目标对比区 / 目标配置区 / 日报汇报区` 四个冻结工作区。
   - 按 `员工 / 主管 / HR(管理员)` 裁剪视图。
   - 显式展示 `公共目标贡献 / 个人补充目标贡献`。
   - 接入 `goal ops` 前端请求封装与 `opsAccessProfile` 权限画像。
2. 未在本窗口做：
   - seed / smoke / GUI 收口。
   - 外部群推送真实验证。
   - 新增一级/二级导航。

## 实际改动

1. 页面主实现：
   - [index.vue](/Users/shaotongli/Documents/xuedao/cool-admin-vue/src/modules/performance/views/goals/index.vue)
   - 改为目标运营台首页壳层，承接四个工作区、角色裁剪、员工填报、主管下发、部门配置与日报状态展示。
2. 前端接口封装：
   - [goal.ts](/Users/shaotongli/Documents/xuedao/cool-admin-vue/src/modules/performance/service/goal.ts)
   - 新增 `opsDepartmentConfig / opsAccessProfile / opsPlanPage / opsDailySubmit / opsOverview / opsReport*` 等请求方法。
3. 复用现有类型：
   - [types.ts](/Users/shaotongli/Documents/xuedao/cool-admin-vue/src/modules/performance/types.ts)
   - 扩展 `GoalOpsAccessProfile` 与空对象工厂，继续保持单一前端契约来源。
4. 权限显隐收口：
   - [index.vue](/Users/shaotongli/Documents/xuedao/cool-admin-vue/src/modules/performance/views/goals/index.vue)
   - 不再用薪酬权限推断 `HR` 身份，也不再把 `goal:add` 误判为部门运营台管理能力；公共目标、部门配置和日报动作改为以 `opsAccessProfile` 真值裁剪。

## 验收映射

1. 页面能体现主管、员工、HR/管理员三层视图差异：
   - 已实现。员工侧不展示部门规则编辑和部门计划池，主管/HR 可见部门配置与日报动作入口。
2. 页面包含 4 个冻结工作区：
   - 已实现。页面固定展示 `今日运营区 / 目标对比区 / 目标配置区 / 日报汇报区`。
3. 员工可看到今日目标并有明确填报入口：
   - 已实现。员工侧展示 `我的待回填目标` 与 `提交我的结果`。
4. 主管可看到下发、统计、日报与配置入口：
   - 已实现。主管侧展示 `下发公共目标 / 执行未填补零 / 生成日报 / 保存部门配置`。
5. 页面可展示双榜、趋势和贡献拆维度：
   - 已实现。完成率榜、绝对产出榜、近 7 天趋势、贡献拆维度明细均已落地。
6. 前端通过 `type-check` 与 `build`：
   - 已通过，见下方真实验证结果。

## 真实验证结果

1. `cd /Users/shaotongli/Documents/xuedao/cool-admin-vue && corepack pnpm run type-check`
   - 结果：通过。
2. `cd /Users/shaotongli/Documents/xuedao/cool-admin-vue && corepack pnpm run build`
   - 结果：通过。
   - 备注：构建过程中仍有仓库既有 `cool-eps` 提示 `API service is not running -> /admin/base/open/eps`、`/admin/dict/info/types` 以及 chunk size 警告，但未阻塞本窗口构建。

## 风险与结论

1. 当前窗口B范围内未发现未关闭 `P0 / P1`。
2. GUI 点测与 latest reset/smoke 收口由窗口C负责，不在本卡冒充已验。
3. 当前窗口B可正式关闭，并以窗口C与最终验收卡作为后续唯一放行依据。
