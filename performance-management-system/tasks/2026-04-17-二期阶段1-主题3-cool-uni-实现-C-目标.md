<!-- 文件职责：记录二期阶段 1 主题 3「cool-uni 目标模块实现」的范围、进度、验证结果和收口结论；不负责考核、环评、登录链路和冻结范围外页面；依赖主题 3 冻结卡、设计卡、06/12/18 文档和现有 goal 接口；维护重点是只能复用既有 goal 接口与状态机，不得新增移动端专用接口或放宽权限边界。 -->
# 二期阶段 1 任务卡：主题 3 - cool-uni 实现 C - 目标

## 基本信息

- 任务名称：二期阶段 1 - 主题 3 `cool-uni` 实现 C - 目标
- 当前阶段：二期 / 阶段 1 / 实现
- 当前状态：done
- 任务等级：L3
- 涉及端：`cool-uni`
- 负责人：Codex
- 创建时间：2026-04-17
- 最近更新时间：2026-04-17

## Before Starting

- Scope：
  - 落地目标列表、详情、编辑、进度更新
  - 复用现有 `goal/page`、`goal/info`、`goal/update`、`goal/progressUpdate`
  - 保持员工仅本人、经理仅部门树范围
  - 保持状态流转与 PC 一致
- Non-goals：
  - 不做考核和环评
  - 不新增后端接口
  - 不改目标状态机
  - 不扩到导出
  - 不改 docs 主文档
- Affected modules：
  - `/Users/shaotongli/Documents/xuedao/cool-uni`
  - 本任务卡
- Acceptance criteria：
  - 列表 / 详情 / 编辑 / 进度更新主链可走通
  - `completed` 后禁止非法更新
  - 权限范围正确
  - `type-check / build` 通过
- Source of truth：
  - [06-权限矩阵.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/06-权限矩阵.md)
  - [12-数据权限与脱敏规则.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/12-数据权限与脱敏规则.md)
  - [18-页面结构与原型映射.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/18-页面结构与原型映射.md)
  - [2026-04-17-二期主题3-cool-uni冻结.md](/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-17-二期主题3-cool-uni冻结.md)
  - [2026-04-17-二期阶段1-主题3-cool-uni-信息架构设计.md](/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-17-二期阶段1-主题3-cool-uni-信息架构设计.md)
  - [2026-04-17-二期阶段1-主题3-cool-uni-接口复用设计.md](/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-17-二期阶段1-主题3-cool-uni-接口复用设计.md)
- High-risk surfaces：
  - 数据范围
  - 非法状态更新
  - 登录态未切换完成时的联调兼容
- Required delivery evidence：
  - 目标模块实现代码
  - `pnpm exec tsc --noEmit`
  - `pnpm exec vite build`
  - 最小手测 / 联调说明

## 当前结论

- 当前状态说明：`目标模块代码与构建链已完成本窗口实现收口，可进入联调`

## 验证计划

1. `cd /Users/shaotongli/Documents/xuedao/cool-uni && pnpm exec tsc --noEmit`
2. `cd /Users/shaotongli/Documents/xuedao/cool-uni && UNI_INPUT_DIR=$PWD pnpm exec vite build`
3. 最小联调核对：
   - 目标列表可拉取
   - 目标详情可查看
   - 草稿 / 进行中目标可编辑
   - `completed` 目标禁止更新进度

## 实现记录

- 新增 `cool-uni` 目标模块本地类型与 service：
  - `types/performance-goal.ts`
  - `service/performance/goal.ts`
- 新增目标页面与组件：
  - `pages/performance/goal/list.vue`
  - `pages/performance/goal/detail.vue`
  - `pages/performance/goal/edit.vue`
  - `pages/performance/goal/progress.vue`
  - `pages/performance/goal/components/goal-status-tag.vue`
  - `pages/performance/goal/components/goal-state.vue`
- 在 `pages.json` 增加目标子路由：
  - `goal/list`
  - `goal/detail`
  - `goal/edit`
  - `goal/progress`
- 复用既有后端接口，不新增移动端专用接口：
  - `admin/performance/goal/page`
  - `admin/performance/goal/info`
  - `admin/performance/goal/update`
  - `admin/performance/goal/progressUpdate`
- 页面行为与冻结口径对齐：
  - 列表与详情页仅在具备对应按钮权限且状态合法时显示“编辑 / 更新进度”
  - 编辑页直达时增加 `performance:goal:update` 权限校验
  - 进度页直达时增加 `performance:goal:progressUpdate` 权限校验
  - `completed / cancelled` 目标即使直达页面，也会在前端进入拒绝态，不再展示可提交表单
- 为通过当前 `cool-uni` 构建链验证，补齐最小构建兼容：
  - `vite.config.ts` 中 `vue` 指向 `shim/vue.ts`
  - `shim/vue.ts` 补齐 uni-app 依赖的最小兼容导出
  - `types/vue-runtime-esm-bundler.d.ts` 补齐深路径类型声明
  - `package.json` / `pnpm-lock.yaml` 对齐到可通过当前 `type-check/build` 的依赖口径

## 验证记录

- 已执行：
  - `cd /Users/shaotongli/Documents/xuedao/cool-uni && corepack pnpm exec tsc --noEmit`
  - 结果：通过
- 已执行：
  - `cd /Users/shaotongli/Documents/xuedao/cool-uni && UNI_INPUT_DIR=$PWD corepack pnpm exec vite build`
  - 结果：通过
- 已执行真实接口联调：
  - 入口：`http://127.0.0.1:8001`
  - 账号：`manager_rd / employee_platform`
  - 登录链路：`captcha -> login -> permmenu`
  - 结果：
    - `manager_rd` 登录成功，且目标分页不包含 `联调-销售中心隐藏目标`
    - `employee_platform` 登录成功，且目标分页仅返回本人 3 条平台组目标
    - `manager_rd` 通过既有 `goal/add` 创建临时目标，`employee_platform` 可在 `goal/page / goal/info` 中看到该目标
    - `employee_platform` 对临时目标执行 `goal/update` 成功
    - `employee_platform` 对临时目标执行 `goal/progressUpdate` 成功，状态从 `draft` 进入 `in-progress`
    - 对既有 `completed` 目标执行 `goal/progressUpdate` 被正确拒绝，返回 `已完成目标不能继续更新进度`
    - 联调结束后，`manager_rd` 通过既有 `goal/delete` 成功清理临时目标，未保留脏数据
- 代码级核对：
  - 目标列表、详情、编辑、进度更新页面均已接入既有 goal 接口
  - `completed / cancelled` 目标在列表/详情不展示非法动作，直达编辑页/进度页也会被拒绝
  - 数据范围继续由既有 goal 接口后端控制，前端不放宽“员工仅本人、经理仅部门树范围”
- 已尝试 GUI 点测准备：
  - 已确认 `cool-uni` 的正确 H5 运行入口应为：
    - `cd /Users/shaotongli/Documents/xuedao/cool-uni && UNI_INPUT_DIR=$PWD corepack pnpm exec uni --host 127.0.0.1 --port 18112 -p h5`
  - 已确认直接使用 `corepack pnpm exec vite --host 127.0.0.1 --port 9900` 不是有效 GUI 点测入口
  - 真实表现：
    - `vite` 入口可返回 HTML，但页面运行时报 `uni is not defined`
    - 基于 `vite build` 产物的静态预览可加载资源，但页面未挂载，仍为白屏
  - 已尝试使用 Playwright 自带 Chromium、WebKit 与系统 Chrome 通道拉起浏览器
  - 结果：均在浏览器进程启动阶段失败，错误表现包括 `bootstrap_check_in ... Permission denied (1100)`、`MachPortRendezvousServer ... Permission denied (1100)` 与 `SIGABRT`
  - 已进一步核对终端直启 Chrome：
    - `open -a "/Applications/Google Chrome.app" http://127.0.0.1:9900`
    - `"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome"`
    - 结果：手工 Chrome 可拉起，不再是“浏览器完全无法启动”
  - 结论：
    - 当前阻塞已从“Chrome 无法启动”收敛为“自动化浏览器拉起不稳定 + 错误 GUI 入口会白屏”
    - 本窗口仍无法在仓库内自动补齐可用的页面截图留痕
- 未执行：
  - 本窗口未完成真实账号的移动端人工点测
  - 本窗口未完成移动端页面截图留痕

## 阻塞 / 风险

- 当前未识别必须新增后端接口
- 当前已无实现级阻塞
- 剩余风险：
  - 当前已完成真实接口联调，但尚未补移动端 GUI 点测与截图留痕
  - 当前 GUI 缺口不是目标模块业务阻塞，而是 H5 点测入口和自动化浏览器环境仍未完全稳定
  - 若需要把“接口联调通过”升级为“页面联调通过”，仍需在 `cool-uni` 壳内完成真实登录、页面打开与表单提交的人工点测

## 下一步

1. 使用员工与经理账号做目标模块真实联调
2. 使用 `corepack pnpm exec uni --host 127.0.0.1 --port 18112 -p h5` 作为唯一有效 H5 入口补列表 / 详情 / 编辑 / 进度更新最小人工点测证据
3. 若页面联调与接口联调存在差异，再回到本任务卡补充问题记录

## 最终结论

- `cool-uni 目标已完成，可进入联调`
