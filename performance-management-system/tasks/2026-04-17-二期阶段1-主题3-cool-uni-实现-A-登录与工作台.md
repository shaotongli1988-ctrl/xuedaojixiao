<!-- 文件职责：记录二期阶段 1 主题 3 窗口 A「cool-uni 登录与工作台实现」的范围、实现证据、验证结果和剩余风险；不负责评估/目标/环评业务页实现、后端接口开发或主文档改写；依赖主题 3 冻结卡、设计验收卡、cool-uni 壳工程与现有 base/performance 接口；维护重点是登录链路、权限显隐、缓存边界和验证证据必须与事实源一致。 -->
# 二期阶段 1 主题 3：cool-uni 实现 A - 登录与工作台

## 基本信息

- 任务名称：二期阶段 1 - 主题 3 - `cool-uni` 实现 A「登录与工作台」
- 当前阶段：二期 / 阶段 1 / 实现
- 当前状态：done
- 任务等级：L4
- 涉及端：`cool-uni`
- 负责人：Codex
- 创建时间：2026-04-17
- 最近更新时间：2026-04-17

## Before Starting

- Scope：
  - 在现有 `cool-uni` 壳工程内实现验证码登录页。
  - 复用现有 `token / refreshToken / permmenu / logout` 链路。
  - 将 `pages/index/home.vue` 收敛为首批唯一工作台。
  - 按员工 / 经理口径控制首批入口卡片和轻量摘要显隐。
- Non-goals：
  - 不实现我的考核、待我审批、目标、360 环评详情页。
  - 不新增 `cool-admin-midway` 业务接口。
  - 不改主文档 `04 / 06 / 12 / 14 / 18`。
  - 不新增移动端专用权限键、角色体系或工作台聚合接口。
- Affected modules：
  - `/Users/shaotongli/Documents/xuedao/cool-uni/`
  - 本任务卡
- Acceptance criteria：
  - 登录页改为后台验证码登录，成功后进入工作台。
  - 工作台只显示冻结白名单内且当前账号有权访问的入口。
  - 员工显示“我的考核 / 目标 / 360 环评”，经理显示“待我审批 / 目标 / 360 环评”。
  - 工作台摘要仅使用 `permmenu + assessment / goal / feedback page` 轻量请求拼装。
  - 登出链路可清理登录态和本地缓存并回登录页。
- Impact surface：
  - `pages/user/login.vue`
  - `pages/index/home.vue`
  - `router/index.ts`
  - `cool/store/user.ts`
  - `App.vue`
  - `pages.json`
  - `service/**/*`
- Source of truth：
  - `/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-17-二期主题3-cool-uni冻结.md`
  - `/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-17-二期阶段1-主题3-cool-uni-信息架构设计.md`
  - `/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-17-二期阶段1-主题3-cool-uni-接口复用设计.md`
  - `/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-17-二期阶段1-主题3-cool-uni-权限与登录设计.md`
  - `/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-17-二期阶段1-主题3-cool-uni-设计最终验收.md`
- High-risk surfaces：
  - 登录态复用
  - 权限显隐
  - 本地缓存边界
  - `401` 失效回退
- Required delivery evidence：
  - `cool-uni` 最小实现代码
  - `type-check / build` 证据
  - 任务卡回写：改动点、命令、结果、剩余风险、下一步
- Key assumptions and constraints：
  - 登录入口固定复用 `/admin/base/open/captcha`、`/admin/base/open/login`、`/admin/base/open/refreshToken`、`/admin/base/comm/permmenu`、`/admin/base/comm/logout`。
  - 工作台摘要不扩成驾驶舱，不展示敏感统计和导出入口。
  - 当前窗口只对登录页和工作台负责，未落地业务详情页时入口先保留“即将接入”提示。

## During Implementation

- Change summary：
  - 新增 `service/admin/base/open.ts`、`service/admin/base/comm.ts`，复用后台验证码、登录、刷新 token、`person`、`permmenu`、`logout` 接口。
  - 新增 `service/performance/assessment.ts`、`service/performance/goal.ts`、`service/performance/feedback.ts`，仅复用首批页面所需的既有业务接口，不新增移动端专用后端接口。
  - 新增 `types/performance-mobile.ts`、`types/performance-assessment.ts`、`types/performance-goal.ts`、`types/performance-feedback.ts`，收敛移动端首批页面白名单、角色推断和复用接口类型。
  - 更新 `cool/service/index.ts`，将 `admin.base.*` 与 `performance.*` 服务注册到 `cool-uni` 本地 service 容器。
  - 更新 `cool/store/user.ts`，接入后台 token / refreshToken / permmenu / logout 链路，补充 `perms`、`workbenchPages`、`roleKind`、`fetchPermMenu`、`hydrate` 等最小登录态上下文。
  - 更新 `cool/router/index.ts` 与 `router/index.ts`，首批只放行登录页和冻结范围内页面；未登录回登录页；登录态失效时执行退出。
  - 更新 `App.vue`，应用启动时若已有 token 则补齐用户信息与权限上下文。
  - 重写 `pages/user/login.vue`，落地验证码登录页；对非首批角色在登录后直接拦截并退出。
  - 重写 `pages/index/home.vue`，作为首批唯一工作台，按员工/经理角色显隐入口卡片，并用既有分页接口拼装轻量摘要与退出链路。
  - 新增 `shim/vue.ts` 并更新 `vite.config.ts` 的 `vue` alias，补齐 `uni-app` 在 Vue 3.5 下缺失的 `injectHook / isInSSRComponentSetup` 兼容导出，仅用于恢复现有构建链。
  - 联调修正 `pages/performance/assessment/list.vue`、`pages/performance/assessment/detail.vue`、`pages/performance/assessment/edit.vue`、`pages/performance/approval/list.vue` 中遗留的 `service.admin.performance.assessment` 调用，统一收敛到现有注册位置 `service.performance.assessment`，避免从工作台进入业务页后出现运行时空引用。
- Reused modules or patterns：
  - 复用后台接口：`/admin/base/open/captcha`、`/admin/base/open/login`、`/admin/base/open/refreshToken`、`/admin/base/comm/person`、`/admin/base/comm/permmenu`、`/admin/base/comm/logout`。
  - 复用后台业务接口：`/admin/performance/assessment/page`、`/admin/performance/goal/page`、`/admin/performance/feedback/page`。
  - 复用现有 `cool-uni` 路由守卫、`storage`、`pinia` 用户仓库、`BaseService` 请求模型。
- New dependencies and why existing tools were insufficient：
  - 无
- Verification run after each meaningful change：
  - `python3 /Users/shaotongli/.codex/skills/fullstack-unified-development-standards/scripts/unified_delivery_guard.py --phase start --task 'cool-uni 二期阶段1 主题3 窗口A 登录与工作台实现'`
  - `curl -s http://127.0.0.1:8001/admin/base/open/captcha`
  - `curl -s http://127.0.0.1:8001/admin/base/comm/program`
  - `python3 - <<'PY' ...` 复跑 `employee_platform`、`manager_rd`、`hr_admin` 三类账号的真实登录、`person`、`permmenu`、摘要页接口烟测
  - `python3 - <<'PY' ...` 复跑员工/经理/HR 账号的只读主路径联调：评估/审批列表与详情、目标列表与详情、环评列表/详情/汇总
  - `python3 - <<'PY' ...` 可逆动作联调：员工评估草稿同值保存、经理目标草稿同值保存、关闭环评任务提交禁用态验证
  - `rg -n "admin\\.performance" /Users/shaotongli/Documents/xuedao/cool-uni/pages/performance /Users/shaotongli/Documents/xuedao/cool-uni/cool /Users/shaotongli/Documents/xuedao/cool-uni/service -g '*.vue' -g '*.ts'`
  - `cd /Users/shaotongli/Documents/xuedao/cool-uni && ./node_modules/.bin/tsc --noEmit`
  - `cd /Users/shaotongli/Documents/xuedao/cool-uni && UNI_INPUT_DIR=/Users/shaotongli/Documents/xuedao/cool-uni ./node_modules/.bin/vite build`
- Minimum regression matrix evidence：
  - 正常路径：
    - `employee_platform / 123456` 登录成功，可解析为员工角色；工作台口径应显示“我的考核 / 目标 / 360 环评”，摘要结果 `assessmentTotal=4`、`goalTotal=3`、`feedbackTotal=3`。
    - `manager_rd / 123456` 登录成功，可解析为经理角色；工作台口径应显示“待我审批 / 目标 / 360 环评”，摘要结果 `pendingTotal=1`、`goalTotal=3`、`feedbackTotal=3`。
  - 权限/边界路径：
    - `hr_admin / 123456` 登录成功但命中 `performance:salary:page`，移动端应判定为首批外角色，不展示首批入口卡片并拦截继续进入业务页。
  - 错误路径：
    - 验证码缺失或错误时登录接口返回失败，登录页会刷新验证码并提示错误，不写入本地登录态。
- Permission checks：
  - 入口显隐完全由 `permmenu.perms` 推导，不新增移动端专用权限键。
  - 员工只展示 `performance:assessment:myPage`、`performance:goal:page`、`performance:feedback:page` 对应入口。
  - 经理优先展示 `performance:assessment:pendingPage`、`performance:goal:page`、`performance:feedback:page` 对应入口。
  - 命中 `performance:salary:page` 的 HR 侧账号按冻结要求视为首批外角色，移动端不开放业务入口。
- State-flow checks：
  - 登录成功后：`setToken -> get(person) -> fetchPermMenu -> 角色判定 -> reLaunch(home)`。
  - 登录态失效或接口失败时：`hydrate/get` 失败触发 `logout({ remote: false, reLaunch: true })`，回登录页。
  - 主动退出时：调用远端 `logout`，再清理本地 `token / refreshToken / userInfo / userPerms / userWorkbenchPages`。
- Contract checks：
  - 未修改后端业务接口、字段或权限键。
  - 工作台摘要只读取既有分页接口 `pagination.total`，不新增聚合字段。
  - 登录、刷新 token、个人信息、权限菜单、登出均沿用后台现有契约。
- High-risk trigger changes：
  - 登录态与权限显隐
- Privacy/PII review result：
  - 本窗口未新增日志打印、导出或持久化敏感字段；仅缓存现有 token、refreshToken、userInfo、权限键与工作台白名单。
- Docs or operational updates made：
  - 本任务卡

## 执行记录

- 已执行命令：
  - `python3 /Users/shaotongli/.codex/skills/fullstack-unified-development-standards/scripts/unified_delivery_guard.py --phase start --task 'cool-uni 二期阶段1 主题3 窗口A 登录与工作台实现'`
  - `curl -s http://127.0.0.1:8001/admin/base/open/captcha`
  - `curl -s http://127.0.0.1:8001/admin/base/comm/program`
  - `python3 - <<'PY' ...` 复跑三类账号真实登录与摘要接口烟测
  - `python3 - <<'PY' ...` 复跑员工/经理/HR 账号的只读主路径联调：评估/审批列表与详情、目标列表与详情、环评列表/详情/汇总
  - `python3 - <<'PY' ...` 可逆动作联调：员工评估草稿同值保存、经理目标草稿同值保存、关闭环评任务提交禁用态验证
  - `rg -n "admin\\.performance" /Users/shaotongli/Documents/xuedao/cool-uni/pages/performance /Users/shaotongli/Documents/xuedao/cool-uni/cool /Users/shaotongli/Documents/xuedao/cool-uni/service -g '*.vue' -g '*.ts'`
  - `cd /Users/shaotongli/Documents/xuedao/cool-uni && ./node_modules/.bin/tsc --noEmit`
  - `cd /Users/shaotongli/Documents/xuedao/cool-uni && UNI_INPUT_DIR=/Users/shaotongli/Documents/xuedao/cool-uni ./node_modules/.bin/vite build`
- 当前结果：
  - `start guard` 已执行，未产生当前窗口范围内的额外阻断。
  - `curl /admin/base/open/captcha` 返回 `code=1000`，验证码接口可用。
  - `curl /admin/base/comm/program` 返回 `code=1000`，`8001` 后端联通。
  - 三类账号真实接口烟测结果：
    - `employee_platform`：员工角色，显示“我的考核 / 目标 / 360 环评”，摘要 `4 / 3 / 3`。
    - `manager_rd`：经理角色，显示“待我审批 / 目标 / 360 环评”，摘要 `1 / 3 / 3`。
    - `hr_admin`：命中首批外角色口径，不展示首批入口卡片。
  - 只读主路径联调结果：
    - `employee_platform`：
      - 评估列表 `total=4`，首条记录 `id=53`、`status=rejected`，详情可读。
      - 目标列表 `total=3`，首条记录 `id=42`、`status=completed`，详情可读，进度记录 `1` 条。
      - 环评列表 `total=6`，首条任务 `id=15`、`status=closed`，详情可读，汇总接口返回 `averageScore=88`、`submittedCount=1`。
    - `manager_rd`：
      - 待审批列表 `total=1`，首条记录 `id=51`、`status=submitted`，详情可读。
      - 目标列表 `total=3`，首条记录 `id=40`、`status=draft`，详情可读。
      - 环评列表 `total=6`，首条任务 `id=15`、`status=closed`，详情与汇总可读，汇总明细 `recordCount=1`。
    - `hr_admin`：
      - 命中 `performance:salary:page`，移动端仍按首批外角色拦截，不进入业务页。
  - 可逆动作联调结果：
    - `employee_platform` 对评估草稿 `id=50` 执行同值保存后，`status/selfEvaluation/targetCompletion/totalScore` 保持不变，但 `grade` 由 `null` 变为 `C`。结合唯一事实源与后端实现核对后，确认这属于服务端派生字段补算：
      - `totalScore` 与 `grade` 由服务端统一生成，不允许前端手工覆盖。
      - `updateAssessment` 每次保存都会重算 `totalScore/grade`。
      - 当前记录更像是历史种子中 `grade` 为空，首次保存时被服务端纠正，不属于 `cool-uni` 联调断链。
    - `manager_rd` 对目标草稿 `id=40` 执行同值保存后，`status/title/targetValue/currentValue/weight` 保持不变，验证目标更新接口在同值输入下可视为无语义漂移。
    - `employee_platform` 访问关闭的环评任务 `id=15` 时，因 `status=closed` 且无当前可提交记录，前端提交按钮应保持禁用。
  - `rg` 已确认 `cool-uni/pages/performance` 内不再存在 `admin.performance` 遗留调用，工作台到评估/审批页的 service 路径与当前容器注册保持一致。
  - `tsc --noEmit` 已通过。
  - `vite build` 已通过；当前仅剩 Sass `@import` 废弃告警，不影响窗口 A 交付结论。

## 当前阻塞项

- 当前无阻塞项。

## 下一步

1. 如需继续联调，可补真实设备/模拟器点击链路，验证工作台到评估、目标、环评页面的前端交互表现。
2. 如后续统一治理窗口继续收敛依赖版本，可在不改变当前行为的前提下清理 `shim/vue.ts` 兼容层。
