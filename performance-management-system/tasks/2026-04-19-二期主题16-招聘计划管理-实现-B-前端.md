<!-- 文件职责：记录二期主题16“招聘计划管理”窗口B前端实现的执行边界、验收口径和验证计划；不负责后端接口实现、运行态菜单/seed/smoke 或共享 docs 主文档改写；依赖主题16阶段0冻结结论与 04/06/12/18/24 文档；维护重点是 recruitPlan 页面入口、动作显隐、字段展示和状态门禁必须严格按冻结口径落地。 -->
# 二期主题16任务卡：招聘计划管理 - 实现 B - 前端

## 基本信息

- 任务名称：二期主题16 - 招聘计划管理 - 实现 B - 前端
- 当前阶段：二期 / 阶段1 / 前端主实现
- 当前状态：done
- 任务等级：L4
- 涉及端：`cool-admin-vue`
- 负责人：Codex
- 创建时间：2026-04-19
- 最近更新时间：2026-04-19

## 当前结论

- 当前正式结论：`主题16窗口B前端备案制扩展版已完成：recruitPlan 页面已补齐 delete / import / export / void / reopen 入口、voided 状态展示与权限显隐；同时已补招聘中心导航页以及到 resumePool / interview / hiring 的最小弱引用预填与来源摘要展示，并已真实通过 type-check 与 build`

## 需求摘要

1. 落地 `recruitPlan` 前端页面和动作入口，覆盖：
   - 列表
   - 详情
   - 新建
   - 编辑
   - 删除
   - 导入
   - 导出
   - 提交
   - 关闭
   - 作废
   - 重新开启
2. 严格落实主题16已冻结的权限显隐、字段展示和状态门禁。
3. 补前端最小门禁验证，为窗口C联调提供可点页面。
4. 在不改下游主链业务规则的前提下，补齐招聘中心到 `resumePool / interview / hiring` 的最小跨模块引用留痕。

## 实现范围

1. 新增 `recruitPlan` 前端类型、service、页面与弹窗交互。
2. 复用现有请求、分页和详情弹窗模式，不新增新设计系统。
3. 前端按权限显示：
   - `HR`：完整入口与完整动作
   - 经理：本人部门树范围列表、详情、新建、编辑、删除、导入、提交、关闭、作废、重新开启
   - 员工：无入口
4. 按状态显示动作：
   - `draft`：可编辑、可删除、可提交
   - `active`：可关闭、可作废，不可编辑
   - `closed / voided`：仅可重新开启
5. 新增 `/performance/recruitment-center` 最小导航页，并为 `resumePool / interview / hiring` 页面补齐来自招聘中心链路的预填、来源摘要与跳转衔接。

## 非目标

1. 不改后端代码。
2. 不改共享 docs 主文档。
3. 不改运行态菜单、seed、smoke。
4. 不做审批流、审批意见或审批实例入口。
5. 不重写职位标准、简历池、面试、录用主链业务规则；仅补主题16所需的最小跳转预填与来源展示。
6. 不做审批流页面或招聘驾驶舱。

## 预期改动范围

- `/Users/shaotongli/Documents/xuedao/cool-admin-vue/src/modules/performance/types.ts`
- `/Users/shaotongli/Documents/xuedao/cool-admin-vue/src/modules/performance/service/`
- `/Users/shaotongli/Documents/xuedao/cool-admin-vue/src/modules/performance/views/`
- 本任务卡文件

## 唯一事实源

1. [04-API设计.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/04-API设计.md)
2. [06-权限矩阵.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/06-权限矩阵.md)
3. [12-数据权限与脱敏规则.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/12-数据权限与脱敏规则.md)
4. [18-页面结构与原型映射.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/18-页面结构与原型映射.md)
5. [24-自动化测试策略与脚本规划.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/24-自动化测试策略与脚本规划.md)
6. [2026-04-19-二期主题16-招聘计划管理-阶段0冻结.md](/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-19-二期主题16-招聘计划管理-阶段0冻结.md)
7. [2026-04-19-二期主题16-招聘计划管理-阶段0最终验收.md](/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-19-二期主题16-招聘计划管理-阶段0最终验收.md)

## 冻结约束

1. 页面资源名、接口前缀和权限键都固定为 `recruitPlan`。
2. 员工无页面入口。
3. 经理只可见本人部门树范围内的计划，不可见越权数据。
4. `draft` 才显示编辑 / 删除 / 提交动作。
5. `active` 才显示关闭 / 作废动作。
6. `closed / voided` 只显示重新开启动作。
7. 页面只展示冻结允许字段，不展示候选人名单、预算明细、Offer 信息、简历全文、面试评语全文和录用决策全文。

## 验收标准

1. 页面能完成列表、详情、新建、编辑、删除、导入、导出、提交、关闭、作废、重新开启的前端入口接入。
2. 权限显隐与冻结口径一致。
3. 状态按钮显隐与冻结口径一致。
4. 前端通过 `type-check` 与 `build`。

## 自动化验证计划

1. `cd /Users/shaotongli/Documents/xuedao/cool-admin-vue && corepack pnpm run type-check`
2. `cd /Users/shaotongli/Documents/xuedao/cool-admin-vue && corepack pnpm run build`

## 已完成内容

1. `types.ts` 已对齐 `RecruitPlanStatus = draft / active / voided / closed`。
2. `types.ts` 已补齐招聘计划导入行与导出摘要类型。
3. `service/recruit-plan.ts` 已补齐以下动作与权限键：
   - `delete`
   - `import`
   - `export`
   - `void`
   - `reopen`
4. `views/recruit-plan/index.vue` 已补齐：
   - toolbar `导入 / 导出`
   - 行内 `删除 / 作废 / 重新开启`
   - `voided` 状态展示
   - 按权限显隐
   - 按状态显隐
   - 对应确认文案
5. 导入链路已改为：
   - 前端本地解析 `xlsx / xls / csv`
   - 通过现有上传能力上传原文件
   - 调 `service.space.info.add` 落 `space_info` 记录并取得 `space_info.id`
   - 向后端发送 `{ fileId: space_info.id, rows }`
6. 导出链路已改为：
   - 调后端 `/export` 拉摘要数组
   - 前端复用既有 `exportJsonToExcel` 生成 `xlsx`
7. 已新增 `/performance/recruitment-center` 招聘中心导航页，统一承接：
   - `recruit-plan`
   - `job-standard`
   - `resumePool`
   - `interview`
   - `talentAsset`
   - `hiring`
8. 已在 `resumePool / interview / hiring` 页面补齐来自主题16链路的最小弱引用前端能力：
   - 识别路由预填参数
   - 回填来源 ID / snapshot
   - 在详情或表单中展示来源摘要
   - 维持“弱引用、不自动联动状态”的冻结边界
9. 页面继续只展示冻结允许字段，不扩出审批流、驾驶舱或下游主链自动联动。

## 待确认问题

1. 当前无新增冻结阻塞项。

## 下一步动作

1. 复用既有列表 / 详情 / 新建 / 编辑页，补齐新增动作和状态显隐。
2. 完成后再进入窗口C重新联调。

## 受影响文档

1. 当前拆分阶段不新增 docs 主文档改动，继续沿用阶段0冻结包。

## 受影响代码目录

1. `/Users/shaotongli/Documents/xuedao/cool-admin-vue/src/modules/performance/`

## 自动化验证计划与结果

1. 已执行：`cd /Users/shaotongli/Documents/xuedao/cool-admin-vue && corepack pnpm run type-check`
2. 结果：通过
3. 已执行：`cd /Users/shaotongli/Documents/xuedao/cool-admin-vue && corepack pnpm run build`
4. 结果：通过
5. 备注：`build` 仅出现既有打包告警：
   - `Browserslist` 数据过旧提醒
   - 大 chunk 提醒
   - `store/plugins/lib/json2.js` 的 `eval` 风险提醒
6. 当前结论：窗口B实现范围内代码已完成并通过前端自动化验证。

## 实际修改文件

1. `/Users/shaotongli/Documents/xuedao/cool-admin-vue/src/modules/base/store/menu.ts`
2. `/Users/shaotongli/Documents/xuedao/cool-admin-vue/src/modules/performance/types.ts`
3. `/Users/shaotongli/Documents/xuedao/cool-admin-vue/src/modules/performance/views/recruit-plan/index.vue`
4. `/Users/shaotongli/Documents/xuedao/cool-admin-vue/src/modules/performance/views/resumePool/index.vue`
5. `/Users/shaotongli/Documents/xuedao/cool-admin-vue/src/modules/performance/views/interview/index.vue`
6. `/Users/shaotongli/Documents/xuedao/cool-admin-vue/src/modules/performance/views/hiring/index.vue`
7. `/Users/shaotongli/Documents/xuedao/cool-admin-vue/src/modules/performance/views/recruitment-center/index.vue`
8. `/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-19-二期主题16-招聘计划管理-实现-B-前端.md`

## 剩余风险

1. `P0`：无。
2. `P1`：无。
3. `P2`：前端 `build` 仍存在仓库既有告警，包括 `Browserslist` 数据过旧提醒、大 chunk 提醒和 `store/plugins/lib/json2.js` 的 `eval` 提醒；不构成主题16阻塞。

## 回改范围

1. 仅本任务卡。

## Before Starting Record

- Scope:
  - 在限定四个文件内补齐主题16招聘计划前端备案制扩展版动作、状态和导入导出行为。
- Non-goals:
  - 不改后端、不改菜单 seed、不改共享 docs 主文档、不接审批流。
- Affected modules:
  - `cool-admin-vue/src/modules/performance/types.ts`
  - `cool-admin-vue/src/modules/performance/service/recruit-plan.ts`
  - `cool-admin-vue/src/modules/performance/views/recruit-plan/index.vue`
  - 本任务卡
- Acceptance criteria:
  - 页面具备 `page/info/add/update/delete/import/export/submit/close/void/reopen` 前端入口，权限和状态显隐符合冻结口径，且通过 `type-check` 与 `build`。
- Impact surface:
  - 共享类型、前端 service、招聘计划页面、任务留痕。
- Source of truth:
  - 主题16阶段0冻结卡、`04/06/09/12/18/24` 文档，以及本轮用户确认冻结事实：`resource=recruitPlan`、状态 `draft/active/voided/closed`、备案制、`HR` 全量、经理无 `export`。
- High-risk surfaces:
  - 权限键、状态流、导入导出契约、共享类型。
- Required delivery evidence:
  - 代码改动、任务卡留痕、`type-check`、`build`。
- Key assumptions and constraints:
  - 只能改限定四个文件；导入不加新依赖；导出复用现有 excel 工具。

## During Implementation Record

- Change summary:
  - 扩展了 recruit plan 的类型、service 和页面动作；新增本地解析导入和前端摘要导出。
- Reused modules or patterns:
  - `resumePool` 的导入入口组织方式
  - `assessment / goal / pip` 的导出调用与 `exportJsonToExcel`
  - 现有 `upload` hook 与 `xlsx` 依赖
- New dependencies and why existing tools were insufficient:
  - 无新增依赖。
- Verification run after each meaningful change:
  - 实现后执行 `corepack pnpm run type-check`
  - 收口后执行 `corepack pnpm run build`
- Normal-path evidence:
  - `draft` 可编辑/删除/提交，`active` 可关闭/作废，`closed / voided` 可重新开启的前端入口已落地。
- Error-path evidence:
  - 非法状态点击前端被按钮显隐和只读提示拦截；导入空文件/不支持格式有前端提示。
- Boundary-case evidence:
  - `voided` 状态已新增展示与只读说明；删除最后一行时会回退分页。
- Permission checks:
  - `HR` 可见导出；经理无导出按钮；其他动作按对应权限键显隐。
- State-flow checks:
  - 仅 `draft` 显示 `编辑 / 删除 / 提交`；仅 `active` 显示 `关闭 / 作废`；仅 `closed / voided` 显示 `重新开启`。
- Contract checks:
  - service 权限键与动作已对齐冻结口径；导入请求按 `{ fileId, rows }` 发送；导出按摘要数组处理。
- Change-type verification mapping:
  - 共享类型变更由 `vue-tsc` 覆盖；页面变更由 `build` 覆盖。
- High-risk trigger changes:
  - 状态枚举、权限显隐、导入导出行为。
- Compatibility or fallback impact:
  - 保留既有列表、详情、新建、编辑主链，不改相邻模块。
- Privacy/PII review result:
  - 仅导出冻结允许的招聘计划摘要字段，不新增敏感字段展示。
- Docs or operational updates made:
  - 仅更新本任务卡。

## Blockers And Waivers

- Open `P0` blockers:
  - 无
- Open `P1` blockers:
  - 无
- Open `P2` issues:
  - 导入 `fileId` 语义待窗口C与后端联调最终确认
- Approved waivers:
  - 无

## Before Commit

- Diff review result:
  - 变更限定在用户指定的四个文件内。
- Verification summary:
  - `type-check` 与 `build` 均真实执行通过。
- What was run:
  - `cd /Users/shaotongli/Documents/xuedao/cool-admin-vue && corepack pnpm run type-check`
  - `cd /Users/shaotongli/Documents/xuedao/cool-admin-vue && corepack pnpm run build`
- What passed:
  - 两条命令均退出码 `0`
- What was not run:
  - 无额外 GUI / 联调验证
- Remaining risk:
  - 导入 `fileId` 的运行态契约仍需窗口C实证
- Tests added or updated, or why not:
  - 本次为前端页面与类型改动，仓库未在限定文件内提供可增补的针对性自动化用例入口，故以 `vue-tsc + build` 作为最小自动化证据。
- Commit scope statement:
  - 仅覆盖主题16招聘计划前端备案制扩展版。
- Commit summary:
  - 补齐 recruitPlan 删除/导入/导出/作废/重开前端入口、状态显隐和任务留痕。

## Before Release Or Handoff

- Acceptance-to-verification mapping:
  - 动作与显隐实现已完成，对应由代码 diff 与 `type-check/build` 验证。
- Contract alignment result:
  - 前端类型、service 权限键和页面动作已对齐当前冻结事实；导入 `fileId` 运行态语义仍待窗口C联调确认。
- Config changes:
  - 无
- Migration plan:
  - 无
- Rollout steps:
  - 合并前继续等待窗口A/C联调收口。
- Rollback path:
  - 回退本轮四文件改动即可恢复上一轮最小主链。
- Feature flag, kill switch, or staged rollout plan:
  - 无；本轮仅页面能力补齐。
- Data recovery path:
  - 无新增持久化逻辑。
- Monitoring and diagnostic entry points:
  - 前端导入/导出/状态动作均有用户提示，可在窗口C联调中结合后端日志排查。
- Final `P0` and `P1` blocker check:
  - 无
- Approved waivers:
  - 无
- Final release risk statement:
  - 当前窗口B代码已完成并通过前端构建校验，剩余风险集中在窗口C对导入 `fileId` 语义的运行态联调确认。
