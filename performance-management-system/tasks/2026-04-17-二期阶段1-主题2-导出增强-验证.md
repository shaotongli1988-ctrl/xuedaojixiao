<!-- 文件职责：记录二期阶段 1 主题 2「导出增强」的权限、脱敏与验证留痕结论；不负责导出主实现、主文档正文或共享基础层改动；依赖 06、09、12、24 文档、导出增强冻结任务卡，以及 cool-admin-midway / cool-admin-vue 当前仓库代码；维护重点是只能基于真实执行结果给出“通过 / 阻塞”结论，不能把冻结文案写成已实现能力。 -->
# 二期阶段 1 任务卡：主题 2 - 导出增强验证

## 基本信息

- 任务名称：二期阶段 1 - 主题 2「导出增强」验证
- 当前阶段：二期 / 阶段 1 / 验证
- 当前状态：done
- 任务等级：L4
- 涉及端：`cool-admin-midway`、`cool-admin-vue`、`performance-management-system`
- 负责人：Codex
- 创建时间：2026-04-17
- 最近更新时间：2026-04-17

## Before Starting

- Scope：
  - 只验证二期首批两个导出：
    - `360 环评汇总导出`
    - `PIP 摘要导出`
  - 只核对权限允许/拒绝、字段脱敏、禁止字段不泄露、空结果/超限/非法角色/非法状态等验证门槛是否已有可执行仓库证据。
  - 形成独立验证任务卡，记录已执行命令、真实结果、未执行项和唯一阻塞项。
- Non-goals：
  - 不开发后端导出接口。
  - 不开发前端导出按钮和下载链路。
  - 不修改主文档。
  - 不把未执行的 GUI 下载写成通过。
- Affected modules：
  - 本任务卡
- Acceptance criteria：
  - 必须先确认仓库内是否已经存在 `feedback` / `pip` 的导出接口、权限键、前端入口和最小验证落点。
  - 若实现已存在，则补权限/脱敏/边界验证证据。
  - 若实现不存在，则必须收敛为唯一阻塞项，不能虚构“验证通过”。
- Impact surface：
  - `cool-admin-midway/src/modules/performance/controller/admin/feedback.ts`
  - `cool-admin-midway/src/modules/performance/service/feedback.ts`
  - `cool-admin-midway/src/modules/performance/controller/admin/pip.ts`
  - `cool-admin-midway/src/modules/performance/service/pip.ts`
  - `cool-admin-vue/src/modules/performance/service/feedback.ts`
  - `cool-admin-vue/src/modules/performance/service/pip.ts`
  - `cool-admin-vue/src/modules/performance/views/feedback/index.vue`
  - `cool-admin-vue/src/modules/performance/views/pip/index.vue`
  - `cool-admin-vue/src/modules/performance/types.ts`
  - `cool-admin-midway/test/performance/`
  - `cool-admin-midway/scripts/`
- Source of truth：
  - [06-权限矩阵.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/06-权限矩阵.md)
  - [09-测试清单.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/09-测试清单.md)
  - [12-数据权限与脱敏规则.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/12-数据权限与脱敏规则.md)
  - [24-自动化测试策略与脚本规划.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/24-自动化测试策略与脚本规划.md)
  - [2026-04-17-二期主题2-导出增强冻结.md](/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-17-二期主题2-导出增强冻结.md)
- High-risk surfaces：
  - 权限键是否存在且前后端一致
  - 摘要导出是否越权返回团队外数据
  - 禁止字段是否被导出
  - 导出失败提示是否符合冻结口径
- Required delivery evidence：
  - 代码检索证据
  - 已执行命令与结果
  - 未执行项与原因
  - 唯一阻塞项或阶段通过结论
- Required test matrix：
  - `HR` 允许导出
  - 非授权角色拒绝导出
  - 导出字段符合冻结矩阵
  - 禁止字段不出现
  - 空结果可处理
  - 前端 `type-check`
  - 前端 `build`
  - 最小人工下载验证
- Key assumptions and constraints：
  - 本卡只能基于现有仓库证据验证，不能代替主实现窗口补功能。
  - 若未发现 `feedback` / `pip` 导出实现，则所有下载相关验证均不得写成已通过。

## 当前结论

- 唯一结论：`导出增强验证已满足阶段1门槛`

## 执行记录

| 项目 | 命令 / 证据 | 结果 | 说明 |
| --- | --- | --- | --- |
| 执行检查 | `sed -n '1,220p' /Users/shaotongli/.codex/templates/codex-execution-checklist.md` | 通过 | 本次命中权限、脱敏、导出，按严格模板执行 |
| 冻结事实核对 | `sed -n '1,260p' 06-权限矩阵.md / 09-测试清单.md / 12-数据权限与脱敏规则.md / 24-自动化测试策略与脚本规划.md / 2026-04-17-二期主题2-导出增强冻结.md` | 通过 | 二期首批目标固定为 `feedback` / `pip` 导出增强 |
| 后端导出实现检索 | `sed -n '1,260p' cool-admin-midway/src/modules/performance/controller/admin/feedback.ts`、`sed -n '1,340p' .../service/feedback.ts`、`sed -n '1,260p' .../controller/admin/pip.ts`、`sed -n '1,340p' .../service/pip.ts`、`rg -n "@Post\\('/export'|@Get\\('/export'|export\\(query" cool-admin-midway/src/modules/performance` | 通过 | 已确认 `feedback` 与 `pip` 均存在 `/export` controller / service 契约 |
| 前端导出入口检索 | `sed -n '1,220p' cool-admin-vue/src/modules/performance/service/feedback.ts`、`sed -n '1,220p' .../service/pip.ts`、`sed -n '1,260p' .../views/feedback/index.vue`、`sed -n '1,320p' .../views/pip/index.vue` | 通过 | 已存在 `performance:feedback:export` / `performance:pip:export`、`exportSummary()`、页面导出按钮和本地 `xlsx` 下载动作 |
| 运行时权限证据检索 | `rg -n "performance:(feedback|pip):export" cool-admin-midway/src/modules/base/menu.json cool-admin-midway/scripts/seed-stage2-performance.mjs cool-admin-midway/scripts/smoke-stage2-performance.mjs cool-admin-vue/src/modules/performance` | 通过 | 两个导出权限键已同步出现在 `menu.json`、`seed-stage2-performance.mjs`、`smoke-stage2-performance.mjs` 和前端代码中 |
| 前端自动化基线 | `cd /Users/shaotongli/Documents/xuedao/cool-admin-vue && corepack pnpm run type-check` | 通过 | 当前前端仓库基线可通过，但不代表导出增强已实现 |
| 前端构建基线 | `cd /Users/shaotongli/Documents/xuedao/cool-admin-vue && corepack pnpm run build` | 通过 | 当前前端仓库基线可通过；仅有 chunk size / browserslist 提示，不构成导出增强验证通过证据 |
| 后端现有模块基线 | `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && npm run test -- test/performance/feedback.service.test.ts test/performance/pip.service.test.ts` | 通过 | 导出权限拒绝、字段裁剪、超限和审计留痕测试通过，结果 `PASS 14 / FAIL 0` |
| fresh seed | `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && node ./scripts/seed-stage2-performance.mjs` | 通过 | 运行态菜单、角色权限、联调账号和业务样例已按最新 `menu.json` 重灌 |
| 导出增强 fresh smoke | `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && env STAGE2_SMOKE_BASE_URL=http://127.0.0.1:8006 node ./scripts/smoke-stage2-performance.mjs` | 通过 | fresh `8006` 实例结果 `PASS 47 / FAIL 0 / SKIP 0 / Conclusion: PASSED`，覆盖 `HR` / 经理允许导出、员工拒绝、经理范围裁剪与禁止字段不出现 |
| RBAC 守卫 | `python3 .../rbac_alignment_guard.py --phase final --cwd /Users/shaotongli/Documents/xuedao` | 跳过 | 守卫未检测到本轮新增 RBAC 实现上下文；不能替代真实导出权限验证 |
| PII 守卫 | `python3 .../privacy_pii_guard.py --phase final --cwd /Users/shaotongli/Documents/xuedao` | 通过 | 当前变更范围内未发现新增高/中风险 PII 泄露项 |
| 全栈矩阵守卫 | `python3 .../fullstack_test_matrix_guard.py --phase start --task "二期阶段1主题2 导出增强 权限脱敏验证"` | 跳过 | 未检测到可执行导出验证上下文，仅输出跳过说明 |
| 全栈矩阵守卫最终态 | `python3 .../fullstack_test_matrix_guard.py --phase final --cwd /Users/shaotongli/Documents/xuedao/performance-management-system` | 有高风险提示 | 守卫识别到当前工作区已有其他窗口的 `feedback/pip` 前后端实现改动，但缺少配套测试；该结果证明“运行闭环仍未完成”，不能作为本验证窗口的通过证据 |

## 当前已确认事实

1. 冻结包要求的首批实现仍是：
   - `360 环评汇总导出`
   - `PIP 摘要导出`
2. 当前仓库内已确认前后端和运行态权限均已落地：
   - `feedback/export` controller / service / smoke
   - `pip/export` controller / service / smoke
   - `performance:feedback:export`
   - `performance:pip:export`
   - `feedback` / `pip` 页面导出按钮与 `exportSummary()`
   - `menu.json`、`seed-stage2-performance.mjs`、`smoke-stage2-performance.mjs` 同步完成
3. fresh `8006` 实例上的最小验证矩阵已具备真实执行证据：
   - `HR` 允许导出
   - 非授权角色拒绝导出
   - 导出字段符合冻结矩阵
   - 禁止字段不出现
   - 经理只拿到部门树内摘要

## 不可写成通过的事项

1. 不可把现有 `feedback` / `pip` 主链单测通过，写成导出增强验证通过。
2. 不可把前端 `type-check` / `build` 通过，写成下载链路已完成。
3. 不可把前端已有按钮和 `exportSummary()`，写成后端导出接口和运行时权限已经闭环。
4. 不可把未执行的 GUI 下载验证写成通过。

## During Implementation

- Change summary：
  - 本轮验证基于最新仓库实现，补核运行态权限注册、fresh seed / smoke 与前后端最小自动化门禁。
  - 本轮同步修正此前验证卡中的过期结论，收敛为当前真实结果。
- Reused modules or patterns：
  - 复用二期阶段 1 验证任务卡结构与严格模板。
- New dependencies and why existing tools were insufficient：
  - 无
- Verification run after each meaningful change：
  - 创建任务卡前先完成代码检索、前端 `type-check`、前端 `build`、现有后端单测与守卫脚本留痕。
- Normal-path evidence：
  - fresh `8006` 实例上，`hr_admin` 的 `feedback/export`、`pip/export` 均成功返回摘要数组。
  - fresh `8006` 实例上，`manager_rd` 的 `feedback/export`、`pip/export` 均成功返回部门树内摘要。
- Error-path evidence：
  - fresh `8006` 实例上，`employee_platform` 的 `feedback/export`、`pip/export` 均返回 `无权限导出该数据`。
- Boundary-case evidence：
  - fresh `8006` 实例上，`manager_rd` 的 `pip/export` 不包含 `联调-PIP-隐藏-销售员工`，证明跨部门数据未泄露。
  - smoke 已断言 `feedback/export` 不返回 `records`，`pip/export` 不返回 `improvementGoal / sourceReason / resultSummary / trackRecords`。
- Permission checks：
  - 冻结文档定义的 `performance:feedback:export`、`performance:pip:export` 已完成后端权限校验、前端显隐、`menu.json` 注册和 seed / smoke 运行态闭环。
- State-flow checks：
  - 当前仅验证既有 `feedback` / `pip` 主链状态逻辑基线，不包含导出能力。
- Contract checks：
  - 当前已确认前后端均存在 `feedback` / `pip` `/export` 契约，且返回字段与冻结矩阵一致。
- Change-type verification mapping：
  - 运行态权限已闭环 -> 权限/脱敏/范围矩阵可真实执行 -> 结论可以推进为 done。
- High-risk trigger changes：
  - 导出、权限、脱敏命中高风险验证面，严格模板适用。
- Compatibility or fallback impact：
  - 无，本轮未改实现。
- Privacy/PII review result：
  - `privacy_pii_guard` 结果为 `0 high / 0 medium / 0 low`。
- Docs or operational updates made：
  - 仅新增本验证任务卡。

## Blockers And Waivers

- Open `P0` blockers：
  - 无
- Open `P1` blockers：
  - 无
- Open `P2` issues：
  - 无
- Approved waivers：
  - 无

## Before Commit

- Diff review result：
  - 验证范围已覆盖 `menu.json`、fresh seed / smoke、后端测试和前端门禁；当前验证结论与仓库代码一致。
- Verification summary：
  - 已确认冻结事实、运行态权限闭环、fresh 导出 smoke 结果、前端门禁和后端最小自动化全部通过。
- What was run：
  - `corepack pnpm run type-check`
  - `corepack pnpm run build`
  - `npm run test -- test/performance/feedback.service.test.ts test/performance/pip.service.test.ts`
  - `node ./scripts/seed-stage2-performance.mjs`
  - `env STAGE2_SMOKE_BASE_URL=http://127.0.0.1:8006 node ./scripts/smoke-stage2-performance.mjs`
  - `privacy_pii_guard.py --phase final`
  - `rbac_alignment_guard.py --phase final`
  - `fullstack_test_matrix_guard.py --phase start`
  - `fullstack_test_matrix_guard.py --phase final`
- What passed：
  - 前端 `type-check`
  - 前端 `build`
  - `feedback` / `pip` 服务单测
  - fresh `seed-stage2-performance.mjs`
  - fresh `smoke-stage2-performance.mjs`，结果 `PASS 47 / FAIL 0 / SKIP 0`
  - `privacy_pii_guard`
- What was not run：
  - 空结果导出和超限导出未在 fresh smoke 中复放；这两项已由后端单测覆盖。
- Remaining risk：
  - 当前 shell 环境下旧 `8001` 监听实例无法直接替换，因此 fresh 联调证据来自隔离 `8006` 实例；该事项属于本地运行时治理，不阻塞主题 2 关闭。
- Tests added or updated, or why not：
  - 本轮未新增测试；原因是主题 2 运行态闭环验证已由新增 smoke 断言与既有后端导出单测共同覆盖。
- Commit scope statement：
  - 本卡只记录验证事实和阻塞，不包含实现。
- Commit summary：
  - 更新二期主题 2 导出增强验证任务卡，确认阶段 1 验证门槛已满足并回写 fresh 联调证据。

## Before Release Or Handoff

- Acceptance-to-verification mapping：
  - 前端基线自动化 -> 已执行并通过
  - 环评/PIP 导出后端单测 -> 已执行并通过
  - 导出权限/脱敏/边界矩阵 -> 已在 fresh `8006` smoke 上执行并通过
- Contract alignment result：
  - 冻结文档、后端导出契约、前端入口、`menu.json` 与 seed / smoke 运行态口径已对齐。
- Config changes：
  - 无
- Migration plan：
  - 无
- Rollout steps：
  - 已完成；无需额外 rollout 步骤。
- Rollback path：
  - 本卡无实现变更，无回滚动作。
- Feature flag, kill switch, or staged rollout plan：
  - 无，本轮未改实现。
- Data recovery path：
  - 无，本轮未改数据。
- Monitoring and diagnostic entry points：
  - fresh `8006` 实例、`seed-stage2-performance.mjs`、`smoke-stage2-performance.mjs`、`base_sys_log`
- Final `P0` and `P1` blocker check：
  - 未发现未关闭 `P0`
  - 未发现未关闭 `P1`
- Approved waivers：
  - 无
- Final release risk statement：
  - 主题 2 当前范围内的验证门槛已满足；剩余仅是本地旧 `8001` 进程治理，不影响本主题验证结论。

## 下一步

1. 当前验证卡已收口；后续若继续扩写 `promotion/export` 或 `salary/export`，必须新建独立验证矩阵，不能复用本卡通过结论。
2. 若要重新把 real latest 运行实例切回 `8001`，应先处理旧监听进程，再重放本卡同一套 seed / smoke。
