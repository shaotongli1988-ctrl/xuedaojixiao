<!-- 文件职责：记录二期主题7窗口7-A“培训课程管理后端主实现”的范围、实现、验证和收口结论；不负责前端页面、menu、共享基础层、培训全域扩展或 seed 脚本改写；依赖主题7阶段0冻结结论、04/05/06/09/12/24 文档与 cool-admin-midway 现有 performance 模块实现；维护重点是只交付 course 主链与 enrollmentPage 摘要查询，不口头扩 scope。 -->
# 二期主题7任务卡：培训课程管理实现 A - 后端

## 基本信息

- 任务名称：二期主题7 - 培训课程管理 - 实现 A - 后端
- 当前阶段：二期 / 阶段1 / 实现
- 当前状态：done
- 任务等级：L4
- 涉及端：`cool-admin-midway`、`performance-management-system`
- 负责人：Codex
- 创建时间：2026-04-18
- 最近更新时间：2026-04-18

## Before Starting

- Scope：
  - 在 `cool-admin-midway/src/modules/performance/` 内实现 `course` 后端主链：
    - `page`
    - `info`
    - `add`
    - `update`
    - `delete`
    - `enrollmentPage`
  - 在 `cool-admin-midway/test/performance/` 补主题7最小 `jest`
  - 回写本任务卡
- Non-goals：
  - 不改前端
  - 不改 `menu.json`
  - 不改共享基础层、登录、权限缓存或 request 基座
  - 不扩到 `AI 背诵 / AI 练习 / 能力地图 / 证书管理 / 合作伙伴 / 员工报名动作`
  - 不改 seed / smoke / 迁移脚本
- Affected modules：
  - `/Users/shaotongli/Documents/xuedao/cool-admin-midway/src/modules/performance/`
  - `/Users/shaotongli/Documents/xuedao/cool-admin-midway/test/performance/`
  - 本任务卡
- Acceptance criteria：
  - `HR` 可调用 `course page/info/add/update/delete/enrollmentPage`
  - 部门经理仅可调用 `course page/info`
  - 员工无课程管理入口且接口被拒绝
  - `code` 允许为空；如填写则唯一
  - `published` 仅允许修改 `description / endDate / status(close)`
  - `enrollmentPage` 只返回报名摘要，不返回培训扩展数据
  - 主题7相关后端 `jest` 通过
  - `cool-admin-midway npm run build` 通过，或给出等效失败原因
- Impact surface：
  - `course` 控制器 / 服务 / 实体 / helper
  - 主题7后端 `jest`
  - 任务卡记录
- Source of truth：
  - [16-需求澄清清单.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/16-需求澄清清单.md)
  - [04-API设计.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/04-API设计.md)
  - [05-状态机与流转规则.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/05-状态机与流转规则.md)
  - [06-权限矩阵.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/06-权限矩阵.md)
  - [09-测试清单.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/09-测试清单.md)
  - [12-数据权限与脱敏规则.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/12-数据权限与脱敏规则.md)
  - [24-自动化测试策略与脚本规划.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/24-自动化测试策略与脚本规划.md)
  - [2026-04-18-二期主题7-培训课程管理-阶段0最终验收.md](/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-18-二期主题7-培训课程管理-阶段0最终验收.md)
- Requirement conflicts or unclear points that must be resolved before coding：
  - 当前主题7冻结材料已覆盖接口 / 状态 / 权限 / 测试边界，可支撑后端代码实现
  - `03-数据库设计.md` 尚无主题7表结构正文；本窗口按已冻结 API 最小字段建模，但不把该隐含表结构视为文档已补齐
- High-risk surfaces：
  - 新增资源接口契约
  - `published / closed` 状态限制
  - `code` 可空唯一约束
  - 经理只读与员工拒绝
  - 报名列表字段裁剪
- Required delivery evidence：
  - 修改文件清单
  - 实际执行命令
  - 主题7相关后端测试结果
  - backend build 结果
  - 剩余风险
  - 是否满足进入下一阶段条件
- Required test matrix：
  - 正常：`HR` 新增、详情、编辑、报名摘要查询
  - 异常：非 `HR` 的新增 / 编辑 / 删除 / 报名列表拒绝
  - 边界：`draft` 可删；`published / closed` 不可删；`published` 只允许改 `description / endDate`
  - 状态：`draft -> published -> closed` 合法；`closed` 后不可编辑
- Key assumptions and constraints：
  - 当前窗口只交付允许修改范围内的代码与测试
  - 当前本地库尚未存在 `performance_course` / `performance_course_enrollment` 实表，且本窗口不允许改 seed / 建表脚本；因此“真实联调可跑”依赖其他窗口补齐运行时表结构

## 当前结论

- 当前状态说明：`主题7后端主实现已完成；原先运行时课程表缺失的阻塞已由联调窗口补齐 seed / 表结构 / 菜单与真实联调证据关闭`

## 验证计划

1. `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && npm run test -- test/performance/course.service.test.ts`
2. `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && npm run build`
3. 代码级核对：
   - `published` 字段修改限制
   - `draft / published / closed` 删除与编辑限制
   - 经理只读、员工拒绝
   - 报名列表仅返回摘要字段

## During Implementation

- Change summary：
  - 新增 `course` 主表实体、`course-enrollment` 报名摘要实体、`course-helper` 纯逻辑辅助、`course` 服务与控制器
  - 实现 `page / info / add / update / delete / enrollmentPage`
  - 新增主题7后端 `jest`
- Reused modules or patterns：
  - 权限读取与当前登录态解析复用 `indicator / promotion / feedback` 的 `baseSysMenuService + jwt + asyncContext` 模式
  - 列表查询、详情返回、错误语义和控制器声明复用现有 `performance` 模块写法
- New dependencies and why existing tools were insufficient：
  - 无
- Verification run after each meaningful change：
  - 新增代码后执行 `git diff --check`
  - 修复测试语法问题后执行 `npm run test -- test/performance/course.service.test.ts`
  - 单测通过后执行 `npm run build`
  - 构建通过后执行 `node + mysql2` 查询本地 `performance_course%` 表存在性
- Normal-path evidence：
  - `normalizeCourseAddPayload` 默认生成 `draft`
  - `buildCourseUpdatePatch` 支持 `draft -> published` 与 `published -> closed`
  - `enrollmentPage` 只返回 `userId / userName / enrollTime / status / score`
- Error-path evidence：
  - 非 `HR` 调用 `enrollmentPage` 被拒绝
  - 课程编码重复时报 `课程编码已存在`
  - `published` 修改标题时报 `已发布课程不允许修改标题`
- Boundary-case evidence：
  - `code` 允许为空并归一化为 `null`
  - `draft` 可删除；`published / closed` 删除被拒绝
  - `published` 状态仅允许改 `description / endDate / status`
- Permission checks：
  - `page / info` 与 `add / update / delete / enrollmentPage` 分权限键校验
  - 经理只读通过权限键收窄；员工无权限时报错
- State-flow checks：
  - 新增固定 `draft`
  - `draft` 仅允许保留 `draft` 或更新为 `published`
  - `published` 仅允许保持 `published` 或更新为 `closed`
  - `closed` 后禁止编辑和删除
- Contract checks：
  - 详情返回字段收敛到 `04/12` 冻结字段
  - 报名列表只返回摘要字段，不透出证书、考试、AI 或合作伙伴数据
- Change-type verification mapping：
  - 新资源接口与状态机：由 `course.service.test.ts` 覆盖状态、删除和权限拒绝
  - 后端构建链：由 `npm run build` 覆盖实体生成、打包与 `mwtsc`
- High-risk trigger changes：
  - 新增 API 资源
  - 新增状态机约束
  - 新增可空唯一字段 `code`
- Compatibility or fallback impact：
  - 不修改现有 `performance` 资源，不改变共享权限链路
  - 当前只新增主题7文件，不回写既有 controller/service
- Privacy/PII review result：
  - 报名列表只返回冻结摘要字段，未返回培训扩展记录或敏感全文
- Docs or operational updates made：
  - 新建并回写本任务卡

## Blockers And Waivers

- Open `P0` blockers：无
- Open `P1` blockers：无
- Open `P2` issues：无
- Approved waivers：无

## Before Commit

- Diff review result：
  - 当前窗口仅新增主题7后端文件与任务卡，未改已有共享高风险基础层
  - `git diff --check` 通过
- Verification summary：
  - 主题7后端单测通过
  - backend build 通过
  - 联调窗口已补齐 `performance_course / performance_course_enrollment` 运行时表结构并完成真实接口验证
- What was run：
  - `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && npm run test -- test/performance/course.service.test.ts`
  - `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && npm run build`
  - `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && node -e "const mysql=require('mysql2/promise'); (async()=>{const c=await mysql.createConnection({host:'127.0.0.1',port:3306,user:'root',password:'123456',database:'cool'}); const [rows]=await c.query(\"SHOW TABLES LIKE 'performance\\\\_course%'\"); console.log(JSON.stringify(rows)); await c.end();})().catch(err=>{console.error(err); process.exit(1);});"`
  - `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && git diff --check -- src/modules/performance/controller/admin/course.ts src/modules/performance/entity/course.ts src/modules/performance/entity/course-enrollment.ts src/modules/performance/service/course-helper.ts src/modules/performance/service/course.ts test/performance/course.service.test.ts /Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-18-二期主题7-培训课程管理-实现-A-后端.md`
- What passed：
  - `course.service.test.ts`：`7` 个测试全部通过
  - `npm run build`：通过
  - `git diff --check`：通过
- What was not run：
  - 本窗口自身未跑真实接口联调 / smoke；该部分已由联调窗口补齐
- Remaining risk：
  - 主题7运行态闭环证据依赖联调窗口，不再构成本窗口阻塞
- Tests added or updated, or why not：
  - 已新增 `test/performance/course.service.test.ts`
- Commit scope statement：
  - 当前窗口只提交主题7后端资源代码与任务卡，不包含前端、menu、seed 或共享基础层
- Commit summary：
  - 新增 `course` 后端主链与主题7最小 `jest`，并记录运行时表结构阻塞

## Before Release Or Handoff

- Acceptance-to-verification mapping：
  - `page / info / add / update / delete / enrollmentPage`：已完成代码实现
  - `published` 字段修改限制：已由 helper / service 测试覆盖
  - `code` 可空唯一：已由 helper / service 测试覆盖
  - `enrollmentPage` 只读摘要：已由 service 测试覆盖
- Contract alignment result：
  - 当前代码与 `04/05/06/09/12/24` 一致
  - `03-数据库设计.md` 尚未补主题7正文，当前仅保持代码侧最小建模
- Config changes：
  - 无
- Migration plan：
  - 当前窗口未提供；运行时表结构需由允许修改 seed / 建表脚本的窗口补齐
- Rollout steps：
  - 运行态表结构、菜单和真实接口联调已由联调窗口补齐
- Rollback path：
  - 当前窗口仅新增未接入现网菜单的新资源代码；若需回退，移除主题7新增文件即可
- Feature flag, kill switch, or staged rollout plan：
  - 不适用；当前尚未达到联调放行
- Data recovery path：
  - 当前未落 runtime 表，无新增业务数据
- Monitoring and diagnostic entry points：
  - 当前主要诊断入口为 backend build、主题7单测和本地库表存在性查询
- Final `P0` and `P1` blocker check：
  - `P0`：无
  - `P1`：无
- Approved waivers：
  - 无
- Final release risk statement：
  - 当前窗口后端主实现已完成；真实联调闭环已由主题7-C 补齐，不存在未关闭的本窗口阻塞项

## 修改文件

1. [course.ts](/Users/shaotongli/Documents/xuedao/cool-admin-midway/src/modules/performance/controller/admin/course.ts)
2. [course.ts](/Users/shaotongli/Documents/xuedao/cool-admin-midway/src/modules/performance/entity/course.ts)
3. [course-enrollment.ts](/Users/shaotongli/Documents/xuedao/cool-admin-midway/src/modules/performance/entity/course-enrollment.ts)
4. [course-helper.ts](/Users/shaotongli/Documents/xuedao/cool-admin-midway/src/modules/performance/service/course-helper.ts)
5. [course.ts](/Users/shaotongli/Documents/xuedao/cool-admin-midway/src/modules/performance/service/course.ts)
6. [course.service.test.ts](/Users/shaotongli/Documents/xuedao/cool-admin-midway/test/performance/course.service.test.ts)
7. [2026-04-18-二期主题7-培训课程管理-实现-A-后端.md](/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-18-二期主题7-培训课程管理-实现-A-后端.md)

## 最终结论

- `主题7后端主实现已完成，可进入联调`
