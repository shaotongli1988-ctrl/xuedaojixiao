<!-- 文件职责：记录二期主题11窗口B“采购与供应商管理前端主实现”的范围、门禁、验证计划和阶段结论；不负责后端接口实现、菜单运行时导入或联调 smoke；依赖主题11阶段0最终验收卡、19 模块拆分与 04/05/06/09/12/24 文档；维护重点是前端只能消费已冻结的 purchaseOrder / supplier 契约与权限边界。 -->
# 二期主题11任务卡：采购与供应商管理 - 实现 B - 前端

## 基本信息

- 任务名称：二期主题11 - 采购与供应商管理 - 实现 B - 前端
- 当前阶段：二期 / 阶段1 / 前端主实现
- 当前状态：done
- 任务等级：L4
- 涉及端：`cool-admin-vue`
- 负责人：Codex
- 创建时间：2026-04-18
- 最近更新时间：2026-04-18

## 需求摘要

- 本次要解决什么问题：把主题11已冻结的采购订单与供应商前端主链拆成正式可执行窗口，并固定页面、权限显隐和前端验证门槛。
- 目标效果是什么：窗口B启动后，只围绕 `purchaseOrder / supplier` 的列表、详情、新增、编辑、删除主链落地前端，不扩到采购全流程。
- 影响范围大概有哪些：`cool-admin-vue/src/modules/performance/` 下主题11相关 service / types / views，以及本任务卡回写。

## 当前结论

- 当前已经确认的结论：
  - 主题11前端已落地 `purchaseOrder / supplier` 的最小列表、详情、新增、编辑、删除主链页面与 service。
  - 页面权限已按冻结口径收束：经理仅可维护采购订单、供应商只读；员工无页面权限时只返回无权限空态。
  - 采购订单状态限制已前端收口为 `draft -> active / cancelled`、`active -> active` 或 `HR` 取消；供应商仅 `HR` 可维护，删除按钮只在 `inactive` 时显示。
- 当前阶段为什么可以关闭：窗口B允许范围内代码、类型和任务卡均已回写，且最小验证 `type-check / build` 已通过。
- 进入下一阶段前还差什么：等待窗口A后端真实接口与窗口C联调验证，确认菜单入口、真实账号与脱敏返回完全一致。

## 已完成内容

- [x] 主题11阶段1前端窗口卡已创建
- [x] 主题11阶段1前端范围、非目标、验证计划已固定
- [x] 已新增 `purchaseOrder / supplier` 前端 service
- [x] 已新增 `purchaseOrder / supplier` 页面并接入最小 CRUD 交互
- [x] 已补充主题11前端类型定义
- [x] 已执行 `corepack pnpm run type-check`
- [x] 已执行 `corepack pnpm run build`

## 实现边界

1. 本轮只允许实现：
   - 采购订单列表 / 详情 / 新增 / 编辑 / 删除
   - 供应商列表 / 详情 / 新增 / 编辑 / 删除
2. 本轮必须遵守：
   - 经理只允许维护本人部门树范围内采购订单
   - 经理查看供应商只读，且遵守敏感字段脱敏口径
   - 员工无页面入口、无操作按钮、无绕行访问结论
3. 本轮不允许：
   - 改共享 request / 鉴权基座
   - 自行扩出采购计划、询价、审批、收货 / 入库、对账、付款、库存联动、采购报表页面
   - 脱离已冻结权限键和状态规则做本地特判

## 自动化验证计划与结果

- 计划执行：
  - `cd /Users/shaotongli/Documents/xuedao/cool-admin-vue && corepack pnpm run type-check`
  - `cd /Users/shaotongli/Documents/xuedao/cool-admin-vue && corepack pnpm run build`
- 当前结果：
  - 已执行 `corepack pnpm run type-check`，结果：`通过`
  - 已执行 `corepack pnpm run build`，结果：`通过`
  - 已执行 `git -C /Users/shaotongli/Documents/xuedao diff --check -- cool-admin-vue/src/modules/performance/types.ts cool-admin-vue/src/modules/performance/service/purchase-order.ts cool-admin-vue/src/modules/performance/service/supplier.ts cool-admin-vue/src/modules/performance/views/purchase-order/index.vue cool-admin-vue/src/modules/performance/views/supplier/index.vue performance-management-system/tasks/2026-04-18-二期主题11-采购与供应商管理-实现-B-前端.md`，结果：`通过`

## 当前阻塞点

- 当前无未关闭 `P0 / P1`。
- 当前无阻塞本窗口关闭的前置缺口。

## 严格变更记录

### Before Starting Record

- Scope: 仅为主题11前端窗口补齐阶段1进入开发的正式任务卡与执行边界
- Non-goals: 不改前端业务代码、不改后端、不改 menu / 联调脚本、不改阶段0冻结正文
- Affected modules: `performance-management-system/tasks/`、`19-模块开发任务拆分.md`、`30-二期范围冻结草案.md`
- Acceptance criteria:
  - 窗口B任务卡已创建
  - 当前状态为 `ready`
  - 页面边界、角色边界、验证门槛与唯一事实源一致
- Impact surface: 任务卡与开发边界文档
- Source of truth:
  - [2026-04-18-二期主题11-采购与供应商管理-阶段0最终验收.md](/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-18-二期主题11-采购与供应商管理-阶段0最终验收.md)
  - [19-模块开发任务拆分.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/19-模块开发任务拆分.md)
  - [04-API设计.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/04-API设计.md)
  - [05-状态机与流转规则.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/05-状态机与流转规则.md)
  - [06-权限矩阵.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/06-权限矩阵.md)
  - [09-测试清单.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/09-测试清单.md)
  - [12-数据权限与脱敏规则.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/12-数据权限与脱敏规则.md)
  - [24-自动化测试策略与脚本规划.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/24-自动化测试策略与脚本规划.md)
- High-risk surfaces: 权限显隐、经理供应商只读、员工拒绝、敏感字段展示边界
- Required delivery evidence: 前端 `type-check`、前端 `build`、任务卡与受影响事实源回写
- Key assumptions and constraints: 仅落地主题11允许范围内的前端页面、service 和类型，不改菜单、seed、后端接口和共享鉴权基座

### During Implementation Record

- Change summary:
  - 新增 `purchaseOrder / supplier` service
  - 新增 `purchase-order / supplier` 页面
  - 扩展主题11前端类型与空表单工厂
- Reused modules or patterns: 复用主题10 `contract` 与既有 `performance` 页面 CRUD 模式、`checkPerm` 权限显隐、`BaseService` 请求封装、基础用户/部门接口
- New dependencies and why existing tools were insufficient: 无
- Verification run after each meaningful change:
  - 页面与 service 写入后执行 `corepack pnpm run type-check`
  - 任务卡回写前执行 `corepack pnpm run build`
- Minimum regression matrix evidence:
  - 正常：列表、详情、新增、编辑、删除入口均已落地
  - 异常：无页面权限时展示空态；非法状态切换在前端被拒绝
  - 边界：查询空值、金额大于 `0`、创建默认状态收口
- Permission checks:
  - `purchaseOrder` 删除按钮仅对 `performance:purchaseOrder:delete` 显示
  - `supplier` 新增 / 编辑 / 删除仅对 `HR` 对应权限显示
  - 无 `page` 权限时统一显示无权限空态
- State-flow checks:
  - `purchaseOrder`：`cancelled` 不可编辑；`active` 经理不可取消
  - `supplier`：仅 `active / inactive` 可维护；删除按钮仅在 `inactive`
- Contract checks:
  - 页面只消费 `page / info / add / update / delete`
  - 字段范围仅使用冻结的采购订单与供应商最小字段
- High-risk trigger changes: 权限显隐、状态流转、敏感字段展示依赖后端脱敏返回
- Privacy/PII review result: 前端不新增本地脱敏策略，只展示后端已裁剪结果，不额外缓存敏感信息
- Docs or operational updates made: 已回写本任务卡执行结果

### Before Commit Record

- Diff review result: 仅修改允许范围内的 `performance/types.ts`、主题11 service / views 与本任务卡，未触碰后端、菜单、seed、共享 request
- Verification summary:
  - `corepack pnpm run type-check`：通过
  - `corepack pnpm run build`：通过
  - 定向 `git diff --check`：通过
- What was not run: 未运行真实接口联调与菜单入口验证，待窗口C基于后端实现执行
- Remaining risk: 后端真实接口、角色数据与菜单导入若与冻结契约仍有偏差，联调阶段仍可能出现字段名或脱敏口径差异
- Open `P0` or `P1` blockers: 无
- Commit scope statement: 仅覆盖主题11窗口B前端最小主链实现与任务卡回写
- Commit summary: 新增采购订单与供应商前端页面、service、类型，并以 `type-check / build` 验证通过

## 推进记录

### 第 1 次更新

- 时间：2026-04-18
- 变更内容：创建主题11窗口B前端任务卡，固定前端实现边界、验证计划与非目标
- 当前状态：ready
- 下一步：进入 `purchaseOrder / supplier` 前端主实现

### 第 2 次更新

- 时间：2026-04-18
- 变更内容：完成 `purchaseOrder / supplier` 前端页面、service 和类型接入，并完成 `type-check / build`
- 当前状态：done
- 下一步：交由窗口C执行真实接口联调与收口

## 正式结论

- `主题11窗口B前端主实现已完成，可进入联调收口`
