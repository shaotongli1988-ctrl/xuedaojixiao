<!-- 文件职责：记录二期主题 6 窗口 A 对范围、来源对象和非目标边界的正式冻结结论；不负责接口正文、权限正文、缓存实现或任何代码开发；依赖 11/16/30 与主题 6 主卡；维护重点是窗口 A 只回答“做哪些来源域、消费什么来源对象、明确不做什么”。 -->
# 二期主题 6 任务卡：跨模块驾驶舱指标冻结

## 基本信息

- 任务名称：二期主题 6 - 跨模块驾驶舱指标 - 范围与来源对象冻结
- 当前阶段：二期 / 阶段 0 / 开发前冻结
- 当前状态：done
- 任务等级：L3
- 涉及端：`performance-management-system`
- 负责人：Codex
- 创建时间：2026-04-18
- 最近更新时间：2026-04-18

## 当前结论

- 当前窗口 A 唯一结论：`跨模块驾驶舱范围与来源对象已冻结`
- 当前是否允许进入设计 / 开发：`是`
- 当前是否存在未关闭 `P0 / P1`：
  - `P0`：`无`
  - `P1`：`无`

## 已冻结范围

1. 首批外部域固定为：
   - `招聘`
   - `培训`
   - `会议`
2. 除上述 3 个域外，其他外部域全部排除，不得顺带扩写。
3. 跨模块驾驶舱固定为“汇总视图”，不允许明细下钻、原始记录列表、导出、跳转外域后台或外域 CRUD。

## 已冻结来源对象

1. 绩效模块在主题 6 边界内只消费来源域聚合快照，不直接消费来源域原始明细。
2. 三域在绩效模块边界内统一为“来源快照契约”，即使上游内部生产方式不同，也不得把内部差异泄露为绩效模块的第二套消费模型。
3. 每个来源快照最小字段固定为：
   - `metricCode`
   - `metricLabel`
   - `metricValue`
   - `unit`
   - `periodType`
   - `periodValue`
   - `scopeType`
   - `deptKey`
   - `updatedAt`
   - `sourceStatus`
   - `statusText`

## 已冻结非目标

1. 不接真实外部系统实时接口。
2. 不做外部库表直连开发结论。
3. 不做员工端入口。
4. 不做全局 BI 平台、数据中台或经营分析平台。
5. 不开放任何跨域明细下钻。

## 自动化验证计划与结果

- 已执行命令：
  - `python3 /Users/shaotongli/.codex/skills/requirements-freeze-guard/scripts/requirements_freeze_guard.py --phase final --cwd /Users/shaotongli/Documents/xuedao/performance-management-system`
- 当前结果：
  - `requirements-freeze-guard`：通过，结果 `0 high / 0 medium / 0 low`

## 下一步动作

1. 窗口 A 已完成。
2. 后续实现必须继续以“来源域聚合快照”作为唯一来源对象，不得回退成原始明细拼装。
