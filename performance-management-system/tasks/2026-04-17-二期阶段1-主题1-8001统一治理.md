<!-- 文件职责：记录二期阶段 1 主题 1「8001 统一治理」的主实现范围、最小代码改动、验证证据、剩余风险与下一步；不负责修改业务规则、权限模型、状态机或主文档正文；依赖 README、00、14、15、20、24、25、26、30 文档与主题 1 冻结任务卡；维护重点是只收敛共享运行入口，不能借机扩写到其他二期主题。 -->
# 二期阶段 1 任务卡：主题 1 - 8001 统一治理

## 基本信息

- 任务名称：二期阶段 1 - 主题 1「8001 统一治理」主实现
- 当前阶段：二期 / 阶段 1 / 主实现
- 当前状态：done
- 任务等级：L4
- 涉及端：`cool-admin-midway`、`cool-admin-vue`、`performance-management-system`
- 负责人：Codex
- 创建时间：2026-04-17
- 最近更新时间：2026-04-17

## Before Starting

- Scope：
  - 统一 backend 默认开发入口、默认 smoke 入口和 latest 运行口径到 `8001`。
  - 统一 frontend `http://127.0.0.1:9000 -> /dev -> http://127.0.0.1:8001` 默认代理口径。
  - 保留 `8006` 的显式回退执行路径，但不再保留为默认 latest。
- Non-goals：
  - 不开发导出增强。
  - 不开发 `cool-uni`。
  - 不修改业务规则、权限规则、状态机、接口契约。
  - 不修改 `14/15/24/13` 主文档。
- Affected modules：
  - [cool-admin-vue/src/config/proxy.ts](/Users/shaotongli/Documents/xuedao/cool-admin-vue/src/config/proxy.ts)
  - [cool-admin-midway/package.json](/Users/shaotongli/Documents/xuedao/cool-admin-midway/package.json)
  - 本任务卡
- Acceptance criteria：
  - `cool-admin-midway npm run dev` 默认以 `8001` 为唯一共享开发口径。
  - `cool-admin-midway npm run smoke:stage2:performance` 默认以 `8001` 为唯一 smoke 口径。
  - `cool-admin-vue` 默认代理改为 `9000 -> /dev -> 8001`。
  - 仍保留 `8006` 的显式回退执行方式，不需要改业务代码即可切回。
- Impact surface：
  - 本地开发默认端口
  - smoke 默认入口
  - frontend dev 代理目标
  - 回退执行路径
- Source of truth：
  - [README.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/README.md)
  - [00-唯一事实源总索引.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/00-唯一事实源总索引.md)
  - [14-交付说明.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/14-交付说明.md)
  - [15-回滚说明.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/15-回滚说明.md)
  - [20-阶段门禁与完成定义.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/20-阶段门禁与完成定义.md)
  - [24-自动化测试策略与脚本规划.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/24-自动化测试策略与脚本规划.md)
  - [25-项目最高准则.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/25-项目最高准则.md)
  - [26-可执行联调门禁与Smoke规则.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/26-可执行联调门禁与Smoke规则.md)
  - [30-二期范围冻结草案.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/30-二期范围冻结草案.md)
  - [2026-04-17-二期主题1-8001统一治理冻结.md](/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-17-二期主题1-8001统一治理冻结.md)
- Requirement conflicts or unclear points that must be resolved before coding：
  - 当前代码里 backend 默认端口与 smoke 默认值已是 `8001`，真正漂移点是 frontend `/dev` 代理仍指向 `8006`；本轮只修正真实漂移点，不制造额外入口。
- High-risk surfaces：
  - 默认入口
  - 兼容回退路径
  - 联调 / smoke 放行口径
- Required delivery evidence：
  - 任务卡回写
  - 最小实现 diff
  - backend 端口守卫 / smoke 相关验证
  - frontend `type-check` / `build`
- Required test matrix：
  - 正常路径：默认 `8001` 入口可解析并可执行。
  - 异常路径：端口守卫对旧实例漂移仍能提示阻断。
  - 边界路径：通过显式环境变量切回 `8006`。
- Key assumptions and constraints：
  - `8006` 只能保留为回退入口，不再作为默认 latest。
  - 不新增新依赖、不改共享基础层、不改业务模块。

## During Implementation

- Change summary：
  - 将 frontend `/dev` 默认代理从固定 `8006` 改为默认 `8001`。
  - 为 frontend 代理增加显式环境变量覆盖能力，允许临时切回 `8006`，不需要再次改代码。
  - 为 backend 补充 `smoke:stage2:performance:8001` 与 `smoke:stage2:performance:8006` 两个可执行别名，固定默认与回退入口。
  - 新建本任务卡，记录范围、验证证据、阻塞与下一步。
- Reused modules or patterns：
  - 复用现有 `PORT`、`STAGE2_SMOKE_BASE_URL` 与 Vite proxy 配置模式，不新增治理层。
- New dependencies and why existing tools were insufficient：
  - 无。
- Verification run after each meaningful change：
  - 修改 `proxy.ts` 与 `package.json` 后，执行 backend 端口守卫、frontend `type-check`、frontend `build`、`8001` smoke、`8006` smoke。
  - 收口前执行 `unified_delivery_guard.py --phase final` 做实现阶段终检。
- Normal-path evidence：
  - `corepack pnpm run type-check` 通过。
  - `corepack pnpm run build` 通过。
  - `npm run smoke:stage2:performance:8001` 成功命中 `http://127.0.0.1:8001`，证明默认 smoke 入口已统一到 `8001`。
- Error-path evidence：
  - `node ./scripts/dev-port-guard.mjs` 在同仓库已有 `8001` 监听实例时正确阻断，提示先释放旧进程或复用既有端口。
- Boundary-case evidence：
  - `npm run smoke:stage2:performance:8006` 成功命中 `http://127.0.0.1:8006`，证明回退 smoke 路径可执行。
- Permission checks：
  - 不涉及权限变更。
- State-flow checks：
  - 不涉及状态机变更。
- Contract checks：
  - 不涉及接口契约变更。
- Change-type verification mapping：
  - backend 配置/脚本：端口守卫、smoke 默认入口验证。
  - frontend 代理：`type-check`、`build`。
- High-risk trigger changes：
  - 默认入口与兼容回退口径。
- Compatibility or fallback impact：
  - 默认切到 `8001`，`8006` 仅通过显式环境变量或回退脚本执行。
- Privacy/PII review result：
  - 不涉及新增 PII 处理。
- Docs or operational updates made：
  - 仅回写本任务卡。

## Blockers And Waivers

- Open `P0` blockers：无
- Open `P1` blockers：无
- Open `P2` issues：无
- Approved waivers：无

## Before Commit

- Diff review result：
  - 仅修改允许范围内的 3 个文件：frontend 代理、backend `package.json`、本任务卡。
- Verification summary：
  - 默认 `8001` 入口代码已落地，frontend 编译校验通过，回退 `8006` 命令可执行。
  - 定向清理本地 `UAT-*` 绩效残留后，重新执行标准 seed，`8001` 与 `8006` 两条真实 smoke 均恢复为 `PASS 41 / FAIL 0 / SKIP 0 / Conclusion: PASSED`。
- What was run：
  - `python3 /Users/shaotongli/.codex/skills/fullstack-unified-development-standards/scripts/unified_delivery_guard.py --phase start --cwd /Users/shaotongli/Documents/xuedao --task "二期阶段1 主题1 8001统一治理主实现：统一 backend default/latest/smoke 到 8001，frontend /dev 代理到 8001，并保留 8006 回退路径" --fail-on none`
  - `lsof -nP -iTCP:8001 -sTCP:LISTEN || true`
  - `lsof -nP -iTCP:8006 -sTCP:LISTEN || true`
  - `node ./scripts/dev-port-guard.mjs`
  - `npm run smoke:stage2:performance:8001`
  - `corepack pnpm run type-check`
  - `corepack pnpm run build`
  - `npm run smoke:stage2:performance:8006`
  - `node --input-type=module <<'EOF' ... SELECT id, code FROM performance_assessment WHERE code LIKE 'UAT-%' ... EOF`
  - `node --input-type=module <<'EOF' ... DELETE local UAT-* assessment/goal related rows in transaction ... EOF`
  - `node ./scripts/seed-stage2-performance.mjs`
  - `node --input-type=module <<'EOF' ... SELECT COUNT(*) FROM performance_assessment / performance_goal ... EOF`
  - `npm run smoke:stage2:performance:8001`
  - `npm run smoke:stage2:performance:8006`
  - `python3 /Users/shaotongli/.codex/skills/fullstack-unified-development-standards/scripts/unified_delivery_guard.py --phase final --cwd /Users/shaotongli/Documents/xuedao --task "二期阶段1 主题1 8001统一治理主实现收口" --changed-file /Users/shaotongli/Documents/xuedao/cool-admin-vue/src/config/proxy.ts --changed-file /Users/shaotongli/Documents/xuedao/cool-admin-midway/package.json --changed-file /Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-17-二期阶段1-主题1-8001统一治理.md`
- What passed：
  - frontend `type-check` 通过。
  - frontend `build` 通过。
  - `8001` 与 `8006` 都存在真实监听实例。
  - backend 端口守卫正确阻断同仓库重复启动。
  - `8001` 与 `8006` smoke 别名脚本都可执行并命中对应端口。
  - 本地 `UAT-*` 绩效残留已清理，重建 seed 后 `performance_assessment=5`、`performance_goal=4`。
  - `npm run smoke:stage2:performance:8001` 通过，结果 `PASS 41 / FAIL 0 / SKIP 0 / Conclusion: PASSED`。
  - `npm run smoke:stage2:performance:8006` 通过，结果 `PASS 41 / FAIL 0 / SKIP 0 / Conclusion: PASSED`。
- What was not run：
  - 未新增测试文件；当前窗口允许范围不包含测试路径，本轮以真实 smoke 与前端构建校验作为最小验证证据。
- Remaining risk：
  - `unified_delivery_guard` final 仍会报出仓库级既有高风险提示：
    - API schema drift 缺少 producer/openapi 真实输入；
    - 全栈测试矩阵按仓库治理要求仍提示缺少测试文件变更；
    - tech stack drift 仅提示 `package.json` 变更未伴随锁文件变化，为低风险提示，本轮未新增依赖。
- Tests added or updated, or why not：
  - 本轮仅做运行入口治理，不改业务逻辑；当前窗口允许范围未包含测试文件路径，故未补 repo 内测试文件，改用真实 smoke、端口守卫、frontend `type-check/build` 作为最小验证证据。
- Commit scope statement：
  - 仅处理 `8001` 统一治理需要的默认入口、代理和回退执行路径。
- Commit summary：
  - 完成 `8001` 默认入口治理最小实现，清理本地 `UAT-*` 残留后恢复 `8001/8006` 双端口真实 smoke 通过。

## Before Release Or Handoff

- Acceptance-to-verification mapping：
  - backend 默认入口统一到 `8001`：
    - 证据：`config.default.ts` 与 `dev-port-guard.mjs` 现状均以 `8001` 为默认端口，`node ./scripts/dev-port-guard.mjs` 真实命中 `8001` 监听实例。
  - smoke 默认入口统一到 `8001`：
    - 证据：`npm run smoke:stage2:performance:8001` 真实命中 `http://127.0.0.1:8001`。
  - frontend `/dev` 代理统一到 `8001`：
    - 证据：`proxy.ts` 默认目标已改为 `http://127.0.0.1:8001`，`corepack pnpm run type-check` 与 `corepack pnpm run build` 通过。
  - 保留 `8006` 回退路径：
    - 证据：`npm run smoke:stage2:performance:8006` 真实命中 `http://127.0.0.1:8006`。
  - 本地数据集恢复到 stage2 基线：
    - 证据：清理 `UAT-*` 残留后，重新 seed，库内总量恢复为 `performance_assessment=5`、`performance_goal=4`。
- Contract alignment result：
  - 未改接口契约。
- Config changes：
  - frontend `proxy.ts` 默认代理目标改为 `8001`，并支持 `COOL_ADMIN_PROXY_TARGET` / `VITE_DEV_PROXY_TARGET` 覆盖。
  - backend `package.json` 新增两个 smoke 别名脚本，分别对应 `8001` 默认口径与 `8006` 回退口径。
- Migration plan：
  - 无数据库变更。
- Rollout steps：
  1. 默认使用 `cool-admin-midway npm run dev` 启动 `8001`。
  2. 默认使用 `cool-admin-vue` 本地 dev，通过 `/dev` 代理访问 `8001`。
  3. 使用 `npm run smoke:stage2:performance:8001` 做默认 smoke。
- Rollback path：
  1. frontend 临时回退：`COOL_ADMIN_PROXY_TARGET=http://127.0.0.1:8006 corepack pnpm run dev`
  2. backend smoke 回退：`npm run smoke:stage2:performance:8006`
- Feature flag, kill switch, or staged rollout plan：
  - 使用显式环境变量 / 回退脚本切回 `8006`。
- Data recovery path：
  - 无数据变更。
- Monitoring and diagnostic entry points：
  - `dev-port-guard.mjs`
  - `smoke-stage2-performance.mjs`
- Final `P0` and `P1` blocker check：
  - `P0`：无。
  - `P1`：无。
- Approved waivers：无
- Final release risk statement：
  - `8001` 统一入口主实现和最小回退路径已完成，当前可进入验证收口；剩余风险为仓库级既有守卫提示，不属于本窗口新增阻塞。

## 当前结论

- 唯一结论：`8001 统一治理主实现已完成，可进入验证收口`

## 下一步

1. 阶段 1 验证与最终验收已补齐真实 smoke、frontend 门禁和 `9000 -> /dev -> 8001` GUI 点测留痕。
2. 后续仅保留 `8006` 作为回滚 / 历史入口，不再作为 current latest 默认入口。
