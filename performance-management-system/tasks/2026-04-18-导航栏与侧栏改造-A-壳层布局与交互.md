<!-- 文件职责：记录“导航栏与侧栏改造”窗口A的前端壳层实现范围、写集与验证门槛；不负责菜单分组算法、不负责业务页面内部改造；依赖业务域归属冻结结果与 cool-admin-vue 现有 topbar/slider 壳层实现；维护重点是先完成壳层视觉与交互承载，不改业务语义。 -->
# 导航栏与侧栏改造 A：壳层布局与交互

## 基本信息

- 任务名称：导航栏与侧栏改造 A - 壳层布局与交互
- 当前阶段：二期 / 阶段1 / 前端实现
- 当前状态：reviewing
- 任务等级：L4
- 涉及端：`cool-admin-vue`
- 负责人：Codex
- 创建时间：2026-04-18
- 最近更新时间：2026-04-18

## 当前结论

- 当前正式结论：`窗口A壳层布局与交互实现已完成，待人工点测后关单`

## Scope

1. 改造顶栏承载样式，使一级业务域导航可见
2. 调整左侧壳层视觉与交互，使其与顶栏分工明确
3. 保持折叠、搜索、移动端适配和当前用户工具区可用

## Non-goals

1. 不负责菜单分组来源和业务域映射规则
2. 不修改业务页面内部布局
3. 不修改权限判断逻辑
4. 不改后端或 `menu.json`

## 建议写集

1. `/Users/shaotongli/Documents/xuedao/cool-admin-vue/src/modules/base/pages/main/components/topbar.vue`
2. `/Users/shaotongli/Documents/xuedao/cool-admin-vue/src/modules/base/pages/main/components/amenu.vue`
3. `/Users/shaotongli/Documents/xuedao/cool-admin-vue/src/modules/base/pages/main/components/slider.vue`
4. `/Users/shaotongli/Documents/xuedao/cool-admin-vue/src/modules/base/pages/main/components/route-nav.vue`
5. 本任务卡

## 禁止事项

1. 不改 `menu.ts` 分组逻辑
2. 不改 `config.app.menu.isGroup` 开关口径
3. 不改业务页面内容区
4. 不引入新 UI 框架或新布局系统

## Acceptance

1. 顶栏能清晰承载一级业务域切换
2. 左侧栏仍保留搜索与折叠，不破坏当前二级菜单访问
3. 在桌面端与移动端都不出现明显布局断裂
4. 用户信息、工具栏、路由定位仍可正常工作

## Verification Plan

1. `corepack pnpm run type-check`
2. `corepack pnpm run build`
3. 桌面端点测：
   - 切换一级业务域
   - 左侧栏折叠 / 展开
   - 搜索过滤
4. 小屏点测：
   - 菜单收起 / 展开
   - 顶栏不遮挡主要操作

## 风险

1. `topbar / slider` 属于共享壳层，误改会影响全部后台模块
2. 如果壳层先做强样式改造、但不兼容当前菜单联动，会导致页面可见但不可用

## 当前阻塞项

- 当前唯一阻塞项：`桌面端与小屏人工点测证据尚未回写`

## 已完成改动

1. 顶栏改为“一级导航 + 路由定位”组合承载
2. 一级导航横向可滚动，避免业务域数量增加后直接折行
3. 左侧栏新增当前业务域标识区块，并保持搜索与折叠链路可用
4. 顶栏分组模式下移除多余弹性占位，避免导航可用宽度被壳层空白挤占

## 执行命令

1. `cd /Users/shaotongli/Documents/xuedao/cool-admin-vue && corepack pnpm run type-check`
2. `cd /Users/shaotongli/Documents/xuedao/cool-admin-vue && corepack pnpm run build`

## 执行结果

1. `type-check`：通过
2. `build`：通过

## 剩余风险

1. 当前未在真实浏览器窗口下补回桌面端 / 小屏壳层点测截图或录屏
2. 若后续角色权限进一步裁剪顶栏项目，仍需再看极窄宽度下的导航拥挤表现

## 下一步

1. 交由窗口 C 补回桌面端与小屏点测
2. 无新增布局回归后推进为 `done`
