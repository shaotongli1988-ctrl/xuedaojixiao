<!-- 文件职责：记录二期主题13“人才发展与认证增强”窗口A后端主实现的范围、改动、验证和风险；不负责前端、共享基础层、课程主链、面试主链或人才资产主链调整；依赖 04/05/06/09/12 与主题13阶段0最终验收卡；维护重点是后端实现只能落 capabilityModel / capabilityItem / capabilityPortrait / certificate 冻结范围。 -->
# 二期主题13-人才发展与认证增强-实现-A-后端

## 基本信息

- 当前阶段：二期 / 阶段1 / 实现
- 当前状态：done
- 负责人：Codex
- 创建时间：2026-04-18
- 最近更新时间：2026-04-18

## Before Starting Record

- Scope:
  - 实现 `capabilityModel / capabilityItem / capabilityPortrait / certificate` 后端最小主链
- Non-goals:
  - 不改前端
  - 不做合作伙伴
  - 不做课程学习过程
  - 不做面试流程
  - 不做人才资产主链
  - 不做移动端
- Affected modules:
  - `cool-admin-midway/src/modules/performance/controller/admin`
  - `cool-admin-midway/src/modules/performance/entity`
  - `cool-admin-midway/src/modules/performance/service`
  - `cool-admin-midway/test/performance`
  - 本任务卡
- Acceptance criteria:
  - 能力模型支持 `page / info / add / update`
  - 能力项支持 `info`
  - 能力画像支持摘要 `info`
  - 证书支持 `page / info / add / update / issue / recordPage`
  - `HR` 全量；经理只读定义且画像/记录仅部门树范围；员工不可见
  - 能力模型状态固定 `draft / active / archived`
  - 证书状态固定 `draft / active / retired`
  - 证书记录状态固定 `issued / revoked`
  - `sourceCourseId` 可空，不强制课程结业
  - 不返回人才档案全文、简历全文、面试评语全文、课程学习过程、证书附件
- Impact surface:
  - entity、service、controller、后端 jest、本任务卡
- Source of truth:
  - `04-API设计.md`
  - `05-状态机与流转规则.md`
  - `06-权限矩阵.md`
  - `09-测试清单.md`
  - `12-数据权限与脱敏规则.md`
  - `2026-04-18-二期主题13-人才发展与认证增强-阶段0最终验收.md`
- High-risk surfaces:
  - 接口契约
  - 权限与部门树范围
  - 状态流转
  - 脱敏与摘要字段裁剪
- Required delivery evidence:
  - 主题13相关后端测试
  - backend build 或等效验证
  - 本任务卡回写
- Key assumptions and constraints:
  - 菜单 / 权限 seed / 前端联调不在本窗口范围内
  - `sourceCourseId` 只做引用，不校验课程结业

## During Implementation Record

- Change summary:
  - 新增 `capabilityModel / capabilityItem / capabilityPortrait / certificate / certificateRecord` 五张主题13后端实体，落地能力地图定义、画像摘要与证书台账/记录主链。
  - 新增 `capability-helper / certificate-helper`，服务端强制执行能力模型与证书状态流转、可空编码、可空 `sourceCourseId` 和输入归一化。
  - 新增 `capability` 服务与 `capabilityModel / capabilityItem / capabilityPortrait` 控制器，落地模型分页/详情/新增/修改、能力项详情和画像摘要。
  - 新增 `certificate` 服务与 `certificate` 控制器，落地证书分页/详情/新增/修改/发放/记录分页。
  - 服务端强制执行权限与范围：`HR` 全量；经理只读定义；画像摘要与证书记录仅部门树范围；员工拒绝访问。
  - 服务端强制执行摘要裁剪：不返回人才档案全文、简历全文、面试评语全文、课程学习过程、证书附件。
- Reused modules or patterns:
  - 复用 `course / interview / meeting / contract` 的 request-scope service、权限校验和最小 controller 模式
- New dependencies and why existing tools were insufficient:
  - 无
- Verification run after each meaningful change:
  - 首轮实现后执行 `npm test -- test/performance/capability.service.test.ts test/performance/certificate.service.test.ts`
  - 首轮 `npm run build` 失败，定位为 helper 通配导出命名冲突；在允许范围内重命名 helper 导出后再次执行 `npm run build`
- Minimum regression matrix evidence:
  - 正常：
    - `should return model page with item count summary`
    - `should return scoped capability portrait summary only`
    - `should issue active certificate without course completion validation`
    - `should scope certificate record page by department and trim fields`
  - 异常：
    - `should reject manager add capability model`
    - `should reject employee access to capability item info`
    - `should reject out of scope portrait for manager`
    - `should reject manager update certificate`
    - `should reject issuing non-active certificate`
    - `should reject employee certificate page access`
  - 边界：
    - `should reject updating archived capability model`
    - `should reject updating retired certificate`
    - `should allow nullable sourceCourseId on certificate payload`
- Permission checks:
  - 能力模型 `add / update` 仅 `HR`
  - 能力项详情、模型分页/详情、证书分页/详情对经理开放只读
  - 能力画像摘要与证书记录分页对经理加部门树范围限制
  - 员工无主题13入口
- State-flow checks:
  - 能力模型仅允许 `draft -> active -> archived`
  - `archived` 能力模型禁止继续编辑
  - 证书仅允许 `draft -> active -> retired`
  - `retired` 证书禁止继续编辑
  - 非 `active` 证书禁止发放记录
- Contract checks:
  - 控制器路径按冻结事实源落到 `/admin/performance/capabilityModel|capabilityItem|capabilityPortrait|certificate/...`
  - 画像摘要只返回 `capabilityTags / levelSummary / certificateCount / lastCertifiedAt / updatedAt`
  - 证书记录只返回冻结的摘要字段，不返回附件字段
- High-risk trigger changes:
  - 新增主题13后端接口、状态与权限规则
- Privacy/PII review result:
  - 已确认接口只返回冻结摘要字段，不返回人才档案全文、简历全文、面试评语全文、课程学习过程、证书附件
- Docs or operational updates made:
  - 本任务卡

## Before Commit

- Diff review result:
  - 改动限定在主题13窗口A允许范围；额外变更仅有 `npm run build` 自动生成的 [entities.ts](/Users/shaotongli/Documents/xuedao/cool-admin-midway/src/entities.ts)
- Verification summary:
  - 主题13定向后端测试通过；backend build 通过
- What was not run:
  - 未执行前端验证
  - 未执行真实联调 / smoke
- Remaining risk:
  - 运行态 menu / perms seed 与前端入口不在本窗口范围内，仍需其他并行窗口补齐
  - 当前为服务级后端验证，主题13整体仍需联调窗口补真实 smoke
- Open `P0` or `P1` blockers:
  - 无
- Commit scope statement:
  - 本窗口不提交 commit
- Commit summary:
  - 主题13后端主链完成，主题13相关后端测试与 build 已通过

## Final Conclusion

- 结论：主题13后端主实现已完成，可交由并行前端与联调窗口继续收口

## 实际执行命令

- `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && npm test -- test/performance/capability.service.test.ts test/performance/certificate.service.test.ts`
- `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && npm run build`

## 实际结果

- `npm test -- test/performance/capability.service.test.ts test/performance/certificate.service.test.ts`
  - 通过，`2` 个 suite、`14` 个用例全绿
- `npm run build`
  - 首轮失败，原因：`capability-helper` 与 `certificate-helper` 均导出了同名 `normalizeOptionalText`，与已有 `src/index.ts` 的通配导出冲突
  - 修复后再次执行通过，`cool entity && bundle && mwtsc --cleanOutDir` 成功
