<!-- 文件职责：记录二期主题12“招聘人才资产增强”联调测试准备、命令矩阵、执行留痕与阻塞项；不负责主题12主实现开发或业务规则变更；依赖阶段0冻结材料、测试/交付门禁事实源与当前仓库真实运行态；维护重点是只能把真实执行结果写成通过，未执行项必须保留原因。 -->
# 二期主题12任务卡：招聘人才资产增强实现 C - 联调测试

## 基本信息

- 任务名称：二期主题12 - 招聘人才资产增强 - 实现 C - 联调测试
- 当前阶段：二期 / 阶段2 / 联调测试完成
- 当前状态：done
- 任务等级：L3
- 涉及端：`cool-admin-midway`、`cool-admin-vue`、`performance-management-system`
- 负责人：Codex
- 创建时间：2026-04-18
- 最近更新时间：2026-04-18

## Before Starting（strict-change 关键记录）

- Scope：
- 仅围绕 `talentAsset` 联调测试链路做验证准备、执行留痕和阻塞项管理。
  - 覆盖 `HR` 成功、经理范围成功/越权拒绝、员工无入口。
- 只写联调任务卡，不改业务规则与主文档。
- Non-goals：
  - 不做主题12后端/前端主实现开发。
  - 不改 `docs/*` 主文档与冻结结论。
  - 不扩展到招聘计划、简历池全文、录用管理、内部员工画像。
- Affected modules：
  - [2026-04-18-二期主题12-招聘人才资产增强-实现-C-联调测试.md](/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-18-二期主题12-招聘人才资产增强-实现-C-联调测试.md)
- Acceptance criteria：
  - 产出主题12联调验证矩阵与可执行命令清单。
  - 最终留痕至少包含 `backend 定向测试`、`backend build`、`frontend type-check`、`frontend build`、`最小 smoke/GUI 建议` 的执行状态。
  - 不把未执行项写成通过。
- Impact surface：
  - 测试执行入口、联调任务卡留痕、阻塞项管理。
- Source of truth：
  - [09-测试清单.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/09-测试清单.md)
  - [14-交付说明.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/14-交付说明.md)
  - [20-阶段门禁与完成定义.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/20-阶段门禁与完成定义.md)
  - [24-自动化测试策略与脚本规划.md](/Users/shaotongli/Documents/xuedao/performance-management-system/docs/24-自动化测试策略与脚本规划.md)
  - [2026-04-18-二期主题12-招聘人才资产增强-阶段0冻结.md](/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-18-二期主题12-招聘人才资产增强-阶段0冻结.md)
  - [2026-04-18-二期主题12-招聘人才资产增强-阶段0最终验收.md](/Users/shaotongli/Documents/xuedao/performance-management-system/tasks/2026-04-18-二期主题12-招聘人才资产增强-阶段0最终验收.md)
- High-risk surfaces：
  - 权限越权（经理部门树范围、员工无入口）
  - 状态与删除约束（`new/tracking/archived`、删除仅 `HR`）
  - 字段级脱敏（联系方式/简历全文禁止返回）
- Required delivery evidence：
  - 命令级执行留痕（已执行、未执行、原因）
  - 唯一阻塞项（如存在）
  - 联调最小点测建议
- Key assumptions and constraints：
- 共享菜单 / 权限种子不在本窗口允许修改范围内。

## 验证矩阵与命令

| 验证项 | 目标 | 命令 | 当前状态 |
| --- | --- | --- | --- |
| backend 定向测试 | 覆盖状态流、部门树权限、删除限制、不自动创建面试单、敏感字段不返回 | `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && npm run test -- test/performance/talent-asset.service.test.ts` | 已通过 |
| backend build | 验证主题12接入后后端可构建 | `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && npm run build` | 已通过 |
| frontend type-check | 验证主题12页面与类型契约 | `cd /Users/shaotongli/Documents/xuedao/cool-admin-vue && corepack pnpm run type-check` | 已通过 |
| frontend build | 验证主题12页面构建链路 | `cd /Users/shaotongli/Documents/xuedao/cool-admin-vue && corepack pnpm run build` | 已通过 |
| 最小 smoke / GUI 点测 | 覆盖 `HR` 成功、经理范围成功/越权拒绝、员工无入口 | `cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && node scripts/seed-stage2-performance.mjs`<br>`cd /Users/shaotongli/Documents/xuedao/cool-admin-midway && STAGE2_SMOKE_BASE_URL=http://127.0.0.1:8014 node scripts/smoke-stage2-performance.mjs` | 已通过 |
| 主题12真实 API 联调 | 覆盖 `permmenu`、`page/info/add/update/delete`、范围内/范围外/无权限链路 | `base=http://127.0.0.1:8014` 上使用 `hr_admin / manager_rd / employee_platform` 真实登录后调用 `talentAsset` 接口 | 已通过 |

## 已执行命令与结果（环境预检）

1. `node -v`
   - 结果：`v25.5.0`
2. `npm -v`
   - 结果：`11.8.0`
3. `corepack pnpm -v`
   - 结果：`10.33.0`
4. `ls -d /Users/shaotongli/Documents/xuedao/cool-admin-midway/node_modules`
   - 结果：目录存在
5. `ls -d /Users/shaotongli/Documents/xuedao/cool-admin-vue/node_modules`
   - 结果：目录存在
6. `npm run test -- test/performance/talent-asset.service.test.ts`
   - 结果：通过，`7/7` 用例通过
7. `npm run build`
   - 结果：通过
8. `corepack pnpm run type-check`
   - 结果：通过
9. `corepack pnpm run build`
   - 结果：通过
10. `rg -n "performance:talentAsset|/performance/talentAsset|talent-asset|人才资产" /Users/shaotongli/Documents/xuedao/cool-admin-midway/src/modules/base/menu.json /Users/shaotongli/Documents/xuedao/cool-admin-midway/scripts/smoke-stage2-performance.mjs`
   - 结果：已匹配；共享菜单 / 权限种子与 smoke 断言已纳入主题12
11. `node scripts/seed-stage2-performance.mjs`
   - 结果：通过；本地数据库已写入主题12菜单与角色菜单绑定
12. `npm run smoke:stage2:performance`
   - 结果：主题12相关 `permmenu` 断言通过：
     - `hr_admin permmenu` 通过，包含 `/performance/talentAsset` 与 `performance:talentAsset:*`
     - `manager_rd permmenu` 通过，包含 `/performance/talentAsset` 与 `performance:talentAsset:page/info/add/update`
     - `employee_platform permmenu` 通过，确认无 `/performance/talentAsset` 且无 `performance:talentAsset:*`
   - 同次全量 smoke 仍失败，但失败项为主题12外既有问题：
     - `promotion:page`
     - `capabilityModel:page`
     - `certificate:page`
     - `courseRecite:page`
     - `coursePractice:page`
     - `courseExam:summary`
13. `node scripts/seed-stage2-performance.mjs`
   - 结果：通过；已补建 `performance_talent_asset` 表并写入主题12联调样例：
     - `联调-主题12平台人才`
     - `联调-主题12销售人才`
14. `base=http://127.0.0.1:8014` 上执行主题12定向真实 API 联调
   - 结果：通过
   - `hr_admin`
     - `permmenu` 包含 `/performance/talentAsset` 与 `performance:talentAsset:page/info/add/update/delete`
     - `POST /admin/performance/talentAsset/page` 成功，返回平台/销售两条样例
     - `GET /admin/performance/talentAsset/info` 成功，未泄露联系方式/简历全文等敏感字段
     - `POST /admin/performance/talentAsset/add` 成功
     - `POST /admin/performance/talentAsset/delete` 成功
   - `manager_rd`
     - `permmenu` 包含 `/performance/talentAsset` 与 `performance:talentAsset:page/info/add/update`
     - `POST /admin/performance/talentAsset/page` 仅返回平台样例，不返回销售样例
     - `POST /admin/performance/talentAsset/update` 范围内成功，状态保持 `tracking`
     - `POST /admin/performance/talentAsset/add` 对销售部门越权新增被拒绝，返回 `无权操作该人才资产`
     - `POST /admin/performance/talentAsset/delete` 被拒绝，返回 `无权限删除人才资产`
   - `employee_platform`
     - `permmenu` 不含 `/performance/talentAsset` 与主题12权限点
     - `POST /admin/performance/talentAsset/page` 被拒绝，返回 `无权限查看人才资产列表`
15. `STAGE2_SMOKE_BASE_URL=http://127.0.0.1:8014 node scripts/smoke-stage2-performance.mjs`
   - 结果：通过，`PASS: 93 / FAIL: 0 / SKIP: 0`
   - 主题12新增自动化断言全部通过：
     - `hr_admin talentAsset:page/info/add/delete`
     - `manager_rd talentAsset:page/info/update/add:out-of-scope/delete`
     - `employee_platform talentAsset:page`

## 未执行项与原因

1. 无

## 建议人工 GUI 点击脚本

1. 预置入口：
   - frontend：`http://127.0.0.1:9001`
   - backend 代理目标：`/dev -> http://127.0.0.1:8014`
2. 登录账号：
   - `hr_admin / 123456`
   - `manager_rd / 123456`
   - `employee_platform / 123456`
3. `HR` 点击路径：
   - 登录后进入左侧菜单 `招聘人才资产`
   - 截图列表页，确认可见：
     - `联调-主题12平台人才`
     - `联调-主题12销售人才`
   - 点开 `联调-主题12平台人才` 详情，截图详情弹窗
   - 重点确认页面未展示手机号、邮箱、微信、简历全文、附件链接
   - 点击 `新建人才资产`
   - 填写：
     - 候选人姓名：`GUI-主题12-HR样例`
     - 编码：`GUI-TALENT-HR-001`
     - 目标部门：`平台组`
     - 目标岗位：`平台工程师`
     - 来源：`GUI点测`
     - 标签：`GUI`
     - 跟进摘要：`GUI 点测新增`
     - 下次跟进日期：任意有效日期
   - 提交成功后截图
   - 删除该条 `GUI-主题12-HR样例`，截图删除成功结果
4. `经理` 点击路径：
   - 登录后进入左侧菜单 `招聘人才资产`
   - 截图列表页，确认只看到 `联调-主题12平台人才`，看不到 `联调-主题12销售人才`
   - 点开 `联调-主题12平台人才` 详情，截图详情弹窗
   - 点击编辑，把状态改为 `tracking` 或更新跟进摘要，提交后截图
   - 确认页面没有删除按钮；若有删除按钮，记为异常
5. `员工` 点击路径：
   - 登录后检查左侧菜单，确认没有 `招聘人才资产`
   - 若手输路由 `/performance/talentAsset`，应进入无权限或空白拒绝态；截图即可
6. 留痕最小截图集：
   - `HR` 列表页
   - `HR` 详情页
   - `HR` 新增成功
   - `HR` 删除成功
   - `经理` 列表页
   - `经理` 编辑成功
   - `员工` 无入口或拒绝页

## 当前已补 GUI 留痕（2026-04-18）

1. `employee_platform`
   - 已提供 `http://127.0.0.1:9001` 页面截图。
   - 当前左侧菜单未出现 `人才资产` 入口，符合“员工无入口”冻结规则。
   - 当前截图页停留在“我的考核”，未额外展示手输 `/performance/talentAsset` 的拒绝页，但“无菜单入口”已可作为最小显隐留痕。
2. `manager_rd`
   - 已提供 `人才资产` 列表页截图。
   - 当前只显示 `联调-主题12平台人才`，未显示 `联调-主题12销售人才`，符合经理部门树范围。
   - 已提供编辑弹窗截图，目标记录状态为 `跟进中`，说明经理可进入范围内编辑态。
   - 当前截图未展示保存成功 toast，但与已通过的真实 API 更新结果可以互相印证。
3. `hr_admin`
   - 已提供 `人才资产` 列表页截图，当前同时可见：
     - `联调-主题12平台人才`
     - `联调-主题12销售人才`
   - 已提供详情弹窗截图，当前只展示冻结允许字段，未见手机号、邮箱、微信、简历全文、附件链接。
   - 已提供“新建人才资产”表单打开截图。
   - 已提供“编辑人才资产”弹窗截图。
   - 已补充“新建提交后列表结果”截图：列表由 `2` 条变为 `3` 条，新增记录为：
     - 候选人：`少`
     - 编码：`1`
     - 目标部门：`总部`
     - 状态：`新建`
   - 已补充“删除后列表结果”截图：前述新增记录消失，列表恢复为原有 `2` 条联调样例。
   - 当前新增/删除成功态留痕采用“结果列表前后对比”方式呈现，未额外捕获 toast，但足以证明 GUI 点击结果已落到页面。

## GUI 证据结论

1. 主题12前端入口显隐已形成真实页面证据：
   - `HR` 可见
   - `经理` 可见且受部门树范围裁剪
   - `员工` 无入口
2. 主题12详情页字段边界已形成真实页面证据：
   - 当前页面未展示冻结禁止字段
3. 主题12 `HR` 新增与删除已形成真实页面结果证据：
   - 新增后列表出现新记录
   - 删除后列表恢复至原有两条样例
4. 因此当前 GUI 留痕状态应记为：
   - `页面显隐 / 列表范围 / 详情字段边界 / 表单打开 / HR 新增成功 / HR 删除成功`：已完成

## Blockers And Waivers

- Open `P0` blockers：
  - 无
- Open `P1` blockers：
  - 无
- Open `P2` issues：
  - 无
- Approved waivers：
  - 无

## Before Handoff（当前快照）

- Verification summary：
  - 已完成后端定向测试、前端 `type-check/build`、共享菜单 / 权限种子落库、主题12真实 API 联调和 `8014` 实例全量 smoke。
- What was run：
  - 环境版本预检
  - `npm run test -- test/performance/talent-asset.service.test.ts`
  - `npm run build`
  - `corepack pnpm run type-check`
  - `corepack pnpm run build`
  - `rg -n "performance:talentAsset|/performance/talentAsset|talent-asset|人才资产" ...menu.json ...smoke-stage2-performance.mjs`
  - `node scripts/seed-stage2-performance.mjs`
  - `npm run smoke:stage2:performance`
  - `base=http://127.0.0.1:8014` 主题12定向真实 API 联调
  - `STAGE2_SMOKE_BASE_URL=http://127.0.0.1:8014 node scripts/smoke-stage2-performance.mjs`
- What passed：
  - 环境基础可用
  - 主题12后端定向测试通过
  - 主题12前端 `type-check/build` 通过
  - 主题12共享菜单 / 权限种子已写入数据库
  - 主题12 `permmenu` smoke 断言通过
  - 主题12真实 API 联调通过
  - `8014` 实例 stage2 smoke 全量通过，主题12自动化断言已纳入
- What was not run：
  - 无
- Remaining risk：
  - 真实联调当前留痕入口为隔离实例 `http://127.0.0.1:8014`。

## 当前结论

- `主题12联调测试已完成，可进入下一阶段；纯 GUI 点击留痕已完成`
