# xuedao repo-level verification record.
# Captures executed evidence for the thirty-third-batch governance rollout
# that registers the UI design system as a repository-level source of truth.

recordVersion: "1.0"
verificationId: "XUEDAO-VER-20260425-031"
changeId: "XUEDAO-CHG-20260425-030"
owner: "codex"
executedAt: "2026-04-25T15:45:42+08:00"
overallStatus: "passed"

coverage:
  riskLevel: "L2"
  categories:
    normalPath: "covered"
    errorPath: "covered"
    boundary: "covered"
    permission: "not_run"
    stateFlow: "not_run"
    contract: "not_run"
    releaseReadiness: "covered"

checks:
  - name: "ssot-manifest-path-check"
    type: "quality"
    command: "node ./scripts/check-xuedao-ssot-manifest.mjs"
    scope: "repo manifest path 与 artifact root 关键路径校验"
    status: "passed"
    artifacts: []

  - name: "ui-token-ssot-all-consumers"
    type: "quality"
    command: "node ./scripts/check-ui-token-ssot.mjs --all"
    scope: "验证 UI 设计系统主源登记、共享模式文件登记和 consumer 范围 raw color drift 守卫"
    status: "passed"
    artifacts: []

  - name: "repo-guard-script-regression"
    type: "test"
    command: "node --test ./performance-management-system/test/repo-guard-scripts.test.mjs"
    scope: "repo guard regression after UI design system SSOT manifest and guard rollout"
    status: "passed"
    artifacts: []

  - name: "ssot-conformance-check"
    type: "quality"
    command: "node ./scripts/check-xuedao-ssot-conformance.mjs"
    scope: "repo ssot conformance after UI design system docs, records, and guard updates"
    status: "passed"
    artifacts:
      - "reports/delivery/xuedao-ssot-conformance.latest.md"
      - "reports/delivery/xuedao-ssot-conformance.latest.json"

skipped: []

findings:
  blockers: []
  followUps: []

residualRisk:
  - "UI token 守卫当前只覆盖 manifest 声明的 consumer 范围；后续若新增前端模块或样式入口，仍需同步登记，否则会形成治理盲区。"

summary:
  passedChecks: 4
  failedChecks: 0
  skippedChecks: 0
